# AI-無限恐怖 TRPG｜UI 版面與閱讀體驗協作審查

**審查範圍：** `public/index.html`、`public/app.js` 的遊戲主畫面

**本輪目標：** 延續第一輪的桌面空間優化，進一步調整決策卡的資訊層級，並讓故事流具備回合邊界、事件篩選與最新位置定位，降低長時間遊玩時的閱讀負擔。

## 設計核心

這個遊戲的主要節奏是「閱讀敘事 → 理解局勢 → 選擇行動 → 接受規則結果」。因此介面不應把骰池、DC 或警告放在最醒目的位置，讓玩家只挑數字最大的選項；也不應讓判定事件、玩家選擇與說書人正文混成一條沒有段落的文字牆。

本輪採用兩個原則。第一，**決策卡先呈現意圖，再呈現代價**：玩家先看到「要做什麼」與「可能改變什麼」，接著才看到屬性、技能、難度、骰池與風險。第二，**故事流先維持完整，再提供視角切換**：篩選只是暫時隱藏不相關事件，不刪除任何資料；玩家可以專心閱讀敘事，也可以切換回事件檢視規則結果與局勢變化。

## 第一輪桌面版面成果

| 項目 | 調整內容 | 設計目的 |
|---|---|---|
| 桌面工作區 | 上限從 1280px 放寬至 1760px | 減少寬螢幕兩側空白，將空間還給遊戲主欄 |
| 角色 HUD | 固定 320px 改為 `clamp(16rem, 18vw, 20rem)` | 保留角色狀態，但不讓側欄過度壓縮正文 |
| 故事流 | 桌面內距收斂，正文上限提高至 70rem；系統事件控制在 58rem | 讓中央主欄變寬，同時維持適合長文的行長 |
| 決策卡高度 | 桌面最小高度從 6.85rem 降至 5.35rem | 減少底部控制區對故事可視高度的侵蝕 |
| 寬桌面選項 | 1536px 以上由 2×2 切換為 4×1 | 充分利用寬螢幕，避免選項堆成兩排 |
| 手機版 | 保留角色抽屜、單欄故事流與手機選項 | 不改變既有觸控操作模型 |

## 第二輪：決策卡資訊層級

決策卡現在採用「主標題／結果提示／規則摘要」三層結構。主標題使用較大的敘事字體，負責回答玩家「我要做什麼」；hint 使用稍弱的文字，負責回答「這樣做可能改變什麼」；最底部規則摘要才呈現「屬性＋技能 · 難度 DC」，並將骰池與未受訓風險放在次要位置。

純敘事選項不再套用檢定卡的空欄位，而是以「純敘事行動」與「不擲骰 · 場景仍會推進」明確說明。如此一來，玩家在按下選項前就能理解這是一個沒有骰點失敗、但仍會推進劇情的選擇。

| 資訊 | 顯示層級 | 顯示方式 |
|---|---|---|
| 行動意圖 | 第一級 | 較大的 Noto Serif TC 標題 |
| 預期結果／hint | 第二級 | 敘事字體，保留完整句意並自然換行 |
| 屬性、技能、難度與 DC | 第三級 | 帶骰子圖示的規則摘要 |
| 骰池 | 第三級中的次要資訊 | 與規則摘要並列，但不搶主標題焦點 |
| 未受訓、自動失敗、保底等風險 | 條件式警示 | 放在摘要列尾端，以語意色識別 |
| 無需檢定 | 清楚標籤 | 顯示純敘事行動與場景推進說明 |

## 第二輪：故事流結構

故事流現在會在玩家行動前加入一條低對比度的「回合 N」分隔線。它不是額外的敘事內容，而是閱讀定位工具，讓玩家在回看時可以快速辨認「哪一段是上一回合」「哪一段是玩家當時做的選擇」。既有的事件種類、順序與資料內容沒有改變。

故事流頂部新增檢視工具列，包含「全部」「敘事」「事件」三種視角。「全部」是預設狀態，保留完整時間線；「敘事」只顯示說書人正文，適合集中閱讀；「事件」則顯示玩家行動、判定、世界變化、傷害、休息與系統訊息，適合確認規則結果。篩選使用 `hidden` 暫時控制顯示，不會移除 DOM 或改動存檔資料。

當故事內容超過可視高度且玩家已向上回看時，工具列會出現「最新」按鈕。按下後會回到故事流底部；一般情況使用平滑捲動，系統開啟減少動態偏好時則改為立即定位。

## 本地驗證結果

本地測試使用 1280×1100 viewport，並建立多回合、長篇敘事、判定、世界變化、傷害與長選項測試資料。靜態預覽沒有後端 API，因此不會真的送出 AI 回合；這次驗證聚焦於 DOM、CSS 與瀏覽器互動。

| 驗證項目 | 結果 |
|---|---|
| 故事流紀錄計數 | 8 則事件正確顯示 |
| 回合分隔 | 2 個 action 產生 2 個回合分隔 |
| 敘事篩選 | 只顯示 3 則 narration，其他事件保留但隱藏 |
| 事件篩選 | 保留 divider、action、check、world、harm 等事件 |
| 最新定位 | 回看上方時顯示；定位後可隱藏 |
| 長選項 | 4 張卡自然換行，沒有水平溢出 |
| 長提示 | 卡片高度自然增加，規則摘要仍留在底部 |
| 1280px 選項排列 | 維持 2×2，避免文字過密 |
| 1536px 以上選項排列 | 切換 4×1，使用寬桌面空間 |
| 減少動態偏好 | 最新定位改用立即捲動，不強制平滑動畫 |
| 核心測試 | 772 個測試全部通過 |

## 修改檔案

本輪修改集中在 `public/index.html` 與 `public/app.js`。HTML 新增故事流工具列、篩選控制與最新定位按鈕；CSS 新增決策卡三層資訊樣式、回合分隔線、工具列與響應式規則；JavaScript 新增事件資料標記、回合分隔生成、紀錄計數、篩選狀態、最新定位與減少動態支援。

不曾修改核心規則引擎、AI 回合 API、存檔資料格式、角色數值計算、擲骰流程或手機角色抽屜。`app.js` 的引用加入版本參數 `?v=20260822-r2c`，避免部署後長時間快取舊版前端腳本。

## 下一輪協作建議

下一步最值得討論的是**決策卡在不同螢幕尺寸下的資訊密度**。目前長選項會自然增高，這比硬截斷更安全；若未來 AI 常產生更長的行動文字，可以再增加「主標題最多兩行」的策略，並把完整文字保留在 `title` 或展開狀態中。

故事流方面，下一輪可以考慮把「目前回合」做成更明確的 sticky 定位，或加入只顯示最近一回合的檢視模式。不過這些功能會影響玩家對完整歷史的理解，因此應先觀察實際遊玩時「敘事」與「事件」兩個篩選的使用頻率，再決定是否增加更多模式。

角色 HUD 的窄欄／完整欄切換仍適合放在更後面。它涉及互動狀態與可用空間，而不是單純 CSS 調整；在決策卡與故事流的閱讀模型穩定後再做，風險較低。

## 目前狀態

修改目前保留在本地工作樹，尚未替使用者 commit 或 push 到 GitHub。這份文件與 `UI_AUDIT_NOTES.md` 是協作紀錄，不參與遊戲執行。


## 第三輪：主欄優先與故事紀錄彈窗

本輪依照明亮主題截圖重新分配遊戲主畫面的優先順序。完整 `#story-feed` 已移入 `#storyLogModal`，主畫面改由 `#story-current` 只呈現最新事件；玩家可以從「故事紀錄」按鈕開啟完整時間線，再使用原有的「全部／敘事／事件」篩選與「最新」定位。這讓中央區域不再同時承擔長篇正文、完整歷史與選項列三種工作。

任務目標 HUD 已從中央頂端水平列移入左側角色欄，改成角色檔案的一部分，包含目前目標、主線進度、威脅與時間資訊。頂端導覽列則縮成輕量高度，左側只保留小型遊戲上下文，右側保留安裝、主題、存檔與設定等功能按鈕。

決策面板進一步收窄。中型桌面自 1100px 起採四欄選項，保留自然換行但不再讓 2×2 選項佔據大面積垂直空間；自訂行動列同步降低內距。固定桌面測試狀態下，主欄約 973px 寬、最新敘事區約 824px 高、決策面板約 194px 高，四張選項卡同列約 84px 高。

## 第三輪：明亮主題與流程修正

明亮主題的建卡選項不再套用深色副本的灰黑背景，題目卡、重塑卡與甦醒摘要改用白色／暖白色層次；角色欄的屬性條軌道改為深色，填色改為深灰綠並加寬到 4.5rem，確保整條進度在亮色背景上可見。亮色任務 HUD 也有專用的淺色背景與進度軌覆蓋。

建卡題目頁移除沒有行為作用的「選一個」底部按鈕。玩家點選選項後仍會自動進入下一題，但改為只更新選取卡片狀態並以 180ms 延遲換題，不再重播整個題目區的淡入動畫，因此不會讓題目與背景一起閃爍。基本資料與甦醒階段仍保留一次性進場過場。

回訪玩家的入口初始化新增 `data-booting` 遮罩，登入／本地存檔檢查完成以前不繪製邀請封面；若有有效存檔，續玩主神空間會使用 `is-resume` 直接呈現，不再先閃過約半秒的封面。前端腳本版本更新為 `?v=20260822-r3`。

## 第三輪驗證狀態

瀏覽器固定測試資料已確認明亮題目選項為白色、底部「選一個」隱藏、點選後由問題 1 正常進入問題 2，且未重播題目區動畫。遊戲測試資料確認左欄目標、主欄最新敘事、四欄選項與故事紀錄彈窗可以同時存在，彈窗中仍保留完整事件流。`node --check public/app.js`、`git diff --check` 與完整 `npm test` 均通過，772 個測試全部通過。

本輪修改尚未 commit 或 push；靜態預覽中的 `/api/auth/me` 與 `/api/character` JSON 錯誤是因預覽只提供靜態檔案、沒有後端 API，不屬於本輪 UI 回歸。


## 第四輪：工具 dock 與角色欄停靠

本輪將遊戲狀態下原本佔據最上方一整列的功能按鈕移除。存檔管理、明暗主題、PWA 安裝與系統設定集中至左下角可收合的「系統工具」dock；入口頁與建卡頁仍保留原有頂端功能列，避免玩家在非遊戲狀態失去入口功能。dock 使用 `details/summary`，關閉時只留下小型觸發鈕，開啟後再展開功能按鈕；角色欄切到左側時 dock 會自動避到右下角。

桌面版的角色欄現在預設停靠右側，讓中央敘事欄從左側直接開始，降低玩家視線在「正文 → 選項」之間的橫向跳動。設定欄新增「輪迴者欄位位置」，可切換「右側（預設）」與「左側」，偏好寫入 `localStorage.user_hud_position`。手機版不受此偏好影響，角色欄仍固定從左側以抽屜方式開啟，維持觸控操作的一致性。

屬性與技能卡移除卡片中間的 `.stat-meter` 迷你條，只保留左緣 `stat-tile-rail` 的條形計數與右側數值。這讓同一個數值不再以兩種進度條重複表達，也讓卡片內部回到「代碼／中文名稱／數值」的簡潔資訊結構。明亮主題的深色屬性條修正仍保留在左緣計數上。

## 第四輪驗證

固定 1280×1100 桌面遊戲狀態確認：頂端 header 為 `display:none`，中央主欄約 973px 寬，角色欄約 256px 寬且 `order=2`，系統工具 dock 為 `display:block` 且預設收起；切換至左側後角色欄 `order=1`、主欄 `order=2`，dock 會移到右下角。屬性與技能卡中的 `.stat-meter` 數量均為 0，設定選項與 `localStorage` 值同步。

`node --check public/app.js` 與 `git diff --check` 通過；前一輪完整 `npm test` 為 772 個測試全部通過。本輪瀏覽器測試確認存檔、主題、安裝與設定四個功能仍可從 dock 操作，登入元件與主題／PWA 狀態也同步支援 dock 版本。


## 第五輪：單一齒輪與 HUD 密度控制

本輪將遊戲內系統入口收斂為唯一的齒輪按鈕，不再讓 dock 在關閉時顯示「系統工具」文字。齒輪的位置與角色欄遵循同一條空間規則：角色欄在右側時，齒輪停靠右下；角色欄在左側時，齒輪停靠左下。dock 展開後才顯示存檔、主題、安裝與設定四個工具，入口頁與建卡頁仍維持原本的導覽功能。

角色欄新增窄化／展開模式。展開狀態顯示完整角色、任務與分頁；窄化狀態只保留窄 rail、HUD 標記與展開按鈕，桌面測試寬度約 74px，中央主欄約增加 181px。狀態寫入 `localStorage.user_hud_compact`，不影響手機抽屜；手機仍以左側抽屜查看完整角色資料。

任務目標 UI 改為四個穩定資訊區塊。第一區是玩家可執行的目標句，第二區是主線完成度與短進度條，第三區是「迫近度」階段與獨立 pips track，第四區是「剩餘回合／時間狀態／剩餘／總數」。剩餘回合數使用 `JetBrains Mono` 等寬數字與 `font-variant-numeric: tabular-nums`，不再把整句「時間：充裕（14/14）」以同一個大字級塞進 badge，藉此避免歪斜與視覺搶焦。

## 第五輪驗證

固定 1280×1100 桌面測試確認：預設角色欄在右側、齒輪位於右下；切換左側後角色欄 x 約 26px、齒輪 x 約 12px，兩者同側。角色欄窄化後寬度約 74px，主欄寬度約 1154px，展開按鈕會更新為「展開角色欄」並保留 `aria-pressed` 狀態。任務 HUD 中迫近度 pips 與剩餘回合數均在欄內完整顯示，`.stat-meter` 仍為 0 個。

`node --check public/app.js`、`git diff --check` 與完整 `npm test` 均通過，772 個測試全部通過。


## 第六輪：窄化角色欄與故事閱讀鎖定

本輪把角色欄窄化定義為「保留即時生存資訊，而非單純隱藏欄位」。窄 rail 固定留下生命、意志、防禦與先攻四組狀態，搭配圖示與實際數值；完整角色名稱、屬性、技能、特質、日誌與任務細節則在展開角色欄後查看。這使玩家在主欄閱讀或等待 AI 回覆時，仍能快速確認最關鍵的戰鬥狀態。

故事紀錄加入閱讀鎖定模型。當玩家向上捲動回看，系統標記為「閱讀鎖定」並停止新事件自動置底；後續事件只累積未讀數，工具列顯示「有新內容 · N」。玩家點擊最新定位後，故事流回到底部，未讀數清除並恢復跟隨最新。篩選切換與重新讀檔都會安全重置／更新閱讀狀態，避免把舊的鎖定狀態帶到另一個事件視角。

| 實測尺寸 | 遊戲版面結果 | 建卡版面結果 |
|---|---|---|
| 390×844 | 無水平溢出；選項單欄約 366px；角色抽屜 x=0、寬 320px | 面板約 358px；選項約 308px；無水平溢出 |
| 430×932 | 無水平溢出；選項單欄約 406px；角色抽屜 x=0、寬 320px | 面板約 398px；選項約 348px；無水平溢出 |
| 768×1024 | 無水平溢出；桌面雙欄 HUD；選項 2 欄約 221px | — |

實際瀏覽器測試確認窄化後角色欄約 74px，四個 rail 數值可見且中央故事區未被遮蓋。實際故事紀錄 modal 測試也確認：回看到頂部時新增事件保持原 scrollTop，最新定位後才回到最大 scrollTop、清除未讀並解除鎖定。建卡題目頁以關閉故事 modal 後的隔離截圖判斷，390px 與 430px 均能看見兩行題目、四張單欄選項卡及下一步按鈕；先前被 modal 覆蓋的截圖不納入結論。

本輪修改仍不涉及核心規則、AI API 或存檔格式。靜態預覽中的 `/api/auth/me` 與 `/api/character` JSON 錯誤仍是本地預覽缺少 Cloudflare Pages Functions 的預期限制。

## 第七輪：HUD 基準線與操作入口回歸

本輪優先修正玩家最直接的回饋：選項與自訂輸入列不能因版面狀態而「看起來消失」。檢查後確認兩者一直存在於 DOM，因此採用狀態隔離而不是重新複製元件：一般劇情模式保留 `#story-action-panel`，戰鬥模式才隱藏；離開戰鬥時清除狀態 class 並恢復劇情操作列。這個規則同時保留「戰鬥使用戰鬥操作」與「劇情使用決策卡／自訂行動」兩種互不干擾的入口。

任務 HUD 改以一致的「標題與數值同列、視覺刻度下一行」基準線。迫近度固定七格，不再直接採用內容包可能不同的 `max`；剩餘回合也採用七格倒數刻度，讓 `14/14`、`7/14`、`5/16` 等狀態都能用同一個比例語法閱讀。主線進度與迫近度以同寬雙卡並排，剩餘回合則讓「剩餘回合／充裕／14/14」保持水平對齊。

屬性格改為英文縮寫、中文名稱與數值同列，保留左緣 rail 作為等級視覺提示，讓單格高度降至約 36px。輪迴者檔案則重新分出身份標題列、XP／身份摘要、生命傷勢軌、衍生數值與任務卡，透過細分區線與一致內距改善原本「資訊散落」的感受；暗色／明亮主題各自使用相同語意 token 的對比覆寫，不改變既有暖墨與紙質語言。

| 驗證尺寸 | 實際結果 |
|---|---|
| 1280×1100 桌面 | 4 張選項卡與自訂輸入列可見；屬性格約 36px；迫近度與剩餘回合各有 7 格 |
| 390×844 手機 | 選項單欄、輸入列完整；無水平溢出；角色抽屜 x=0、寬 320px |
| 430×932 手機 | 選項單欄、輸入列完整；無水平溢出；角色抽屜 x=0、寬 320px |
| 768×1024 平板 | 角色欄／主欄雙欄；選項 2×2；輸入列可見；無水平溢出 |

本輪前端腳本版本更新為 `?v=20260822-r7`。

## 第八輪：以共同基準線精修輪迴者檔案

本輪的第一個設計結論是：**主線進度條與迫近度七格已完全對齊；剩餘回合刻度則是有意識的第二行，不應硬拉到主線同一 y。** 在 1280×1100 實際 viewport 中，主線 track 與迫近度 track 都是 y=351.2、高 6.7px；屬性格內部內容線的 x=1018 與主線進度填色起點相差 0px。剩餘回合刻度 x=1017、寬 219.8、高 6.7px，與主線共用左右內容 gutter，但因其承載「標籤／狀態／N/總數」及倒數資訊，放在下一行能保留清晰的閱讀順序。

輪迴者檔案的視覺最佳化採用五項具體策略。第一，身份標頭加上短導引線，讓「輪迴者檔案」成為穩定的視覺錨點。第二，姓名／身份摘要與 XP 以獨立區塊處理，避免與生命數值黏成同一串。第三，生命傷勢軌與衍生數值之間加上細分隔線，並把防禦、先攻、意志整理成一致的三欄讀數。第四，分頁改為等寬 tab 與單一 active 底線，降低原本像零散按鈕的感覺。第五，屬性格保留左緣 rail，但改成英文縮寫、中文名稱與數值同列，讓比較行為更快、卡片高度更低。這些調整都沿用暖墨／紙質 token，不加入外部影像或改變核心資訊架構。

| 版面判斷 | 第八輪結論 |
|---|---|
| 主線與迫近度 | 同 y、同高、同內容寬度基準；迫近度固定七格 |
| 剩餘回合 | 同列文字＋下一行七格倒數，第二行是刻意的層級分隔 |
| 屬性格 | 36px 高；內容線與主線填色起點 x 差 0px |
| 輪迴者檔案 | 身份、生命、衍生數值、任務、分頁五段式層級 |
| 響應式 | 390×844、430×932、768×1024 均無水平溢出，選項／輸入列保持可見 |

本輪前端腳本版本更新為 `?v=20260822-r8a`。


## 第九輪：從完整故事流轉為近期現場與玩家手稿

本輪正式撤銷「主畫面承擔完整故事歷史」的設計。新的閱讀模型是：**中央只服務當下決策，劇情回顧才服務長期閱讀**。中央 `#recent-story-list` 保留最後五則訊息，玩家可以在這個小窗口上滑回看最近現場，但更早內容不再以隱藏 DOM、篩選狀態或完整時間線留在主畫面。這個分界同時改善閱讀節奏、降低高頻 DOM 操作，也避免後續 check／world／fault 訊息把說書人正文當成唯一「最新敘事」覆蓋。

故事訊息採結構化 entry 而不是 DOM 當資料來源。說書人正文使用較大的小說字級；玩家行動、判定與世界事件使用次級卡片；暫時等待則是獨立的「說書人書寫中」卡，顯示 typing dots 與經過秒數。如此即使伺服器回應需要數十秒，玩家也能知道系統正在工作；回應抵達時只移除 pending，不重建或 clone 一條完整故事流。

劇情回顧入口與主神商店並列，回顧 modal 使用「玩家的輪迴手稿」作為視覺隱喻。章節以 scenarioId 分開，正文維持時間順序，metadata、章節標籤與 AI package 狀態被放在正文周圍而不打斷閱讀。進行中的副本只顯示「持續累積」；副本結束後才顯示可複製／下載的 ready package，不對玩家宣稱資料已送給 AI，除非真的發生後續 context 使用。

| 設計問題 | 第九輪決策 | 可驗證指標 |
|---|---|---|
| 完整故事流造成主畫面不順 | 主畫面只 render 最近五則 entry | 永久 entry 數固定為 5；無 `#story-feed` |
| pending 被事件覆蓋 | pending 直接進 recent renderer，narration 回來後只移除 pending | 等待期間可見秒數與 typing dots |
| history 只有八回合 | 新增 chronicle，history 仍只負責短期 prompt | 12 回合以上仍可回顧；legacy history 可補欄位 |
| 長期故事何時給 AI | 副本完成時建立唯一 deterministic package；後續 prompt 只帶 compact context | package status=ready；每回合 context 最多最近 2 份且截斷 |
| 長篇 modal 影響主畫面效能 | 回顧開啟時才請求 `/api/chronicle` | runtime session 僅取 recent preview |

## 第九輪實測結論

含 Pages Functions 的本地預覽完成真實建卡、開場回合與劇情回顧流程。測試角色完成五題後，開場 response 實際顯示說書人正文、四張選項卡與自訂輸入框；中央標示 `1 / 5`，且主神商店旁的劇情回顧入口存在。打開回顧後，瀏覽器實際讀到副本標題、角色名、1 回合／1 則長期紀錄及完整開場 prose；AI package 在進行中狀態顯示尚未封存。

390×844、430×932 與 768×1024 稽核皆無水平溢出。390px 的 chronicle panel 為 366×828，footer bottom=835；430px 的 panel 為 406×907，footer bottom=919；兩者的正文區均有獨立 overflow auto。遊戲畫面在 390／430px 以單欄選項呈現，768px 以 2×2 呈現；四張選項與輸入列均在 viewport 內。1280×1100 桌面量測中 recent list 與 action panel 精確銜接，action panel bottom=1100。

本輪實作仍保留暖墨／紙質 token、右側預設角色欄、同側齒輪停靠、手機左側角色抽屜與屬性／技能不重複 meter 等前輪原則。`/api/chronicle` 採現有 ownership 門禁； deterministic package 由 storage 純函式組裝，不新增通關 LLM 呼叫與額外延遲。
