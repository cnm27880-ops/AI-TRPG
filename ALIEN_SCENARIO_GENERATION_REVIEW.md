# 《異形：生化深淵》副本生成內容可用性審查

**審查對象：** 使用者提供的《異形(一).md》至《異形(四).md》四份生成內容
**對照專案：** `cnm27880-ops/AI-TRPG`，目前審查基準為 `main` 分支 HEAD `c92f41d`
**審查目的：** 判斷生成內容能否使用、哪些部分可以直接接入、哪些部分需要補寫或修改，以及它是否已經足以支撐 AI GM 式的自由行動玩法。

---

## 一、總結結論

這四份內容**可以使用，而且整體方向是成功的**。它不是只有一篇《異形》的劇情簡介，而是已經包含副本世界觀、地圖、NPC、物品、線索、威脅階段、20 張事件卡、判定方法、陣營信號、結局矩陣與死亡結局。從「副本設計資料」的角度來看，它已經足以作為第一版劇本聖經與事件資料庫的基礎。

但是，它目前還不能被稱作「可直接丟進現有引擎的完整副本包」。最準確的定位是：

> **這是一份內容設計完成度很高、程式接入完成度仍偏低的副本草稿。**

它最適合目前拿來做三件事：第一，作為 AI GM 讀取的固定世界真相；第二，作為之後程式事件系統的資料來源；第三，作為 Gemini 或其他模型生成敘事文字的劇情聖經。它不適合現在直接當成現有 `/api/turn` 可以完整執行的 JSON 副本，因為目前專案的引擎仍然以「節點＋AI 生成選項＋AI 接續敘事」為主，尚未正式支援這份文件裡的 `availableApproaches`、地圖位置、物品狀態、NPC 好感度、事件解鎖與 `stateEffects`。

我的整體評分如下：

| 評估項目 | 評分 | 判斷 |
|---|---:|---|
| 副本核心概念 | 9/10 | 《異形》式封閉空間、生存壓力與企業陰謀都很清楚 |
| 主線骨架 | 8/10 | 五個階段與 20 個事件形成完整逃生流程 |
| AI GM 可用性 | 7/10 | 已有主持真相與事件條件，但仍缺少正式狀態機格式 |
| 自由行動支援 | 7/10 | 已列出多種解法，但資料仍偏「選項卡」而非真正意圖規則 |
| 文字量 | 5.5/10 | 結構很多，但可見演出文字明顯不足，且單段偏短 |
| 事件結果完整度 | 3/10 | 大量 `successTextId` 等欄位已宣告，但實際文字只有一部分 |
| 目前引擎直接相容性 | 3/10 | 需要轉接層與數項程式修正，不能直接匯入 |
| 作為第一版副本基礎 | 8.5/10 | 非常適合繼續補強，不建議推倒重做 |

---

## 二、你覺得「劇情敘述有點短」是正確的

四份文件加起來約 **2,282 行、5,296 個詞元／文字單位、140,201 位元組**。表面上內容並不少，因為它包含大量表格、JSON、ID、欄位與規則說明；但真正供玩家閱讀的敘事文字比例不高。

它比較像「劇情資料庫」而不是「完整小說式劇本」。每個事件通常由一段 `setup`、一個玩家目標、數個行動方法與幾段結果文字組成。這種形式對程式非常有價值，但對玩家來說，一個事件往往只會顯示一小段文字，因此實際遊玩時可能會感覺內容很快。

目前 20 張事件卡共列出 56 個處理方法，程式欄位總共宣告了 **268 個結果文字鍵**，但四份文件中實際寫出的帶有結果標籤的演出文字只有 **108 個**，約有 **160 個結果文字仍然缺少內容**。這是目前最明確的文字量缺口。

| 項目 | 數量 | 意義 |
|---|---:|---|
| 事件卡 | 20 | 骨架已完整 |
| 處理方法 | 56 | 每個事件約 2 至 3 種預設解法 |
| 已宣告結果文字鍵 | 268 | JSON 欄位已預留完整五級結果 |
| 實際寫出的結果文字 | 108 | 目前真正有內容的結果 |
| 尚未補齊的結果文字 | 約 160 | 不能宣稱每個方法都已完整 |
| 結局 | 8 種 | 已有生還、支線、感染與死亡結局 |

這裡要注意，**短不等於一定不好**。短的 `setup` 和結果文字適合作為 AI 的事件提示，讓模型自己依玩家行動寫出完整段落；但如果你希望某些文字在沒有 AI、AI 輸出失敗或使用固定事件卡時直接顯示，就必須補得更完整。

我不建議把全部文字無差別擴寫成長篇小說。比較好的做法是分成三種層級：

| 文字層級 | 建議長度 | 用途 |
|---|---:|---|
| 事件資料層 | 一至三句 | 描述真相、條件、後果，供規則系統讀取 |
| 玩家演出層 | 約 100 至 250 字 | 顯示一個回合的行動結果 |
| 重大場景層 | 約 300 至 800 字 | 開場、真相揭露、Ash 衝突、自毀、最終戰與結局 |

目前最值得擴寫的不是所有事件，而是**開場、事件 06、事件 11、事件 12、事件 14、事件 17 至 20，以及八個結局**。這些地方決定玩家是否感覺自己真的經歷了一個完整副本。

---

## 三、目前已經做得很好的部分

### 3.1 主線骨架已經有清楚的戲劇節奏

文件將副本分成休眠與異常、調查與分歧、污染與企業真相、自毀倒數、接駁艇決戰五個階段。事件從 `evt_cryo_inspect` 開始，經過橋樓、醫療區、科學區、貨艙、主機核心與工程區，最後進入水仙號決戰。這條主線很容易被玩家理解，也符合第一個副本應該先讓玩家學會遊戲規則的需求。

它不是單純把電影場景一個個重播，而是把玩家放進固定危機中，讓玩家決定先調查、先拿資源、先相信誰或先逃跑。這一點已經比「請 AI 自由模仿電影」更接近 TRPG。

### 3.2 每個事件都有主持真相

事件卡中的「主持真相」非常重要。例如貨艙不是單純的場景描述，而是明確寫出它是異形巢穴外圍、Brett 在此遇害、鏈條與積水提供匿蹤環境。這種資料能讓 AI 知道表面文字背後真正發生了什麼。

未來 AI GM 應該優先讀取這類資料，而不是只讀 `title` 或 `setup`。這也是本次生成內容與「讓 AI 搜尋《異形》電影後自行決定」之間最清楚的區別。

### 3.3 解法不只一種，已經有 TRPG 味道

多數事件至少提供調查、交涉、環境利用、暴力處理或資源消耗等不同方法。例如工具櫃可以撬鎖、砸鎖，或請陸遠開槍；主機房可以破解門禁、使用權限卡或用焊槍破拆；水仙號可以穿戴太空服、搶魚叉、使用冷卻劑或操縱氣閘。

這些解法不應被視為玩家只能按的固定選項，而應視為「GM 提醒玩家目前可想到的幾種方向」。玩家說出其他合理做法時，AI GM 應該使用相同的事件真相與後果規則進行裁定。

### 3.4 失敗不是單純顯示「你失敗了」

很多結果文字會增加威脅、造成傷勢、損失物品、提高噪音、影響 NPC 信任或改變撤退條件。這是很好的方向，因為 TRPG 的失敗應該改變世界，而不是讓玩家回到原地重新骰同一件事。

### 3.5 結局矩陣已經具備重玩價值

目前有單人生還、救出同伴、攜帶樣本、感染生還、超時重傷，以及三種死亡結局。玩家不是只有「成功／失敗」兩個出口，而是會根據是否救人、是否帶回樣本、是否接受感染風險、是否完成自毀與是否固定安全繩，導向不同結果。

這些結局可以保留。不過它們目前比較像結果設計稿，還需要轉成程式真正能計算的狀態條件。

---

## 四、目前最重要的內容問題

### 4.1 事件結果文字實際上遠未完整

每個需要檢定的處理方法大多宣告了：

`success`、`narrow_success`、`minor_failure`、`failure`、`critical_failure`

但實際演出文字常常只寫其中一至三個。例如事件 01 的 `recon` 方法五級都有內容，但 `console` 與 `ask` 只有 `success`；事件 04 的三個方法加起來只有少數結果；事件 16 至 20也普遍只有部分結果。

更嚴重的是，很多 JSON 中的 ID 是完整路徑，例如：

```text
 evt_cryo_inspect.recon.success
```

但演出文字使用的是短 ID：

```text
 recon.success
```

如果引擎以完整 ID 查找文字，這些內容會被判定為不存在。若引擎打算以事件 ID 自動加前綴，就必須明確定義這個規則；否則應統一成完整 ID。

建議優先做一次「文字鍵正規化」，例如統一使用：

```text
 evt_cryo_inspect.recon.success
 evt_cryo_inspect.recon.narrow_success
 evt_cryo_inspect.recon.minor_failure
 evt_cryo_inspect.recon.failure
 evt_cryo_inspect.recon.critical_failure
```

不要一部分使用完整 ID、一部分使用 `recon.success`，也不要讓程式靠猜測補前綴。

### 4.2 有些事件沒有完整描述「結果造成的狀態變化」

`stateEffects` 大多是空物件，真正的狀態變化被藏在自然語言中。例如文字寫「陸遠信任度 +3」「獲得物品」「威脅度上升」「工具損壞」，但 JSON 沒有正式欄位記錄這些變化。

對人類主持人來說，這樣仍然看得懂；對程式和 AI GM 來說，這會造成不穩定。建議每個結果都增加類似以下的結構：

```json
{
  "resultId": "evt_meet_ripley.evidence.success",
  "effects": {
    "npcTrust": {"npc_ripley": 3},
    "cluesAdd": ["clue_crew_status"],
    "unlockEvents": ["evt_narcissus_prep"],
    "threatStage": -1,
    "itemsAdd": []
  },
  "narrative": "Ripley 看著帶血的日誌數據板……"
}
```

現在的資料已經有這種設計的雛形，但必須把規則效果從文字中抽出來。

### 4.3 事件解鎖圖與主線節點圖有部分衝突

事件卡的 `unlocks` 形成了一張比目前 `n1` 至 `n4` 節點更細的事件網路，但許多事件只是寫成「解鎖」而沒有說明：解鎖後是否可以跳過前置事件、事件是否只能在特定地點觸發、事件完成後是否關閉其他事件、玩家回頭時是否還能觸發。

例如：

- `evt_meet_ash` 解鎖 `evt_mother_chamber_infiltrate` 與 `evt_cargo_stalk`。
- `evt_order_937_reveal` 解鎖 `evt_ash_ambush` 與 `evt_trigger_overload`。
- `evt_trigger_overload` 關閉 `evt_deck_a_terminal`。

這些關係很有潛力，但需要明確定義成三種不同概念：

| 概念 | 意義 |
|---|---|
| `unlocks` | 事件從不可用變為可用 |
| `requires` | 沒有滿足就不能觸發 |
| `closes` | 發生某事件後，其他事件永久失效 |

目前部分事件用 `觸發條件`、部分用 `unlocks`、部分用節點階段表達同一件事，稍後接入程式時容易互相矛盾。

### 4.4 陣營信號目前是內容標記，不是完整陣營路線

事件卡的 `alignmentSignals` 很適合作為第一版設計，但現在只代表「這個行動偏向哪一軸」，例如 `善良 +2` 或 `混亂 +1`。文件後段則把幾個結局直接標成守序善良、混亂中立、守序邪惡。

目前仍缺少以下規則：

- 玩家行動如何累積善惡軸與守序混亂軸。
- 中立的容許範圍是多少。
- 玩家是否能在同一副本中改變陣營傾向。
- 陣營是否影響 NPC 信任、事件可用性與結局，而不只是結算標籤。
- 「絕對混亂」的玩家行為如何處理，而不只是把它視為另一個選項。

建議不要把陣營當成玩家必須遵守的路線。它應該是「行動留下的統計與世界反應」。玩家可以先救人，再背叛同伴；也可以先服從指揮，後來為了自由破壞程序。AI GM 只需根據每次行動的實際意圖記錄信號，不要替玩家提前貼上陣營標籤。

### 4.5 「絕對混亂」目前還沒有真正的系統支持

文件提到混亂玩家可能做出非常奇怪的事情，這個方向很有趣，但目前事件卡大多仍然是三個預設解法加一個無檢定選項。真正的混亂玩家可能會提出：故意切斷整層電力、把 NPC 鎖在貨艙、把異形引向 Ash、拆掉水仙號的氧氣管、把樣本丟進反應爐，甚至在不理解完整後果的情況下做出荒謬行動。

這種行動不應直接被 AI 判定為「不符合劇本」。較好的規則是：

1. 先判斷行動在物理上是否可能。
2. 再判斷玩家是否擁有必要位置、工具與時間。
3. 若可行，就選擇最接近的技能與難度。
4. 若行動沒有直接解決主線，就讓它改變威脅、資源、NPC、地圖或事件順序。
5. 只有在明確違反世界規則時才拒絕。

這樣玩家可以真的亂來，但世界不會因此失去邏輯。

---

## 五、目前最重要的程式相容性問題

以下判斷是根據專案審查報告與目前程式結構，而不是單純根據生成文件推測。

### P0：目前引擎不會直接使用這些事件卡

目前 `content/scenario/schema.js` 主要支援節點資料，例如 `id`、`title`、`canonSummary`、`prerequisites`、`baseRewardPoints`、`baseDC`、`isFinale` 與 `bossEncounter`。它還沒有正式支援事件卡裡的：

- `availableApproaches`
- `requiredItems`
- `clues`
- `stateEffects`
- `alignmentSignals`
- `unlocks`
- `closes`
- 地圖位置
- NPC 位置與狀態
- 物品持有與消耗

所以現在把這些內容放進 `alienNostromo.js`，不代表 `/api/turn` 就會按照事件卡運作。

### P0：玩家的自由輸入仍然由關鍵字推導檢定

目前 `content/checkIntent.js` 不是完整語意解析器。它主要根據「觀察、搜索、修理、終端、說服、威脅」等關鍵字推導屬性、技能與難度。

這四份文件已經提供了足夠的自由行動資料，但現有引擎還沒有把玩家輸入轉成：

```json
{
  "goal": "封鎖異形通道",
  "method": "用焊槍焊死通風管格柵",
  "target": "工程區通風管",
  "tools": ["item_blowtorch"],
  "location": "loc_engine"
}
```

因此，玩家輸入「我把焊槍接到冷卻管，故意讓蒸氣噴向通風口」時，目前可能只會被歸類成一般的技藝或求生檢定，而不會真正判斷它是否能改變異形的路線。

### P0：`baseDC` 和事件卡裡的 difficulty 還沒有接成同一套

生成文件使用「容易、普通、困難」等難度描述，這符合目前選項契約；但事件卡又提供自己的 `baseDC`、事件優先級與節點階段。目前這些欄位還沒有成為統一的裁定來源。

建議未來由事件卡固定：

- 使用的屬性。
- 使用的技能。
- 難度名稱。
- 是否需要特定物品。
- 失敗的狀態效果。

實際 DC 仍然由程式根據難度表計算，不能讓 Gemini 自己回傳 DC 數字。

### P0：`nodeComplete` 與事件完成不是同一件事

目前專案仍然以節點完成作為主要推進方式，但生成內容把事件完成、節點完成、主線里程碑與結局觸發混在一起。例如事件 14 完成後可能代表啟動自毀，但事件 15 至 20 仍然是多個必經事件。

需要先定義：

- 事件完成是否自動完成節點。
- 一個節點是否需要完成多個事件。
- 事件失敗是否可以再次嘗試，或只會換成另一個事件。
- `evt_trigger_overload` 是否是一次性主線里程碑。
- `evt_hypersleep_return` 是否直接進入結算。
- 死亡結局是否跳過一般結算。

### P1：時間耗盡與 n-expire 仍需程式接線

文件寫得很清楚：副本有十四回合時間限制，時間耗盡後可能進入 `n-expire` 或死亡結局。然而目前專案的 `onExpireNodeId` 主要只是提醒資料，並沒有完整自動轉入劣化節點的流程。

因此，這份文件中的劣化結局文字可以先保留，但實際測試時要特別確認：玩家在第十四回合結束後是否真的會離開正常節點並進入 `n-expire`。

### P1：位置、NPC、物品與線索需要成為持久狀態

目前專案有短期 history、事件日誌、迫近度與時間預算，但沒有一個完整且由程式維護的世界狀態。文件裡即使寫了「玩家走到貨艙」「陸遠受傷」「取得權限卡」，只要沒有寫入正式狀態，下一回合 AI 仍可能忘記。

最少需要保存：

```json
{
  "location": "loc_cargo",
  "inventory": ["item_flashlight", "item_blowtorch"],
  "cluesFound": ["clue_alien_trace", "clue_vent_network"],
  "npcState": {
    "npc_luyuan": {"status": "injured", "trust": 2, "location": "loc_engine"},
    "npc_ripley": {"status": "alive", "trust": 0, "location": "loc_deck_a"}
  },
  "worldFlags": ["evt_order_937_reveal", "overload_started"],
  "threat": {"stage": "貼近", "track": 4}
}
```

---

## 六、文件內已發現的具體矛盾與錯誤

### 6.1 一個明確的技能欄位錯誤

事件 02 的通訊處理方法使用：

```json
"check": {"attribute": "智力", "skill": "null", "difficulty": "困難"}
```

目前專案的純屬性檢定契約應使用 JSON 的 `null`，而不是字串 `"null"`。否則查驗器可能把它視為一個不存在的技能名稱。

應改為：

```json
"check": {"attribute": "智力", "skill": null, "difficulty": "困難"}
```

### 6.2 事件文字鍵與 JSON 宣告鍵不一致

如前面所述，JSON 宣告的是完整路徑，但演出文字常用短路徑。這不是單純排版問題，而是接入程式後很可能造成找不到文字的實際錯誤。

### 6.3 部分物品 ID 沒有和文字完全對齊

物品表使用英文 ID，例如 `item_medkit`，但事件文字寫「醫療用品」；有些事件結果寫「獲得工程切割焊槍」，但沒有同時列出正式物品 ID。建議所有獲得、消耗、損壞與遺失都使用正式 ID，不要只寫顯示名稱。

### 6.4 事件 13 的 Parker 狀態前後略有跳躍

事件 13 寫 Parker 可能在工程區出現並協助操作，但前面的 NPC 資料主要只描述 Parker 的特質與功能，沒有明確說明他在事件 13 前的所在位置、是否仍然存活、玩家如何知道他在工程區，以及他是否會因前面事件被異形或其他危機影響。

這不是大問題，但需要補一個 NPC 位置與存活狀態規則。

### 6.5 Ash 的「說服」與「底層邏輯鎖死」需要區分資訊與行動

文件一方面寫 Ash 無法透過常規說服改變立場，另一方面仍然提供與 Ash 交涉成功的結果。這兩件事其實可以同時成立，但要寫清楚：

- 交涉成功只能讓 Ash 提供部分資料、降低戒心或延後攻擊。
- 交涉不能讓 Ash 放棄 937 指令。
- 玩家可以欺騙 Ash、利用他的程序限制或讓他誤判威脅。
- 只有破壞、權限覆寫或特殊事件才能改變他的最終行動。

這樣 Ash 才會像一個有固定核心目標的 NPC，而不是依照骰子結果忽善忽惡。

### 6.6 水仙號最終戰的「先脫離、再異形甦醒」順序要明確

事件 16 是脫離母船，事件 17 才是異形在接駁艇內甦醒。這個安排本身合理，但要明確說明：玩家在事件 16 中是否已經知道異形跟上來、為什麼能安全完成脫離、事件 16 的失敗是否會讓事件 17 直接消失。

如果事件 16 完成後異形才被發現，這是很好的反轉；如果玩家此前已經在事件 15 知道異形就在身後，則事件 17 應該設計成短暫喘息後的第二次恐怖，而不是重新揭露已知資訊。

### 6.7 部分結局的狀態條件目前沒有資料來源

例如「樣本被保留」「體內已經寄生」「自身未被寄生」「救出 Ripley／Lambert」「攜帶資深者遺物」等條件，在文字中都有，但目前沒有對應的正式狀態欄位。它們需要轉成旗標，例如：

```text
sample_status = none | tissue | embryo | intact
infection_status = unknown | suspected | infected | cleared
npc_ripley_status = alive | injured | dead | rescued
npc_lambert_status = alive | injured | dead | rescued
luyuan_status = alive | dying | dead | rescued
```

否則結局矩陣只能由 AI 猜測，而不是由引擎確定。

---

## 七、我建議的補強優先順序

### 第一階段：先做「可以穩定玩完」的內容修正

這一階段不需要大量擴寫，只需要讓現有內容不互相打架。

1. 統一所有事件文字鍵，使用完整的 `eventId.approachId.resultTier` 格式。
2. 將事件 02 的 `"skill": "null"` 改成 `"skill": null`。
3. 確認所有事件引用的物品、NPC、線索 ID 都存在。
4. 把文字中提到的信任度、威脅度、物品獲得、物品損壞與傷勢轉成明確 `effects`。
5. 為 `sample_status`、`infection_status`、NPC 存活狀態與 `overload_started` 建立狀態旗標。
6. 明確定義 n1 至 n4 各需要哪些事件完成，避免玩家完成一個事件就跳過整個階段。

完成這些後，這份內容就能作為「固定事件資料」進行手動 GM 測試。

### 第二階段：補寫真正影響玩家感受的文字

不要先把 160 個缺失結果全部補滿。建議先補以下部分：

| 優先 | 內容 | 原因 |
|---|---|---|
| 最高 | 副本簡介與開場 | 建立玩家對任務、時間與異常的理解 |
| 最高 | 事件 01、03、06 | 教玩家調查、威脅與 NPC 試探 |
| 最高 | 事件 11、12、14 | 真相揭露、Ash 衝突與自毀倒數 |
| 最高 | 事件 17、18、19 | 決定最終戰是否有壓迫感 |
| 高 | 八個結局 | 讓玩家感覺選擇真的有意義 |
| 中 | 每個事件的 success／failure／critical_failure | 先支援主要分支 |
| 低 | 所有 minor_failure 與 narrow_success | 可在測試後依常用程度補齊 |

每個重大場景最好增加「進入、玩家行動後、危機升高、離開」四段文字，而不是只增加更多形容詞。這樣文字量會增加，但不會變成冗長小說。

### 第三階段：把事件卡接進引擎

建議先不要一次改完整 AI GM。第一版轉接層只需做到：

1. 根據目前節點和世界旗標篩選可用事件。
2. 根據玩家所在位置篩選事件。
3. 由 AI 將自由輸入轉成意圖與目標。
4. 由程式用事件卡判斷是否合法、需不需要物品與使用哪個檢定。
5. 由程式擲骰並產生結果 tier。
6. 由程式套用 effects。
7. 最後讓 AI 只把已確定的結果寫成敘事。

這會先保留目前的 AI 呼叫方式，但把「AI 自己決定規則」改成「AI 描述程式已裁定的結果」。

---

## 八、建議採用的事件結果最小格式

目前生成內容已經接近這個格式，可以再整理成以下資料模型：

```json
{
  "id": "evt_cargo_tool_scavenge",
  "title": "貨艙工具櫃與焊槍爭奪",
  "phase": "n2",
  "location": "loc_cargo",
  "truth": "工具櫃內有焊槍與高壓氣瓶；暴力開啟會造成噪音或爆燃。",
  "requires": {
    "all": ["evt_cargo_stalk"],
    "items": []
  },
  "approaches": [
    {
      "id": "pick",
      "intentTags": ["open", "quiet", "technical"],
      "requiresCheck": true,
      "check": {
        "attribute": "敏捷",
        "skill": "技藝",
        "difficulty": "普通"
      },
      "requiresItems": [],
      "results": {
        "success": {
          "narrativeId": "evt_cargo_tool_scavenge.pick.success",
          "effects": {
            "itemsAdd": ["item_blowtorch"]
          }
        },
        "failure": {
          "narrativeId": "evt_cargo_tool_scavenge.pick.failure",
          "effects": {
            "timeCost": 1,
            "approachDisabled": ["pick"]
          }
        },
        "critical_failure": {
          "narrativeId": "evt_cargo_tool_scavenge.pick.critical_failure",
          "effects": {
            "injuriesAdd": ["burn_minor"],
            "threatDelta": 1
          }
        }
      }
    }
  ]
}
```

其中最重要的是，`narrativeId` 和 `effects` 要分開。AI 讀取的是結果文字，程式處理的是效果；不要讓 AI 從自然語言自行猜測「獲得焊槍」或「信任度加三」。

---

## 九、關於「簡要選項」的建議

你原本擔心玩家看到選項後只比較數值，這個擔心是正確的。這份生成內容目前的 `availableApproaches` 還是偏傳統選項，因為它直接列出屬性、技能與難度。

未來前端可以把它們分成兩種顯示：

### 玩家可見的簡要行動提示

```text
- 安靜地觀察通風管的聲音
- 利用貨櫃與起重鏈設置陷阱
- 搜查 Brett 遺留的物品
- 你也可以自由描述其他做法
```

### 玩家展開後才看到的規則資訊

```text
使用：感知＋偵察
難度：普通
可能代價：花費時間、提高異形警覺
```

更進一步，可以不要在每個簡要選項旁邊顯示「成功率最高」的字樣，而只在玩家主動查看時顯示骰池。這樣數值系統仍然保留，但它不會把敘事選擇變成單純的最佳化題目。

事件卡中的預設方法仍然要保留，因為它們是玩家理解場景的提示；但自由輸入必須享有同等地位。界面上應該呈現「這些是你目前想到的方向」，而不是「只有這四個行動可以做」。

---

## 十、最終判斷：哪些可以直接用，哪些不能直接用

| 內容 | 判斷 | 建議 |
|---|---|---|
| 副本名稱、簡介、主線目標 | 可以用 | 小幅補充玩家視角與失敗代價 |
| 五階段主線骨架 | 可以用 | 保留，作為節點與事件的上層結構 |
| 地圖與地點 ID | 可以用作設計稿 | 需要新增正式 map schema 與位置狀態 |
| NPC 設定 | 可以用作劇本資料 | 補充位置、存活、知識、信任與行動規則 |
| 物品表 | 可以用作內容資料 | 統一 ID，補上持有、消耗、損壞與數量 |
| 線索表 | 可以用 | 補上發現旗標與玩家可見／主持真相分離 |
| 20 張事件卡 | 可以用作事件草稿 | 需要轉成程式可讀的事件 schema |
| 預設處理方法 | 可以用 | 前端只顯示簡要版本，規則放在展開資訊 |
| 事件 setup | 大多可以用 | 重大事件需要擴寫，普通事件可維持短句 |
| 結果文字 | 部分可以用 | 先修正 ID，再補缺少的結果層級 |
| 結局矩陣 | 可以用作設計稿 | 建立正式 state flags 後再接入結算 |
| 九宮格陣營路線 | 概念可用 | 先實作軸分數與信號累積，不要直接硬鎖路線 |
| 目前直接匯入現有引擎 | 不可以 | 必須先做事件轉接層與數項 P0 修正 |

---

## 十一、最後建議

這份內容**不需要推倒重做**。它的問題不是「劇情太差」或「AI 亂寫」，而是很典型的第一版生成內容問題：結構已經超前，程式資料模型還沒跟上；事件規則寫得比實際演出文字完整；多數後果存在於人類可讀文字中，尚未抽成機器可驗證狀態。

你覺得它的劇情描述偏短，判斷也是正確的，但目前最好的做法不是要求 Gemini 把每個事件全部寫成十倍長，而是採用以下順序：

> **先修正資料契約，再補重大場景，最後依測試紀錄補常用結果。**

建議下一輪讓 Gemini 先不要生成新的 20 張事件，而是專門補寫以下內容：

1. 開場與副本簡介的完整玩家版文字。
2. 事件 01、06、11、12、14、17、18、19 的進入與離場敘事。
3. 每個事件所有處理方法的 `success`、`failure`、`critical_failure` 三個核心結果。
4. 八個結局各自的完整前置條件、狀態結算與結局文字。
5. 每個事件的機器可讀 `effects`，不得只把效果藏在敘事句子裡。
6. 一份「自由行動裁定表」，說明玩家提出未列出行動時如何映射到現有事件與檢定。

完成這一輪後，這個副本就會從「很漂亮的生成劇本」進一步變成「可以真正拿來測試 AI GM 引擎的副本資料包」。

---

## 附錄：本次審查使用的資料

本報告主要對照以下內容：

- 使用者提供的《異形(一).md》至《異形(四).md》。
- 專案審查文件 `PROJECT_AUDIT_AND_GEMINI_SCENARIO_SPEC.md`。
- 目前副本資料結構 `content/scenario/schema.js`。
- 主回合流程 `functions/api/turn.js`。
- 選項契約 `content/turnOptions.js`。
- 自訂行動推導 `content/checkIntent.js`。
- 副本進度、威脅、扭轉度與結算模組。

本次統計結果為：四份內容合計 20 張事件卡、56 個預設處理方法、268 個已宣告結果文字鍵與 108 個已寫出演出文字鍵。統計以檔案中的 Markdown 與 JSON-like 內容為準，文字鍵的完整性仍應在正式轉換成 JavaScript／JSON 後再跑一次機器驗證。
