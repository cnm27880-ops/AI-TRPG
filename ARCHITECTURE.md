# 架構文件 —— 無限恐怖 TRPG AI 網站

寫給任何接手這個專案的人看(包括未來的你、未來的 Claude Code、或任何被丟進來的 AI)。
目的是讓接手者不用重新問一輪「這專案到底要幹嘛」，看完這份文件加上下面列出的其他文件，
應該就能無痛接續下去。

## 專案是什麼

把使用者手上一份 150 多萬字的《無限恐怖》同人 TRPG 規則書(`rules2.35.txt`，D10骰池點數購買制，
規模接近 GURPS/HERO System)，轉化成一個網站化的單人 AI TRPG。玩家的行動與骰子判定完全由程式碼
運算，AI 只負責讀運算結果去寫劇情(「AI 是主持人，不是計算機」)。

## 最高原則(不可違背，任何新模組都要遵守)

1. **AI 不做算術。** 骰子、成功數、生命值增減、經驗值花費、劇情獎勵數值——全部是程式碼算出來的。
   AI 收到的永遠是「已經算好的結果」，它的工作是把這個結果寫成一段有畫面的文字，不能自己編數字，
   也不能因為玩家的敘述文字而覆蓋掉骰子的結果(這是使用者明確提出的防呆需求：避免玩家用話術凹結果)。
2. **敘事語氣必須跟數值結果一致。** 骰子判定的 margin(成功數-DC)決定這次行動的「結果分級」
   (大成功/成功/驚險成功/失敗/慘烈失敗，見 `core/narration.js`)，AI 的敘事基調被這個分級鎖定，
   不可以無視分級亂寫「你輕鬆獲勝」或「你慘遭滑鐵盧」。
3. **規則書內容 vs 自製設計，必須清楚標記出處，不能混為一談。** 這個專案裡的每個模組頭部註解都要寫明：
   - `[規則書]`：直接對應 `rules2.35.txt` 某一頁的內容，必須有測試比對書中範例或公式(見 `TEST_PLAN.md`)。
   - `[設計]`：規則書沒有寫、由我們自己設計補上的機制(例如劇情扭轉度系統)，允許之後依實際測試調整數值，
     但同樣要有測試鎖定「目前這個版本的行為」，不可以無測試地隨便改動。
   這個區分很重要，因為使用者手上的規則書是「聖經」，我們不能悄悄用自己編的規則覆蓋掉書上寫的東西，
   也不能把自己設計的內容誤植成「規則書就是這樣寫的」。
4. **數值系統一律要有嚴格公式，不交給 AI 臨場發揮。** 包含使用者要求的「劇情扭轉度」獎勵/難度換算、
   「RP精彩度」獎勵——這些聽起來像是主觀判斷，但實作原則是：AI 只負責在一個**有限選項的分級量表**裡
   挑一個分級(例如「這回合的角色扮演精彩度：普通/出色/優異」)，分級對應的實際數值換算，一律是程式碼
   查表算出來，不讓 AI 自己決定「這次給幾點」。

## 模組地圖

```
core/                  純運算引擎，不含任何 AI 呼叫，Node/瀏覽器都能跑
  schema.js            [規則書] 九維屬性、技能表、空白人物卡骨架
  dice.js              [規則書] 骰池判定(含機運骰、傳奇屬性/技能附加成功)
  health.js            [規則書] 生命值傷勢引擎(B/L/A四階層疊轉換)，2026-08-17 補上 healDamage()
                        (指定軌、指定點數直接回完好——主神療傷與治療類商品都是這個語意)
  xp.js                [規則書] 建卡期經驗值花費公式
  check.js             [規則書] 組裝一次判定(屬性+技能+專業+傳奇加值)
  campaignXp.js         [規則書] 跑團後經驗值發放(固定/動態分配，含RP精彩度獎勵)
  narration.js          [設計]  骰子margin -> 敘事結果分級契約
  deathAndRevival.js    [規則書+設計簡化] 死亡判定與復活費用公式(見下方決策記錄)
  eventLog.js           [設計]  append-only事件日誌，「角色日誌回顧」的資料層
  legendaryAttributes.js [規則書] 九個屬性的傳奇效果總表(檢定附加成功/防御/傷害上限/先攻/XP紅利等)
  combat/
    turnOrder.js         [規則書] 先攻排序(1d10+先攻值)，同分時沉著->敏捷->玩家/隨機決定
    defense.js            [設計，2026-08-15取代規則書版] 單一防御DC=min(敏捷,感知)+技能補正
                           (格鬥/體魄取較高者)+裝備/天生防御，見「戰鬥數學簡化」決策記錄
    attackTypes.js         [規則書] 七種攻擊方式(肉搏/白刃/投擲x2/弓箭/槍械/炮)的DP公式表
                            (damageCap()公式已停用，保留當規則書參考記錄)
    attack.js               [設計，2026-08-15取代規則書版] resolveAttack命中判定核心
                             (原始成功數>防御DC才算命中 -> 總成功數-DC當基礎傷害，無上限)
    actionEconomy.js         [規則書] 自由/迅捷/移動/標準/整輪/全回合/反射/專注的額度追蹤與轉化鏈
    armor.js                  [設計，2026-08-15取代規則書版] 單一護甲值(Armor)傷害吸收，
                              取代原本物理/能量/精神/力場/毒素/墜落六大類的8步驟減免流程
    resolveCombatAction.js    [設計]  把attack.js+armor.js+health.js接成一次完整攻擊行動
  character.js           [設計]  戰鬥用角色檔案資料形狀(emptyCombatProfile：skillCorrection/
                          equipmentDefense/armor三個欄位)，橋接character屬性/技能跟combat/
                          模組要吃的參數，Phase 3裝備/血統資料進來前先給零值骨架
content/               即插即用內容包
  combat/                 [設計] 單敵人戰鬥遭遇的臨時佔位資料與狀態機
    placeholderEncounters.js  臨時武器(徒手/手槍)與測試敵人(掠奪者)資料，等型錄轉換工具
                               做出真實裝備/怪物資料後會被取代，見「戰鬥數學簡化」決策記錄
    encounterState.js          單敵人回合制狀態機：先攻排序、玩家/敵人交替攻擊、勝負判定
  packs/                各個 resource pack / scenario pack / contract pack 的 JSON 檔案
                        (含 d-tier-samples-*.json：7個資源分類各3個D級真實條目，見下方決策記錄；
                         以及 shop-starter-*.json：商店貨架9個包共36件商品，是同一批原文
                         **轉換後**的商品，跟 d-tier-samples-* 刻意分開存，因為轉換是有損的，
                         原文那份要留著核對)
  loader.js             pack 的清單驗證與載入邏輯(KNOWN_TYPES 目前含血統/改造/瞳術/稱號/流派/
                         技藝/法術/魔導書/修真/物品/載具/專長/副本/契約)
  templates.js           [規則書] 資源模板DCBA定價表
  affection.js           [設計] NPC好感度分級與判定加值對照表
  downState.js            [設計] 傷勢閘門——把 core/health.js 的 dead/unconscious 與
                          core/deathAndRevival.js 的復活規則接到實際流程上(/api/turn、
                          /api/combat/start、/api/revive)。2026-08-16 之前這兩個 core 模組
                          在整個 functions/ 與 content/ 底下一次都沒被引用過，玩家可以用
                          一張死掉的角色卡繼續玩，見下方「靜默失敗排查」決策記錄
  scenario/               [設計] 劇情節點圖、扭轉度系統、時間預算(社交vs主線資源競爭)
    threat.js             [設計] 迫近度軌(0~6)：判定成敗換算成會寫進存檔的世界狀態，
                          階段一變餵給AI的強制指令就整段換掉，見「成敗差異與開場體驗」決策記錄
    repetition.js         [設計] 套路遞減：同一個「屬性＋技能」連續用會加DC(第3次+1，封頂+3)，
                          換一招歸零。見「單一檢定通關與定向」決策記錄
    examples/alienNostromo.js [設計] 新手副本《異形》諾斯托羅莫號，附固定開頭(openingNarration)
  shop/                   [規則書＋設計] 主神商店。四個檔案的分工是刻意切開的：
    wallet.js             [規則書] 三種貨幣(支線DCBAS/獎勵點數/XP)與 "D+500" 價格標記法。
                           支線內部一律換算成D當量整數(3D=1C，書上明講可無損雙向拆組)，
                           順帶補上 core/deathAndRevival.js 檔頭寫的那筆「分數帳本」缺口
    effects.js            [設計] **商品效果的封閉詞彙表**——整個商店架構的核心限制。
                           14種效果各自寫明接到引擎的哪個欄位；轉不進詞彙表的型錄特性
                           必須進 droppedTraits 並寫明理由(理由取自 UNSUPPORTED_MECHANICS)
    catalog.js            [設計＋規則書] 商品結構驗證與購買流程：前提/模板能力五類排他/
                           依序購買DCBAS/專長按級學習/屬性點自由分配的合法性檢查
    pricing.js            [規則書] 公式定價的商品(屬性直購/技能直購/療傷/洗點/精神時光屋)，
                           這批是唯一不需要「簡化轉換」的商品，價格與效果逐字照抄
  contracts/              [設計骨架] 主神商店奴隸/員工契約，數值留白。跟 shop/ 的關係：
                          契約是商店的一種商品，之後接上時走 catalog.js 的同一套購買流程
  gemini/                 [設計骨架] Gemini敘事整合——AI敘事契約prompt組裝+REST API最小包裝，
                          沒有網路呼叫的部分(promptContract.js)有完整測試，client.js用假fetch測試
                          HTTP組裝邏輯，沒有實際打過Gemini的API(沒有金鑰/網路)，細節見
                          GEMINI_INTEGRATION.md
functions/api/         Cloudflare Pages Functions範例端點(check.js/combat/resolve.js/narrate.js)，
                        直接呼叫core/與content/gemini/的引擎，沒有實際部署測試過，細節見DEPLOYMENT.md
wrangler.toml          Cloudflare Pages設定骨架(nodejs_compat flag是必要的，見檔案內註解)
test/                  對應每個模組的測試，方法論見 TEST_PLAN.md
TEST_PLAN.md           規則條文 -> 程式碼 -> 驗證方式 -> 狀態 的對照表，隨專案一起長大
ARCHITECTURE.md        本檔案
RULES_DIGEST.md        規則精要單頁參考(數值/公式速查+模組反查索引)，不用翻 rules-2.35.txt 全文；
                        跟本檔案是互補關係——本檔案是「模組怎麼組出來的」，那份是「數值本身是多少」
CONVERSION_RULES.md    型錄條目 -> 商店商品的轉換規則(上半)＋目前的簡化規則總表(下半)。
                        要轉型錄的人先讀這份；它的執行版本是 test/shopPacks.test.js
RULES_TRIM.md          rules-2.35.txt 的精簡紀錄：刪了哪些已簡化的頁、內容去哪了、行號怎麼對
README.md              給新手的快速導覽(怎麼跑測試、目錄結構)
DEPLOYMENT.md           Cloudflare Pages部署步驟(給使用者，非接手AI專用)
GEMINI_INTEGRATION.md   Gemini API金鑰申請與串接步驟(給使用者，非接手AI專用)
```

## 目前狀態(Phase 進度)

- ✅ **Phase 1：核心規則引擎** —— 骰池、生命值、XP花費公式，多處用規則書原文範例或數學推導驗證過。
- ✅ **Phase 1.5：動態經驗值 + 敘事契約 + 內容包架構 + 劇情節點/扭轉度系統** —— 完成。
- ✅ **Phase 1.6：死亡復活、NPC好感度、劇情時間預算、契約骨架** —— 完成，細節見下方決策記錄。
- ✅ **Phase 2：戰鬥系統(引擎+接線)** —— 累計198個測試全過。先攻/行動順序、四層防御值組裝
  (基礎/閃避/洞察/格擋，含盔甲/天生防御)、七種攻擊方式(肉搏/白刃/投擲輕/投擲重/弓箭/槍械/炮)的
  DP與傷害上限公式、命中判定核心(`resolveAttack`)、九屬性傳奇效果總表、行動經濟(自由/迅捷/移動/
  標準/整輪/全回合/反射/專注)的額度追蹤與轉化鏈、傷害類型與減免系統(物理/能量/精神/力場/毒素/
  墜落六大類的8步驟減免流程)、**`resolveCombatAction()` 把以上全部接成一次完整攻擊行動(命中判定
  ->傷害減免->health.js扣血)**，`core/character.js` 提供戰鬥用角色資料的零值骨架讓引擎不用等
  Phase 3真實裝備/血統資料也能跑。**仍然不含任何怪物/武器的實際資料**——使用者明確要求這輪只做
  引擎，資料由使用者之後自己輸入。**仍然明確排除、之後才會處理的東西**：全力一擊/衝鋒攻擊等進階
  戰鬥動作、範圍攻擊(AoE)的特殊防御規則、混合傷害類型(一個傷害同時是兩種以上類型)的計算、不良
  狀態(暈眩/流血等)、載具戰鬥。這些不是被遺忘，是刻意排除，見下方決策記錄的詳細說明。
- ✅ **建卡點數預算驗證** —— `content/characterBuilder.js`(輕量化版，6屬性/10技能)。原始規則書的
  完整版曾實作於 `core/characterCreation.js` 並用書中「羅蘭」範例反向驗證過，但因為建卡 UI 一直
  只走輕量化版，為了只維護單一建卡路徑已移除，內容保留在 git 歷史，細節見下方決策記錄。
- 🔶 **Phase 3：資源型錄(500+頁血統/道具)實際轉成內容包** —— 剛起步，還不是大量鋪資料，而是先做了
  「7個已模板化的資源分類(血統/改造/瞳術/稱號/流派/技藝/法術)各3個D級真實條目」當測試用選項，
  細節見下方決策記錄。批量轉換工具本身、以及非典型定價結構的類別(魔導書/修真/載具)仍未開始。
- 🔶 **Phase 3.5：主神商店(貨幣＋效果詞彙表＋購買流程＋轉換商品)** —— 引擎層完成，
  `content/shop/` 四個模組加上**九個商品包共36件商品**(物品7/專長8/血統3/改造3/瞳術3/稱號3/
  流派3/技藝3/法術3；25件上架、11件掛名)，全部是型錄真實條目轉換而來，每件都有行號級的
  `sourceRef` 與「被丟掉的特性＋理由」紀錄。**還沒有 API 端點、沒有 UI、角色卡上也還沒有錢包欄位**，
  錢包目前是獨立物件由呼叫端保管。轉換規則與「目前的簡化規則總表」已成文，見 **`CONVERSION_RULES.md`**
  ——接手轉型錄的人先讀那份，不要從各模組註解拼湊。
- 🔶 **Phase 4：Cloudflare Pages 部署骨架、Gemini敘事整合骨架** —— 骨架已完成(設定檔/API端點/
  prompt契約/測試)，但**兩者都沒辦法在這個沙盒環境實際跑過**(沒有Cloudflare帳號、沒有Gemini金鑰、
  沒有對應網路存取)，使用者拿到後需要自己走一次 `DEPLOYMENT.md`/`GEMINI_INTEGRATION.md` 的步驟
  才能確認真的接得上，細節與風險見下方決策記錄。簡單前端UI仍未開始，使用者明確表示UI可以最後做。

## 給接手者的重要背景(使用者的決策脈絡，避免走回頭路)

- 使用者是**個人開發者**，用 Claude 做規則分析與程式撰寫，用 Gemini 做正式上線後的敘事推進，
  部署平台選 **Cloudflare Pages**(跟他參考的第二個網站一樣)。
- 使用者明確要求**先不要碰 UI 美化**，也**先不要手動digitize整個500頁型錄**，優先把「核心規則骨架」
  打牢，型錄之後會用「模板 -> JSON」的方式批次/半自動處理，模板本身在 `rules2.35.txt` 的
  `撰寫規則.htm`(約第9898行開始)，內含血統/改造/瞳術/專長/稱號/流派/魔導書/修真/技藝/法術/物品/
  載具，各自的 DCBA 定價與撰寫格式，這是之後做「型錄轉換工具」時的第一手依據，不要重新用AI猜測格式。
- 使用者想要**內容包(plugin pack)**架構，血統/瞳術/道具各自成包，副本(劇情)也做成同樣的即插即用包，
  未來要接後台管理介面。這個念頭直接決定了 `content/` 目錄的設計方向：不要把資料寫死在程式碼裡，
  一律走「pack manifest + loader」的模式。
- 使用者要求的「劇情扭轉度」系統，是規則書沒有的全新設計：劇情跟原設定差越多，玩家獎勵越豐厚，
  但相對地「劇情阻力」(骰子難度)也越高。這個機制的數值公式目前是我方(AI協作者)提出的草案，
  標記為 `[設計]`，之後使用者實際玩過應該會想調整數字，屬於正常迭代，不用當成bug。
- 使用者原話提醒（要牢記）：「骰子數值一定要做好，AI除了看玩家的行動，也要看骰子做判斷，要是玩家說
  他很厲害，結果骰子失敗超徹底，那就尷尬了」——這是本文件第2條最高原則的直接來源，任何未來要動
  narration.js 或 check.js 輸出格式的人，都不能違反這條。
- **這是單人遊戲，不是原規則預設的多人團戰**，而且使用者明確表示**至少現階段不做NPC隨行戰鬥**——
  NPC的作用是「培養好感度換增益」，不會加入戰鬥。技術上不需要改寫戰鬥引擎本身：先攻佇列/動作經濟
  本來就是「以角色為單位」設計的，天生支援1vN，拿掉的只是「隊友」這個資料結構，不是規則。**風險**：
  資源型錄裡有大量寫死「隊友」觸發條件的技能效果(增益隊友/隊友死亡觸發等)，之後把型錄轉JSON時，
  每個資源要標記 `requiresAlly: true/false`，單人模式下沒有合法目標的技能先視為不可用，等以後
  真的做出「契約NPC隨行戰鬥」才會生效——不能假裝這個問題不存在。
- **NPC好感度系統(`content/affection.js`)**：規則書裡「好感度」這個詞散落各處，但沒有統一的
  「好感度->增益」換算表，這是全新設計([設計])。目前是我方出的**草案分級表**(敵對/冷漠/中立/友好/
  親切/崇敬，對應判定加值-2~+3)，使用者要求先出草案、之後再調整數字——**這組數字還沒有經過使用者
  最終確認，之後如果要改，改 `AFFECTION_TIERS` 常數就好，不要讓AI臨場給buff**。
- **主線與NPC好感度的資源競爭模型**：使用者選定「劇情任務都有時間限制，推進回合會減少時間」，
  於是設計了 `content/scenario/timeBudget.js`：章節/任務開局設定一筆時間預算(單位是抽象「回合」)，
  推進主線節點與培養NPC好感度**共用同一筆倒數計時**，逾時會觸發劣化結局(`onExpireNodeId`，機制已留
  欄位但實際的「劣化結局長什麼樣」還沒設計，需要之後配合實際副本包內容一起做)。
- **死亡與復活(`core/deathAndRevival.js`)**：規則書「特殊兌換」(主神修復.htm，第7743~7743行)
  其實已經有完整的復活機制，不是我們發明的：每個角色一輩子有一次透過主神復活的保證機會，第二次死亡
  要靠特定劇情道具/劇情效果才能復活，兩種方式各限用一次(一輩子最多復活2次)，費用=角色身上未花分數+
  已花分數總和(書中範例：D+1000未花+D+600血統+3000屬性=DD+4600)。**使用者明確決定拿掉「第二次復活
  需要特定劇情道具」這個門檻**(理由：道具是每部影片/劇本專屬的稀有物品，這個引擎還沒有做劇本限定
  道具的資料結構，硬性要求會卡住)，改成**兩次復活都直接照同一個公式收費**，付得起就能復活，用完
  2次額度後第三次死亡就是真死，需要重新創角——這正好對應使用者原本想要的「湊不出分數就真的死了」。
  **這裡仍有一個已知的資料落差**：復活費用公式需要「角色身上未花分數/已花分數明細」的帳本，但目前
  `core/schema.js` 的 `emptyCharacter()` 只有經驗值(XP)帳本，還沒有建卡分數(支線點數)帳本。
  `content/characterBuilder.js`(建卡點數預算驗證)驗證的是「建卡當下花費合不合法」，
  不等於「角色身上的分數帳本」——這兩者是不同的資料結構，帳本還沒做，所以 `deathAndRevival.js`
  的函式仍然故意不直接吃 character 物件，等帳本做出來後才能真正接起來。
- **主神商店契約(`content/contracts/`)**：使用者要的「把好感度夠的NPC帶出世界」機制，規則書搜尋
  不到對應章節(用「主神商店」「奴隸契約」「員工契約」查過，查無結果，如實回報未杜撰)，使用者決定
  **現在先搭資料骨架，數值之後補**——`validateContractPack` 只驗證結構(契約類型/所需好感度分級/
  price欄位必須存在)，不驗證任何具體數字，`example-contract-placeholder.json` 裡的 price 全部是
  `null`，看到這個檔案不要誤以為是最終數值。
- **角色日誌回顧(`core/eventLog.js`)**：不是新規則，是資料架構補完——把判定/傷害/XP/節點完成/
  死亡復活/好感度變化/時間預算消耗全部寫進一個 append-only 事件日誌，日誌回顧功能只是把這份日誌
  攤開顯示。之後每加一個新的數值系統，記得也呼叫 `appendEvent` 掛進來，不要自己另外存一份。

## 戰鬥系統(Phase 2)的決策記錄

- **命中判定是兩層機制，不是單層margin比較**：讀「戰！戰！戰！.htm」才發現的重要結構——
  目標的「防御值」(`defense.total`，基礎+閃避+洞察+格擋+盔甲+天生防御的總和)直接從攻擊方的DP
  扣掉，骰池變小之後才擲骰；命中與否是拿攻擊方「擲出來的原始成功數」跟目標的「防御附加成功數」
  (`defense.bonusSuccesses`，主要來自傳奇敏捷)比較，原始成功數要大於這個數才算命中(書中原文第
  13687行)。命中後，總成功數(原始+攻擊方自己的附加成功)才是傷害，但有傷害上限公式封頂。
  這跟 `core/check.js` 的技能檢定(骰池不變、事後跟DC比較總成功數)是完全不同的形狀，**不要
  誤用 `resolveCheck` 去處理攻擊判定，要用 `core/combat/attack.js` 的 `resolveAttack`**。
- **使用者明確要求「一次做完四層防御(基礎/閃避/洞察/格擋)」且「遠程跟近戰一起做」**——這是本輪
  跟 Phase 1.6 不同的地方：Phase 1.6 系列(好感度/時間預算等)使用者選的是保守的最小切片，但戰鬥
  這裡使用者選了完整範圍，所以本輪把七種攻擊方式(肉搏/白刃/投擲輕/投擲重/弓箭/槍械/炮)跟四層
  防御都做了，不是只做「垂直切片」。
- **使用者要求「先只做引擎本身，數據留待你之後自己輸入」**：所以 `defense.js`/`attackTypes.js`/
  `attack.js` 全部只吃「已經算好的零件數字」(屬性值、技能等級、武器傷害值……)當參數，不假設
  character物件長什麼樣，也沒有內建任何示範怪物/武器資料。要玩起來，呼叫端(未來的遊戲迴圈)要
  自己把這些數字準備好——這些數字大部分要等 Phase 3(資源型錄轉JSON)做出來才有真正的來源。
- **[已修正] `洞察防御`當初是推測，使用者後來提供「傳奇感知」完整原文後確認公式本身是對的，
  但抓出一個真的漏算的bug**：建卡頁角色卡欄位公式「洞察防御=傳奇感知(0)=0」格式跟「閃避防御=
  傳奇敏捷(0)=0」對稱，當初合理推測洞察防御=`legendaryAttributeBonus(感知)`，這部分是對的。
  但使用者提供的「傳奇感知」完整原文顯示：傳奇感知同時貢獻「洞察防御」跟「防御附加成功」
  (跟傳奇敏捷同時貢獻「閃避防御」跟「防御附加成功」的結構完全對稱)，而 `defense.js` 原本的
  `bonusSuccesses` 只算了傳奇敏捷(dodge)的貢獻，漏了傳奇感知(insight)——**這是一個真的算錯的
  bug，不是數值草案問題**，已修正(`bonusSuccesses = dodge + insight + extraBonusSuccesses`)，
  測試也補上了涵蓋兩個來源的案例。完整的九個屬性傳奇效果原文現在整理進了
  `core/legendaryAttributes.js`，之後要查任何傳奇屬性效果直接查那份檔案，不用重新找書。
  「全力防御=基礎防御(雙倍)」的觸發時機仍然只找到角色卡欄位公式，還沒有另外驗證，維持原本的
  「可信度中」標註。
- **本輪(第三輪)新增：行動經濟狀態追蹤 + 傷害類型與減免系統**，回應使用者「還沒做的區塊接著做」
  的指示。`core/combat/actionEconomy.js` 把自由/迅捷/移動/標準/整輪/全回合動作的額度與轉化鏈
  (標準->移動->迅捷單向轉化)做成不可變的budget物件，含反射動作(使用後下一輪失去迅捷)跟專注
  (一次只能維持一個)。`core/combat/damageTypes.js` 把書中「傷害類型與防御類型.htm」的8步驟
  減免流程(免疫->忽略->硬度->吸收->轉化->抗力/減免->抵消)做成 `applyDamageMitigation()`，
  六大傷害類別(物理/能量/精神/力場/毒素/墜落)各自適用哪些減免手段都逐條對照書中原文整理成表
  (`DAMAGE_CATEGORY_RULES`)——**這一步是連接 `attack.js` 算出的傷害點數跟 `health.js` 的B/L/A
  傷害系統的中間層，之前這兩個系統實際上還沒接起來，現在有了正確的減免邏輯，但實際「接線」
  (呼叫端把 resolveAttack 的傷害結果、依角色的減免資料跑過 applyDamageMitigation、再送進
  applyDamage)還沒做，因為還缺角色資料模型(Phase 3)。**
- **傷害類型系統的已知簡化(刻意，不是遺漏)**：
  1. 混合傷害類型(一個傷害同時是兩種以上類型)不支援——書中三個範例(第5116~5116行)彼此對照
     解讀不完全一致，沒辦法在不猜測的情況下歸納出一條乾淨規則，這裡沒有硬做，需要之後跟你
     確認正確讀法。
  2. 硬度對能量傷害「作用於物體只造成一半傷害」的規則不實作，因為那主要影響對物品/環境的傷害，
     不是生物vs生物的戰鬥場景；但「哪些能量子類型(雷電/腐蝕/音波/光能)對生物也無視硬度」這條
     有實作，因為這條直接影響戰鬥。
  3. DR/能量抗力的「特定弱點來源比對」(例如DR/魔法要比對武器有沒有【魔法】特性)不內建，
     呼叫端要自己判斷這次攻擊符不符合弱點條件，再把算好的單一數字傳進來。
  4. 傷害轉化只提供掛勾點(呼叫端傳一個轉化函式)，不內建任何具體轉化能力(例如【防彈】)，
     這些屬於資源/技能資料，還沒做。
  5. 墜落/墜落物傷害「怎麼算出基礎傷害點數」(3米起算等)不在這裡，只做了「怎麼被減免」。
- **仍然明確排除在外**：
  1. 全力一擊(全力攻擊，用基礎防御換傷害加值)、衝鋒攻擊(用移動距離換DP)——進階戰鬥動作，
     book原文有(第4470~4470行)，屬於錦上添花的戰術選項，不影響核心迴圈，留到之後有餘力再做。
  2. 範圍攻擊(AoE)的特殊防御規則——書中原文(第4470行)寫範圍傷害要排除基礎/閃避/洞察/格擋防御，
     换算成對抗檢定的減值，跟單體攻擊的處理方式不一樣，`resolveAttack` 目前只處理單體攻擊。
  3. 不良狀態(暈眩/流血等)、載具戰鬥——都還沒開始讀對應章節。
- **[修正先前判斷] 角色資料模型+戰鬥接線其實不需要等Phase 3**：先前(上一輪)判斷「戰鬥引擎的實際
  接線需要角色資料模型，Phase 3才有」，這個判斷過度保守了——實際動手才發現，`resolveCombatAction()`
  要接線只需要角色資料「有這些欄位」(`core/character.js` 的 `emptyCombatProfile()`：格擋/盔甲/
  天生防御/傷害減免相關欄位，全部預設0/空)，不需要「欄位裡塞滿真實裝備/血統資料」——那才是真的要
  等Phase 3。現在 `resolveCombatAction()` 已經可以用「空手空防具」的角色跑完整套攻擊流程(命中判定
  ->傷害減免->扣血)，之後Phase 3把真實資料填進 `combatProfile` 的欄位就好，不需要改動任何
  `core/combat/` 的邏輯。8個測試(`test/resolveCombatAction.test.js`)涵蓋：命中扣血、未命中hpState
  原封不動回傳(同一物件參照)、物理DR/硬度在送進health.js前先扣、免疫直接歸零、傷害上限+額外加成
  的封頂、遠程距離減值反映在effectiveDP上、全力防御提高防御方有效DP扣減量、真骰子端到端smoke test。
  傳奇屬性帶來的加值(攻擊附加成功/傷害上限加成)**不會自動套用**，呼叫端要自己用
  `core/legendaryAttributes.js` 算好再傳進 `attackBonusSuccesses`/`extraDamageCap` 參數。

## 戰鬥數學簡化(2026-08-15)的決策記錄

- **使用者明確決定廢除書中「基礎/閃避/洞察/格擋」四層防御拆分與「免疫->忽略->硬度->吸收->
  轉化->抗力/減免->抵消」八步驟傷害減免，改為單一DC+單一Armor值**：這是本專案第一次主動
  偏離「規則書內容 vs 自製設計必須清楚標記出處」原則裡「規則書=聖經」的預設立場——不是
  誤用或簡化失真，是使用者親自拍板的房規調整，且明講「之後我們會逐步將規則書轉化為適合我們
  網站的內容」，也就是說接下來的規則調整不再要求逐字對應 `rules-2.35.txt`，改成「書為靈感，
  網站體驗優先」。舊版四層防御(`defense.js`)與八步驟減免(`damageTypes.js`)的完整規則書出處
  (rules-2.35.txt 第5116~5116行等)保留在 git 歷史紀錄裡，不是被遺忘，未來若要找回可以直接
  revert 相關commit。
- **新公式**：`防御DC = min(敏捷,感知) + 技能補正(格鬥/體魄取較高者) + 裝備/天生防御`；
  攻擊方原始成功數要嚴格大於DC才算命中；`基礎傷害 = 總成功數(原始+附加成功) - 防御DC`，
  不再有武器別傷害上限；`實質傷害 = max(0, 基礎傷害 - 護甲值)`，不分傷害類型。
  完整版見 `RULES_DIGEST.md` 第9節。
- **改動的檔案**：`core/combat/defense.js`(改寫)、`core/combat/damageTypes.js`(刪除，
  邏輯搬到新增的 `core/combat/armor.js`)、`core/combat/attack.js`(命中判定改成DC比較，
  移除防御扣DP機制)、`core/combat/resolveCombatAction.js`(接線跟著簡化)、
  `core/character.js` 的 `emptyCombatProfile()`(從十幾個欄位簡化成三個：
  `skillCorrection`/`equipmentDefense`/`armor`)。`core/combat/attackTypes.js` 的
  `dp()` 公式（決定攻擊骰池大小）沒有變，只有 `damageCap()` 停用（保留函式本體當規則書
  參考記錄，沒有呼叫端在用）；`core/legendaryAttributes.js` 的 `力量.damageCapBonus`
  同理變成沒有掛勾點的紀錄資料，等以後若恢復傷害上限機制可以復用。
- **測試**：`test/defense.test.js`/`test/attack.test.js`/`test/resolveCombatAction.test.js`
  全部改寫成新公式；`test/damageTypes.test.js` 刪除，改成 `test/armor.test.js`。
- **順帶完成的單敵人戰鬥MVP**：`content/combat/placeholderEncounters.js`(臨時佔位武器/
  敵人資料，型錄裡的血統/瞳術等資源目前是自由文字描述如「骨爪：3L天生武器」，還沒有結構化
  成 `attackTypes.js` 可以直接吃的欄位，所以先用兩把陽春武器接通引擎到UI這條路)、
  `content/combat/encounterState.js`(單敵人回合制狀態機：先攻排序、玩家/敵人交替攻擊、
  勝負判定，敵人固定用配備的唯一武器攻擊，不呼叫AI)、`functions/api/combat/start.js`+
  `act.js`(session整合，血量同步回`character.derived.hp`)、`public/index.html`+`app.js`
  的戰鬥面板(重用既有的d20擲骰動畫與HUD視覺語言，遵守既有的`style.display`切換慣例而非
  Tailwind `.hidden`)。只支援一對一，多敵人/隊友留到有實際需求再做。

## 建卡點數預算驗證的決策記錄

> **[歷史記錄]** 以下記載的是原始規則書完整建卡系統的推導與驗證過程，實作曾位於
> `core/characterCreation.js`。因為建卡 UI 一直只走 `content/characterBuilder.js` 的輕量化版本，
> 為了只維護單一建卡路徑，該模組已移除（程式碼保留在 git 歷史）。這段記錄留著是因為
> 「羅蘭範例反推出的公式」本身是對規則書的查證結果，之後若要做進階建卡模式仍然適用。

- **用書中「羅蘭」完整建卡範例(建卡.htm第668~668行)當ground truth，而不是憑空推導公式**：
  這個範例逐段列出屬性/技能/專業/專長的實際分配過程，手算驗證：屬性類別預算{生理3/心智2/互動1}
  +自由3點=9點，羅蘭最終屬性(力量3/敏捷1/耐力4/智力1/感知1/決心4/風度1/操控1/沉著2)換算成花費
  剛好是9(其中4→5那一級要花2點的「傳奇突破」加價規則，也是從這個範例反推確認的)；技能類別預算
  {生理6/互動5/心智4}+自由5點=20點，羅蘭建卡當下的技能(運動3/肉搏3/求生2/白刃3/調查3/神秘學1/
  感受1/表達1/交際3)換算花費剛好是20；3個免費專業(運動掛游泳+輕投擲武器、白刃掛重劍)剛好用完；
  專長5點(死硬4級+特殊身份1級)剛好用完，語言專長額度=智力×2=1×2=2點(英語2級)剛好用完。
  九個數字全部精確吻合，代表推導出的公式(`attributeChargenCost`/`skillChargenCost`等)是對的。
- **[已知簡化] 類別預算只驗證總量，不逐類別強制查核**：屬性/技能的「類別預算」({3,2,1}/{6,5,4})
  加上「自由點數」共用一個池子，自由點數可以花在任何分類，光看最終數值沒辦法反推「這一點到底是
  從哪個預算池出的」，勉強拆分只是憑空assume，違反「嚴禁杜撰」原則。`validateAttributeBudget`/
  `validateSkillBudget` 只驗證：(a) 類別預算的分配是不是{3,2,1}/{6,5,4}的合法排列，(b) 總花費
  沒有超過「類別預算總和+自由點數」的總盤子。如果之後要逐類別強制查核，需要建卡介面額外記錄
  「這一點花在哪個預算池」，不是能從最終數值反推的。
- **[已知限制] 不處理專長對技能上限的異動**：羅蘭範例的最終角色卡顯示肉搏2/白刃4(不是建卡當下的
  肉搏3/白刃3)，因為「特殊身份」專長把白刃上限從3提高到4，玩家重新分配了1點——這是「建卡預算花完
  之後」的專長效果，不是`skillChargenCost()`這層要處理的東西。`validateSkillBudget`驗證的測試案例
  刻意用「專長調整前的快照」(肉搏3/白刃3)，呼叫端如果要驗證含專長調整後的最終技能等級，要自己先
  扣掉/還原專長帶來的上限異動。

## 資源型錄D級範例(Phase 3起步)的決策記錄

- **這是「摘錄真實條目」，不是「自己編homebrew內容」**：使用者要求「每個資源分類各轉換三個D級的
  出來」，這裡的「轉換」理解為從 `rules2.35.txt` 的500+頁資源型錄裡實際找出真實條目摘錄轉JSON，
  跟這個引擎一路的「不杜撰」原則一致——不是我方自己設計3個新的D級血統/瞳術範例湊數。7個已模板化
  的資源分類(血統/改造/瞳術/稱號/流派/技藝/法術，見`content/templates.js`)各挑3個，共21個真實條目，
  分別存成 `content/packs/d-tier-samples-*.json`，每個entry都有精確到行號的`sourceRef`可回頭核對。
  魔導書/修真/物品/載具/專長這5類還沒模板化(見`templates.js`的註解)，這輪沒有處理，不是遺漏。
- **搜尋方法**：先用行號定位每個資源分類在原始檔案裡的起訖範圍(用檔案裡各分類的「XX.htm」
  標頭行定位)，再分別針對每個分類的範圍內搜尋「D級」關鍵字定位候選條目，讀取上下文抓出完整寫法，
  最後回頭用grep核對摘錄內容跟行號完全對得上原始檔案，才收進pack。这個過程對每個分類是獨立進行的，
  互不影響彼此的判斷。
- **[真實抓到的落差，非杜撰] 3組定價落差**：跟金剛狼血統範例(當初就抓到D級500 vs 模板600不符)
  同樣性質，這次又抓到3組——**改造類**3個D級範例書中原文全部是`D+1000`，但改造模板規定D級應為500；
  **技藝類**「太玄鑲華劍譜」書中`D+600`，模板規定500(同技藝類另外2個範例「葵花寶典第一重」
  「饕餮盛宴」都確實是500，符合模板)；**法術類**「阿尼馬格斯」書中`D+1000`，模板規定500(同法術類
  另外2個範例「變身術」「暗示術」都是500，符合模板)。血統/瞳術/稱號/流派這4類抽到的範例價格則都
  跟模板一致。這些落差**保留原始數字，不自行覆蓋成模板規定的數字**，用`test/resourcePacks.test.js`
  裡的`auditAgainstTemplate`測試鎖定「這些落差是已知且被測試覆蓋的事實，不是bug」——這進一步印證
  使用者提醒過的「舊資源亂寫」現象在多個分類都存在，不只金剛狼一個特例。
- **[已知限制] 瞳術類的「無需從D級開始撰寫」規則被實際驗證**：`瞳術模板`(`DOJUTSU_TEMPLATE`)的
  註解本來就寫「無需從D級開始撰寫」，這次逐一查過瞳術目錄裡全部31個條目後證實：21個從D級開始、
  5個從C級開始(妖精眼/黑與白之瞳/幻視瞳/瞳人語/遠與近之瞳)、2個從B級開始(殲滅眼/火眼金睛)、
  3個是無分級的一次性瞳術。這個包只收錄D級起跳的3個(寫輪眼/審判眼/毀滅之眼)當範例，之後如果要
  幫這些「不從D開始」的瞳術建模，要另外設計「起始等級」欄位，這裡的pack結構還沒處理這件事。
- **[已知限制] 部分條目的欄位不完整是書本原文的問題，不是轉換時漏抄**：例如「京都神鳴流」D級段落
  寫「特性：劍道精修(四特性)」但底下只實際展開了1個子特性(「劍術入門」)，另外3個沒有寫出來；
  「太玄鑲華劍譜」的D級只有1個特性(模板寫baseTraitsPerRank通常概念上是3個)。這些如實保留，
  在對應pack的`note`欄位裡寫清楚，不是幫書「補完」缺漏的部分。
- **這些資料还没有被`resolveCombatAction`/建卡流程實際使用**：目前只是靜態的pack資料，
  搭配`content/loader.js`的`mergePacks`/`auditAgainstTemplate`可以載入跟稽核，但還沒有UI或遊戲迴圈
  真的把這些條目的效果套用到角色身上(例如金剛狼的骨爪要真的變成攻擊方式、阿蘭斯的4點屬性要真的
  加進角色屬性)——這是之後接前端/遊戲迴圈時才要做的事，這裡只做到「有真實資料可以載入跟選」。

## 部署骨架(Cloudflare Pages + Gemini)的決策記錄

- **這兩塊都是「骨架」，不是「已驗證可用的功能」**：這個開發環境沒有Cloudflare帳號、沒有Gemini
  API金鑰、也沒有網路能連到這兩個服務，所以 `wrangler.toml`/`functions/api/*.js`/
  `content/gemini/client.js` 全部是照官方文件寫的，邏輯上經過單元測試(用假的fetchFn依賴注入驗證
  HTTP請求組裝對不對)，但**沒有實際打過真正的Cloudflare/Gemini服務**。使用者拿到後必須自己走一次
  `DEPLOYMENT.md`/`GEMINI_INTEGRATION.md` 的步驟，才能確認「引擎邏輯」跟「真的部署上去能不能動」
  這兩件事都成立——這是誠實的風險揭露，不是藉口。
- **`core/dice.js` 用 `node:crypto` 的 `randomInt()`，Cloudflare Pages/Workers環境要開
  `nodejs_compat` flag才能跑**：這不是小細節，沒開這個flag，任何呼叫到擲骰的API端點會直接丟例外
  (不是算錯，是整個函式炸掉)，`wrangler.toml` 已經預先開好，但使用者如果之後自己改設定檔要記得
  保留這一行。
- **Gemini API的端點/模型名稱標記「可信度：中」**：查證2026年當下的官方文件時發現Google已經把
  `generateContent`(這個骨架用的端點)標成「Legacy」，新的建議介面是「Interactions API」，舊端點
  查證當下仍可用，但這是會變動的資訊。`content/gemini/client.js` 的檔頭註解跟 `GEMINI_INTEGRATION.md`
  都已經明講這個不確定性，使用者部署前務必重新查一次官方文件確認端點/模型名稱沒有變。這是遵守
  「查不到的訊息/會變動的訊息不能杜撰成確定事實」原則的具體做法，不是敷衍。
- **AI敘事契約寫死在 `content/gemini/promptContract.js` 的 `SYSTEM_INSTRUCTION`**：直接對應
  這個專案最早訂下的最高原則第1、2條(AI不做算術、敘事語氣必須跟數值結果一致)，明確告訴Gemini
  「你收到的數字是最終結果，不能質疑、不能重算、不能因為玩家話術改變基調」，並且要求玩家嘗試
  誘導AI忽略規則時要禮貌拒絕。`buildTurnPrompt()` 把 `core/narration.js` 算出的分級指令跟
  (可選的)`core/eventLog.js` 事件摘要組進使用者訊息，這一層完全不含網路呼叫，8個測試全部可以
  離線驗證組裝邏輯本身，跟「打不打得通Gemini」這件事分開驗證。

## 前端UI接線(Phase 5)的決策記錄

前端UI是另外請一個AI獨立做的，跟這個引擎不是同一輪開發，接回來時兩邊的假設對不上，
以下是把它們接起來時做的決定與當時的理由：

- **前端做的是D&D式假設，不是這套規則**：接回來的UI原本用 `{STR:14, DEX:16}` 這種英文縮寫+
  10~18的數值、`hp:{current,max}` 單一血條、以及 `calcMod()` 產生的 `+2/+3` 修正值。
  這三個都是D&D概念，這套規則沒有：屬性是**中文九維**、建卡值域**1~5**、屬性帶來的加值是
  **傳奇屬性附加成功** `floor((值-1)/5)`、生命是**四段傷勢軌**(完好/沖擊B/嚴重L/惡性A)且總量守恆。
  已全部改成照 `core/schema.js` 的形狀，否則 `performCheck()` 收到英文屬性名會直接丟
  「人物卡沒有屬性undefined」。
- **前端原本在API失敗時用 `Math.random()` 生一個假的成功結果**，骰子動畫裡也寫死一個「7」。
  這直接違反最高原則第1條——畫面上會出現沒有經過引擎的數字，而且玩家跟開發者都分不出
  「引擎算出成功」跟「後端根本沒接上」。已改成明確顯示 `SYSTEM.ERROR` 區塊與 `ENGINE OFFLINE` 標記，
  **寧可讓玩家看到壞掉，也不能讓他看到假的骰子結果**。
- **「這個行動該擲什麼檢定」放在引擎層，不放前端**(`content/checkIntent.js`)：前端第一版是自己
  用關鍵字表挑屬性/技能的。挑錯技能會直接影響成功率(甚至觸發技能0的自動失敗)，那是規則決定，
  依原則第1條必須待在測得到的地方。現在 `/api/check` 與 `/api/narrate` 都接受只給 `playerAction`，
  由後端推導；呼叫端仍可用 `params`/`checkParams` 明確指定(之後戰鬥或劇本節點會用到)。
  **關鍵字比對只是暫時方案**，正解是多一次AI呼叫讓Gemini在有限選項裡挑檢定(符合原則第4條)，
  細節見該模組檔頭。
- **部署目標只有一個：Cloudflare Pages**。接回來時repo裡同時有 `wrangler.toml`(輸出目錄`public/`)
  跟一個把整個repo根目錄丟去GitHub Pages的Jekyll workflow，而 `index.html` 躺在根目錄、
  前端JS躺在 `public/append.js` 且**從來沒有被任何HTML載入過**。已統一成 `public/index.html` +
  `public/app.js`；GitHub Pages workflow保留但降級成「只上傳public/的純靜態預覽」，因為
  GitHub Pages不能執行 `functions/`，在那上面所有 `/api/*` 一定404(畫面會顯示ENGINE OFFLINE)。
- **[未解決，需要你決定] UI依賴三個外部CDN**(Tailwind Play CDN / Font Awesome / Google Fonts)，
  而且**所有自訂CSS都寫在 `<style type="text/tailwindcss">` 裡**——這個block只有Tailwind Play CDN
  的JS載入後才會被處理。也就是說CDN一旦連不上，掉的不只是配色，是連 bottom sheet、卡片堆疊、
  骰子動畫的定位CSS全部消失，版面會整個垮掉(實測過：把CDN擋掉後抽屜會變成static定位跑到頁面外)。
  Tailwind官方也明講Play CDN不是給正式環境用的。要正式上線應該改成build-time的Tailwind，
  但那會引入建置步驟，屬於架構決定，留給使用者拍板。

## 多供應商AI敘事(Phase 5.1)的決策記錄

- **市面上的LLM API只有兩種線路格式，不是每家一種**：DeepSeek、OpenRouter、Groq、硅基流動、
  以及絕大多數第三方中轉都相容OpenAI的 `/chat/completions`；Gemini走自己的 `generateContent`。
  所以 `content/llm/client.js` 只實作兩份轉換程式碼，`providers.js` 只是一張設定表。
  **要多支援一家第三方接口通常不用寫程式**，設 `LLM_BASE_URL`/`LLM_MODEL`/`LLM_API_KEY` 即可。
- **預設供應商選 Cloudflare Workers AI，理由是「它不需要任何金鑰」**：使用者問能不能找一個
  網路上的公益API當預設，查證後的判斷是**不要**——那類免金鑰公共代理會無預警消失、
  相當一部分背後是來源可疑的金鑰池、而且玩家的所有輸入都會送到不知道留不留log的伺服器。
  Workers AI 用的是使用者自己Cloudflare帳號的免費額度(查證當下每天10,000 Neurons)，
  不經第三方，而且專案本來就部署在Cloudflare上，所以它是唯一正當的「零設定即可跑」選項。
  代價是小模型的中文敘事品質不如Gemini/DeepSeek，文件裡有寫明這個取捨。
- **`generateContent` 被標為legacy，但刻意不遷移**：先前的交接文件把這件事列為「可信度：中」
  的待查項，現在有答案了。Google已把Interactions API列為GA並建議新專案採用，但**明確聲明
  generateContent仍完整支援**，且對「單次、無狀態、低延遲」的呼叫(正好是本專案每回合敘事的形狀)
  官方建議就是繼續用它。遷移沒有實際好處，只有破壞既有測試的風險。之後若要用到需要狀態或
  工具呼叫的功能(例如讓AI自己查角色卡)，那才是該遷移的時機。
- **DeepSeek的模型名稱查證後與記憶不同**：查證當下官方列的是 `deepseek-v4-flash` /
  `deepseek-v4-pro`。這印證了本專案「會變動的資訊一律現查、不憑記憶」的原則——
  `providers.js` 每一筆都標了查證日期與出處連結，之後重新核對時不用重找。
- **OpenRouter刻意不給預設模型**：免費模型的 `:free` slug每週在變，寫死一個等於保證未來某天
  壞掉、而且錯誤訊息會很難懂。沒設 `LLM_MODEL` 時直接報錯並告訴使用者去哪裡挑。
- **系統提示拆成「規則契約層 + 文筆層」兩層**(`content/narrativeStyle.js`)：如果混在同一段文字裡，
  之後為了調整文筆去改那段提示，很容易不小心把「不能自己編數字」的語氣改弱，
  而且**不會馬上發現**——AI大部分時候仍會照做，只有玩家開始用話術凹的時候才破功。
  分層之後改文筆永遠碰不到規則，這是結構上的保證，不是靠人記得小心。
  組裝順序也是防線的一部分：文筆在前、規則契約在後、最後一句宣告優先序，
  `test/narrativeStyle.test.js` 用一段「試圖奪權的惡意文筆提示詞」鎖住這個保障。

## 主神商店(Phase 3.5)的架構決策記錄 —— 2026-08-17

### 一句話總結

商店不是一個「賣東西的介面」，是**型錄轉換的出海口**：500+頁資源要進遊戲，唯一的入口就是
「能不能變成一件玩家買得到、而且效果真的接到引擎上的商品」。所以這一輪做的其實是轉換管線，
商店只是它的使用者。

### 貨幣：三種，而且只有兩種能轉讓

規則書第419~464行(核心規則部分.htm「自我強化」)寫得很清楚：支線(DCBAS)、獎勵點數(又叫分數)、
經驗(XP)。價格標記法 `D+500` 的意思是「一個D支線 **加** 500分」，`DD+4600` 是兩個D加4600分。

實作上最關鍵的一個決定是：**支線內部一律存成「D當量」的整數**。書上明講支線可以無損雙向拆組
(3D=1C=1/3B…，「一個C可以被拆開來購買3個不同的D支線資源」)，所以「手上有幾個C」本身沒有資訊量。
存D當量之後，「1個C能不能買3件D級商品」這種問題不用寫任何特例，減法自己就答對了；只有要顯示給
玩家看的時候才貪心攤回面額。`test/shopWallet.test.js` 直接用書上這句話當測試案例。

順手補掉了一個既有缺口：`core/deathAndRevival.js` 的檔頭寫著「復活費用公式需要未花分數＋已花分數
明細，但 emptyCharacter() 只有XP帳本」，`content/downState.js` 因此用XP帳本代打。錢包的 `spentLedger`
正是那份明細，`buildRevivalWalletFromShopWallet()` 把兩邊接起來，並用書中的
「D+1000未花 + D+600血統 + 3000屬性 = DD+4600」範例驗證過。**但 downState.js 仍然走舊的XP代打路徑**
——把它換掉需要先決定「角色卡上要不要長出一個 wallet 欄位」，那是存檔格式的變更，這一輪沒有動。

### 效果詞彙表：整個架構的重點在這裡

型錄的每個條目，特性都是一段自由文字，而且寫的是**完整版規則的語言**：高速、破甲、格擋加值、
洞察加值、專業、能量池、技能樹、豁免、劇痛點數、體積、友方單位。這些詞在簡化後的引擎裡
**沒有欄位可以寫**(六維十技能、單一防御DC、單一護甲值、沒有專業、沒有不良狀態、單人無隊友)。

所以轉換不是搬字，是把每個特性逼到三條路的其中一條：

1. 落進 `EFFECT_KINDS` 的14種效果之一(每一種都寫明接到引擎哪個欄位)；
2. 降級成 `kind:"敘事"`，明確標記「這條沒有數值效果，只給AI當背景」；
3. 進 `droppedTraits`，寫明理由，理由取自 `UNSUPPORTED_MECHANICS` 的鍵。

**不允許第四條路**，尤其不允許「寫進JSON、看起來有效、實際上沒有任何程式碼會讀它」——那正是
下面「專業(specialization)系統：決定不做」那則記錄裡拆掉的東西。`test/shopCatalog.test.js` 為
每一個 kind 都寫了一則「它真的改變了某個引擎函式的輸出」的測試，這是這批測試唯一的重點。

**條件式效果的紅線**：書上大量效果帶情境條件(「位於酒館時」「對沒有盔甲的目標」「每天一次」)。
引擎判斷不了，照收就等於讓AI在敘事時決定加值有沒有生效——違反最高原則第4條。所以檢定類效果
**只接受 attribute/skill 這種引擎自己看得懂的匹配鍵**，一律常態生效；需要臨場判斷的，只有降級
或丟棄兩條路。實務上這造成了一次刻意的放寬(「處女座」的社交加值原本限女性佩戴，轉換後變成無條件)，
那條在商品的 `conversionNote` 裡寫明了是放寬，不是漏抄。

### 轉換率：這一輪最有價值的發現

三個包做完之後，數字很誠實：

| 分類 | 看過 | 上架 | 真正卡住的是什麼 |
| :--- | :--- | :--- | :--- |
| 物品(武器) | 7 | 7 | 沒有卡住。物品的核心價值是「武器傷害」與「防具數值」，簡化規則原封不動保留了這兩個欄位 |
| 個人專長 | 11 | 8 | 卡在專業/重擲/不良狀態/移動速度/意志力消費 |
| 模板能力(血統) | 4 | 1 | 卡在**暫時性增益容器**與**意志力消費**，這兩個機制引擎裡完全沒有 |

結論很具體：**模板能力類要能真的上架，缺的不是轉換工，是引擎裡的兩個機制**——一個能裝
「持續一個場景的buff」的容器，以及「花1點意志力換效果」的消費端(`character.derived.willpower`
目前只有數字，沒有任何地方會扣它)。血統的價值幾乎全寫在「變身/爆發/每場景一次」這種形態上，
沒有這兩個機制，血統就只剩屬性點。這條建議直接當作 Phase 3 後續的排程依據。

> **2026-08-17 後記：這條建議被執行了，而且結論成立。** 兩個機制都做在
> `content/shop/forms.js`（見本檔案「暫時性增益容器『型態』」那一節），做完之後
> Orphnoch 從掛名轉正、阿蘭斯把原本丟掉的「帝國子民」撿了回來，血統從 1/3 變 2/3。
> 上面「意志力目前沒有任何地方會扣它」這句話從那一輪起不再成立。

專長類還有一個結構性的坑值得記下來：規則書要求專長**必須逐級按順序學習**，所以中間某一級轉不出
效果時，不能把它藏起來——藏起來會讓上面那一級永遠買不到。做法是**照原價上架、效果只剩敘事**，
並在 `droppedTraits` 寫明整級被丟掉的理由。誠實而且不擋路。

### 順帶抓到並修掉的一個規則落差

做技能直購定價時對照原文，發現 `core/xp.js` 的 `skillRaiseCost()` 漏了下限：第686行原文是
「（當前技能等級-1）×2XP，**最低為1點**」，第456行另有更直白的「技能從1級提升到2級需要1XP」。
舊實作對 1→2 算出 0XP(免費升級)，而 `test/engine.test.js` 還用註解把 0 說成「規則書公式字面上如此」。
兩邊都已更正。影響範圍只有 1→2 這一級(4級以上 `(4-1)*2=6 > 1`，下限不作用)。

### 刻意還沒做的事(不是遺漏)

- **洗點的退貨邏輯**：`RESPEC_PRICE` 與商品資料都備好了(`owned.effects` 保留了完整的購買紀錄，
  包含當時的屬性點分配)，但「把買過的東西退回去」的實作沒寫。書上的洗點規則有一長串例外
  (經驗買的不退、送的物品要一起失去、額外XP要按400分/XP扣回、精神時光屋的花費不返還)，
  那是獨立一輪的工作。
- **商店的API端點與UI**：`functions/api/` 底下還沒有 shop 端點，貨架只有 `buildStorefront()`
  這個純函式。這跟專案一路的做法一致——引擎先做完、接線後做。
- **角色卡上的錢包欄位**：`emptyCharacter()` 還沒有 wallet。錢包目前是獨立物件，由呼叫端保管。
  要接上存檔就得動 `content/storage/sessionStore.js` 的格式，那是會影響既有存檔的變更。
- **契約(`content/contracts/`)接進商店**：契約本來就是商品的一種，資料骨架也在，但它的數值
  使用者還沒定，所以沒有寫進貨架。

## 型錄轉換規則成文化＋貨架補齊＋規則書精簡 —— 2026-08-17（第二輪）

使用者在第一輪商店做完之後追加了四件事，這裡記決策，細節在 `CONVERSION_RULES.md` 與 `RULES_TRIM.md`。

### 一、轉換規則寫成文件（`CONVERSION_RULES.md`）

第一輪的轉換規則散在各模組的檔頭註解與 pack 的 note 裡，接手的人要拼湊。現在集中成一份，
分成兩部分：上半部是**轉換規則**（每條標【硬性】或【判斷】，硬性的都有測試守住），
下半部是**目前的簡化規則總表**（角色數值／判定／戰鬥／成長與經濟／原創系統五類），
因為轉換的人真正需要知道的是「現在的規則長什麼樣，所以什麼接得上」。

`test/shopPacks.test.js` 是那份文件的執行版本：文件裡每一條「必須怎樣」都有一則測試。
兩者不一致時以測試為準，並回頭改文件。

### 二、第三種上架狀態：掛名

使用者的原話是「如果上架物品有D以後的能力(比如是一整套的)，就先放名字就好」。
這句話解掉了第一輪的一個死結：honest-first 的設計讓轉不出來的條目只能整個不收，
於是血統類貨架上只有 1 件，玩家會以為這個分類就只有那麼多。

現在有三態：`上架`（效果完整轉出，可購買）／`掛名`（只登錄名稱、價格、出處與
「還缺哪個機制」，**不可購買**）／不收（寫進 pack 的 `examinedButRejected`）。
掛名商品的 `pendingReason` 與 `originalTraits` 都是必填 —— 前者逼你講清楚缺什麼，
後者讓之後補效果的人不用回頭再翻一次規則書。

同一個機制也用在「一整套」的資源上：`higherRanks: [{rank, name}]` 只登錄後續級數的名字，
不帶效果也不帶價格。

### 三、貨架補到每類 3 件，且這一輪只到 D 級

七個模板化分類（血統／改造／瞳術／稱號／流派／技藝／法術）各 3 件，共 21 件，
加上第一輪的物品 7 件與專長 8 件，總共 36 件。有測試鎖定「每類剛好 3 件」與
「所有商品的 rank 只會是 D」。

補齊之後轉換率的樣貌更清楚了（完整表在 `CONVERSION_RULES.md` 第 12 節）：

- **稱號 2/3、技藝 2/3、改造 2/3** —— 這三類的效果偏「永久被動」（技能等級、天生防禦、意志值、
  檢定加減值），簡化規則原生就接得住。
- **血統 1/3、瞳術 1/3、流派 1/3、法術 1/3** —— 卡在能量池、變身形態、施法者職能。

於是待辦的優先順序不再是猜的：**暫時性增益容器 → 意志力消費端 → 能量池資料結構**，
這三個做完，模板能力類的轉換率會整個翻上來。

### 四、rules-2.35.txt 的精簡（這一條有風險，做法記在這裡）

使用者要求把已經簡化過的部分從規則書刪掉並註明去向。做了 16 個頁面，刪掉 9,948 行
（388,119 → 378,171，約 760KB）。**沒有碰資源型錄**，那是還要繼續挖的礦。

**風險與處理**：`rules-2.35.txt` 的行號是這個專案的引用系統（207 處引用散在 37 個檔案）。
刪行會讓每一個行號位移。做法是**trim 與行號重寫同一次做完**：先產生 old→new 位移表，
trim 完立刻用同一份表把整個 repo 的行號一起改掉，機械式對照，不重新查證。
被刪掉的頁面原地留下一個標記，寫明「這頁去哪了」以及取回原文的 git 指令。

**之後如果還要再 trim，請照同樣做法**，不要留給下一個人手動對。完整紀錄、
判斷「刪什麼／不刪什麼」的準則、以及刻意保留的頁面清單，全部在 `RULES_TRIM.md`。

## 暫時性增益容器「型態」 —— 2026-08-17（第三輪）

### 為什麼是這一項

上一輪留下的 `CONVERSION_RULES.md` 第 12 節轉換率表，把「卡住的機制」欄當成待辦清單，
排出來的第 1、2 順位是**暫時性增益容器**與**意志力消費端**。這一輪把兩個一起做掉，
順便驗證了那張表到底能不能當排程用——結論是可以：血統從 1/3 上架變成 2/3，
而且不是靠多花轉換人力，是靠補上一個引擎機制。

### 設計：型態是第 14 種效果，但它是唯一一個「容器」

```json
{ "kind": "型態", "label": "進化形態",
  "activation": { "action": "移動", "willpower": 1 },
  "duration": { "unit": "場景" },
  "grants": [ { "kind": "防御", "amount": 3 }, { "kind": "武器", "...": "..." } ] }
```

四個關鍵決定，每一個都是為了讓型態不會退化成「免費的永久加值」：

**1. 型態不改角色卡。** 啟動只是在 `formsState.active` 放一筆紀錄；加值由 `effects.js` 的
三個查表函式在每次用到時把 `activeGrantSources()` 併進來。於是「到期」不需要有人記得
回收加值——紀錄一移除，下一次查表就查不到了。這也決定了 `grants` 只能放**查詢型**效果：
`屬性／技能／生命上限／意志上限` 是購買當下寫進角色卡的，收不回來；`治療／好感度`
是發生一次的事件，放進會反覆開關的容器裡會變成無限回血。

**2. 只吃兩個時鐘：戰鬥輪與場景。** 兩個都是引擎裡**真的在前進**的計數器。書上的
「10 分鐘」「每天一次」沒有東西可以倒數它們——加一個沒有人會遞減的計時器，等於加一個
永不過期的型態。這條直接沿用「專業系統」那則決策記錄的教訓，也是為什麼
`UNSUPPORTED_MECHANICS` 新增了 `長期計時` 這個鍵。

**3. 啟動一定要付代價**（意志力或動作，至少一樣），`validateEffect()` 擋。
這順便讓 `character.derived.willpower` 有了**第一個消費端**——在這之前它是一個算得出來、
但沒有任何程式碼會扣的數字。

**4. 型態內部的檢定加值可以沒有匹配鍵。** 這看起來像是把「條件式效果紅線」放寬了，
其實是**換了一個守門員**：紅線的理由是「不能讓 AI 決定加值何時生效」，而型態的開關是
引擎自己持有、自己倒數、自己到期的狀態。裁判從 AI 換成程式碼，紅線的理由就不成立了。
代價是型態必須有成本、必須會過期——這兩件事被驗證擋著，所以這個例外沒有辦法被濫用。

### 順手接上的兩條斷線（都是「寫好了但沒有人呼叫」）

型態要能生效，得先讓商品效果真的進到戰鬥與判定裡，於是這一輪同時修掉兩個既有的洞：

- **`encounterState.js` 先前用寫死的 `emptyCombatProfile()`**，也就是說買了防具/血統也不會
  變難打。現在玩家的防御/護甲/先攻/可用武器都從 `combatProfileFrom()`／`weaponsFrom()` 重算。
- **`checkModifiersFor()` 先前的唯一呼叫端是測試。** `functions/api/` 底下三個會呼叫
  `performCheck()` 的地方（check／turn／narrate）沒有一個呼叫它，於是整個「專長」貨架
  買下去在真實遊戲裡完全不生效。現在三個都經過新的 `applyCheckModifiers()`。

這兩條都是 `CONVERSION_RULES.md` 第 1 節在講的同一件事——「看起來有效、實際上沒有程式碼
會讀它」——只是漏掉的不是欄位，是最後一段接線。**給下一個人的提醒：擴充效果詞彙表時，
請一路追到 API 進入點，不要只追到查表函式。**

### 還沒做的（刻意）

型態狀態目前只活在 `combat.forms` 裡。戰鬥有存檔，所以戰鬥中的變身撐得過重整；
但**戰鬥外**啟動的型態沒有地方放——跟錢包一樣，卡在 `content/storage/sessionStore.js`
的存檔格式還沒長出對應欄位。這是錢包／型態／購買紀錄三件事共用的同一個待辦，
建議之後一次做完，不要各接各的。

## 攻擊路徑接上效果表，與「能量池不是下一步」 —— 2026-08-17（第四輪）

### 一、原訂的下一步（能量池）查證後被否決，這是本輪最重要的產出

上一輪把「能量池」排成下一個機制。動工前先去查書上的池子怎麼定義，結果發現
**它卡在資料，不是卡在程式**——規則書第 604 行明寫「每種能量在獲得時，都會同時獲得它的
運作機制（使用何者作為關鍵屬性，如何恢復、補充…）」，也就是說書上刻意**沒有**通用的
池子上限公式；查到的三個實例全是某個資源各自給的增量（魔力擴容 +20、+40、+5）。

而我們手上那三個掛名條目（青蓮劍歌總決／寫輪眼／紅馬尾戰士）**全部只是池子的消費者**，
沒有一個定義池子的上限與恢復。現在做容器，會做出一個沒有數字可以填的容器，
三個條目仍然全部掛名，只是理由換一句話。

**這件事本身是個方法論教訓，值得記下來：** 「卡住的機制」欄能排出優先順序，但它排的是
「缺什麼」，不是「補得起來嗎」。**動工前要多問一句：補這個機制需要的數字，書上到底有沒有？**
需要範圍決定（要不要追到 D 級以外去轉「開池子」的資源）的事情，留給使用者決定，不要自己選。

### 二、改做原訂第 2 順位：讓攻擊也查效果表

發現的洞比預期嚴重：`檢定加骰／附加成功` 只接在 `core/check.js` 上，而戰鬥攻擊走的是
`resolveCombatAction()／attackTypes.js`，**完全不經過效果表**。於是同一個「+2DP」的加值，
在敘事檢定裡有效、在戰鬥攻擊裡靜靜地無效——這比完全不生效更糟，因為它看起來是有效的。

修法是三個小東西一起：

1. **`ATTACK_CHECK_KEYS`**（`attackTypes.js`）——把每種攻擊方式算的是哪一組屬性＋技能
   講出來（肉搏＝力量+格鬥、炮＝智力+射擊…）。這些是公式裡**本來就在用**的欄位，
   只是先前被包在 `strength`／`unarmedSkill` 這種具名參數裡，外面看不見。
   **這不是改寫攻擊公式**，`CONVERSION_RULES.md` 第 5 節仍然成立。
2. **`attackModifiersFor()`**（`effects.js`）——用跟一般檢定**完全相同**的匹配規則查攻擊。
   結果經新的 `attackDpModifier` 參數進 `resolveCombatAction()`，跟 `attackParams` 分開傳：
   `attackParams` 是公式的輸入，改它等於改公式；這個是公式算完後的外部調整。
3. **匹配鍵的兩個維度**——複數鍵（`attributes: [...]`，寫得出「生理系屬性」）與
   `scope`（`檢定`／`攻擊`／`全部`）。沒有 scope 的話，一個「攻擊+2」會連帶讓所有力量檢定 +2，
   那是憑空放大；有了 scope，範圍精確，而且判斷者仍然是程式碼。

成果：假面騎士EVOL 的「攻擊強化」從 `droppedTraits` 撿回來——它需要三個機制同時到位
（型態容器給期限與意志力成本、複數鍵給「生理系屬性」、scope 給「攻擊檢定」），
是這三樣東西是否夠用的一次完整驗收。

### 三、下一輪建議：存檔格式

錢包／型態／購買紀錄三件事都卡在 `content/storage/sessionStore.js` 還沒長出欄位。
這一項**純粹是工，沒有任何未知**，而且做完商店才真的能玩（現在型態只在戰鬥中撐得過重整）。
建議一次做完，不要各接各的。

## 存檔格式、商店前端、能量池 —— 2026-08-17（第五輪）

使用者的三項決定：**A（往上追池子的來源）／存檔格式／商店前端，副本中不能買**。

### 一、門禁：規則書比「副本中不能買」更精確

第469行原文是**兩種貨幣兩種待遇**：支線與獎勵點數只能在主神空間花；經驗兩邊都能花，
但在副本中要付兩倍。本專案「專長」整個貨架都是 XP 定價的，所以這條規則直接決定了
玩家在副本中還剩下什麼事可以做。實作在 `content/shop/access.js`，
「現在算不算在主神空間」由引擎自己從存檔算（沒有副本／已通關＝主神空間），不是 AI 說了算。

### 二、順手修掉的語意錯位：節點獎勵一直被當成 XP

節點的獎勵欄位叫 `baseRewardPoints`，schema 註解寫「基礎**積分**獎勵」，
但 `turn.js`／`act.js` 收到之後做的是 `character.xp.earned += reward`。
原因很單純：**在錢包進存檔之前，沒有地方可以放獎勵點數**。現在放回該去的地方，
XP 改由副本通關時結算（`core/campaignXp.js` 的 `fixedSessionXp`——本專案第三個
「寫好了、測好了、但一次都沒有被呼叫過」的模組）。兩種貨幣於是各有各的來源：

    副本中完成節點 → 獎勵點數（要回主神空間才花得掉）
    副本通關      → XP 結算，同時商店開門

### 三、能量池：一次錯誤判斷的完整經過（**這一輪最重要的部分**）

上一輪查證後宣告「書上刻意沒有通用的池子上限公式，能量池卡在資料不是卡在程式」。
**那是錯的。** 通用公式一直都在（`生命.htm`：「關鍵屬性一般為 2 個屬性，
這 2 個屬性之和即為該能量池大小的基礎上限」），只是**被本專案自己的規則書精簡輪刪掉了**，
而且刪除紀錄還寫著「能量池整套不實作」，讓它看起來沒有東西可以失去。

兩層教訓，都寫進 `RULES_TRIM.md` 了：

1. **不要因為「目前不實作」就移出一頁規則。**「目前不實作」正是「將來要實作」的同義詞，
   而實作它的時候需要的就是那一頁。該不該移出的標準是「這一頁的規則**已經住進程式碼**了嗎」。
2. **查書查不到某條通則時，先去對一下 `RULES_TRIM.md` 的移出清單。**
   那份清單就是為此存在的，但第一次查證時沒有去看它。

原文已經取回並貼進 `RULES_TRIM.md` 的附錄，實作在 `core/energyPools.js`。

### 四、A 的成果：第一條完整的池子鏈

    買混元劍經 → 開劍氣池（上限＝敏捷+感知）→ 買青蓮劍歌總決 → 支付1點劍氣變出6L的劍

每一環都有測試守著，任何一環斷掉最後那把劍就變不出來。順帶把
`prerequisites.goods`（「必須先擁有某件商品」）做成真的擋得住的欄位——
書上大量條目的前提是另一個資源，在此之前那種前提只能寫在註解裡。

**範圍上的取捨**：這一輪踩出了「只做七個模板化分類」的限制，多收了一類
「開啟能量池的資源」（`shop-starter-pools.json`）。原本鎖「每類剛好3件」的測試
因此放寬成「每類至少3件起始商品，池子來源另計」，理由寫在測試裡。

## 池子的第二輪，與一個活在存檔格式裡的謊言 —— 2026-08-17（第六輪）

這一輪做的是上一輪排的第 5 順位「繼續追池子來源」。**整輪沒有新增任何一行引擎機制**——
兩條新的鏈完全是既有詞彙表拼出來的，這件事本身就是上一輪的驗收結果。

### 一、通則寫好之後，追一條新的鏈只剩「抄兩個屬性名」

    買忍者(稱號)   → 開查克拉池(耐力+感知) → 買寫輪眼(同池第二個來源，上限+5)
                   → 標準動作＋1點查克拉開「洞察眼」 → 攻擊+2DP、防御+2，持續一個場景
    買內力心法(技藝) → 開內力池(感知+耐力)   → 買葵花寶典(第二個來源，上限+5)

上一輪的第一條鏈（劍氣）證明「機制接得起來」，這一輪證明的是**邊際成本**：
新的池子來源在原文裡是一個獨立欄位（單獨一行「能量池：X」），
所以 `grep -n "能量池：" rules-2.35.txt` 直接就是下一輪的待辦清單。

順帶收掉三件事：

- **寫輪眼掛名的理由本身有一半是看漏。** 上一輪寫的是「需要查克拉能量池」，
  但寫輪眼那一頁的第 117996 行就寫著「能量池：查克拉」——**它自己就是來源**。
  下次寫 `pendingReason` 說「缺某個資源」時，先確認那個資源不是這個條目自己給的。
- **通則被一個獨立條目複述了。** 葵花寶典原文寫「若已擁有內力則使內力上限提高5點」，
  那個 5 正是 `REOPEN_BONUSES[0]`。因此葵花寶典的商品資料裡沒有任何地方寫 5——
  一個獨立條目複述了通則的數字，是「上一輪的通則抄對了」目前最強的旁證。
- **兩件新商品的價格都對得上模板**（稱號 D+1000、技藝 D+500），
  稽核通過，反過來替「忍者是稱號」「內力心法算技藝」這兩個分類推論背書。

### 二、上一輪的教訓有一個對稱面：「查到了，但不可能」也要寫下來

上一輪的教訓是「查不到某條通則時，先確認它是不是被自己刪掉的」。這一輪碰到的是反面：
**屬性力的關鍵屬性查得到，答案是決心+風度——兩個都是六維裡不存在的維度。**

紅馬尾戰士先前的掛名理由寫的是「關鍵屬性還沒查到」，那句話會讓下一個人再查一次同一件事。
所以新增了 `core/energyPools.js` 的 **`REJECTED_POOLS`**：查過但收不了的池子，
附行號、附它擋住哪些商品，並且有測試守著「被否決的池子擋住的商品確實還是掛名」。

**一個查證過的否定結論如果不寫下來，它跟「還沒查」在下一個人眼裡長得一模一樣。**

### 三、⚠️ 查證過程中發現：`session.forms` 沒有任何消費端

第五輪把 `wallet` 與 `forms` 加進存檔 version 2，`CONVERSION_RULES.md` 第 11 節
的「型態狀態還沒進存檔」就當它解決了。**它沒有解決**——存檔欄位建了，但全專案
沒有任何一行程式碼讀它或寫它：

| 缺的接線 | 現況 |
| :--- | :--- |
| 戰鬥外啟動型態的入口 | 不存在（`activateForm()` 在 `functions/` 底下零呼叫端） |
| `createEncounter(..., { forms })` | `combat/start.js` 沒有傳，開戰時一律空的 |
| 三個 `applyCheckModifiers()` 呼叫點 | 都沒傳 `extraSources`，戰鬥外的型態吃不到 |

這正是本專案一再拆掉的那個東西（見下面「專業(specialization)系統：決定不做」），
只是這次躺在**存檔格式**裡，而且商店前端還列得出「可啟動的型態」，
讓它從畫面上看起來像是能用的。**已排成下一輪第 1 順位。**

補它之前要先回答一個真正的設計問題：**戰鬥外的「場景」什麼時候結束？**
戰鬥裡有 `finalizeIfOver()` 當那個時刻，戰鬥外沒有——答錯就會做出一個永不過期的型態，
也就是 `forms.js` 整個設計在避開的那件事。

### 四、一條被丟掉的**限制**（不是好處），必須被看見

寫輪眼的「內耗點數」（每次付查克拉就累積，最後會瞎）沒有轉——沒有那種點數軌。
**丟掉它讓這件商品比書上強**，這是簡化造成的加強而不是損耗，
而 `CONVERSION_RULES.md` 第 0 節只要求記下損耗。所以它同時寫進了該條目的
`conversionNote` 與優先順序第 7 條：做不良狀態軌那一輪時，內耗點數第一個補回來。

## 場景 ＝ 當下所在的地點，與 `session.forms` 的三條接線 —— 2026-08-17（第七輪）

使用者的三項決定：**場景＝當下所在的地點／不良狀態全部刪除／每類 3 件就停，開始做別的**。

### 一、一句話解掉了一個卡住的設計問題

上一輪查到 `session.forms` 沒有任何消費端，但**沒有順手補**，因為補之前要先回答
「戰鬥外的場景什麼時候結束？」——答錯就是一個永不過期的型態，正是 `forms.js`
整個設計在避開的事。使用者的答案讓它變得算得出來：

```
sceneKeyOf(session) → "主神空間" ｜ "恐怖片中:scenario.nostromo-01:n1"
```

**純粹從存檔算出來，沒有新欄位**，跟 `locationOf()` 同一個原則：
「現在算不算換場了」由引擎自己算，不是 AI 敘事時說了算，也不是前端傳進來的。

**這個定義推翻了一段既有的實作**：`finalizeIfOver()` 原本呼叫 `endScene()` 把型態全部清掉
（那是在「場景」還沒有定義時的權宜作法）。打一場架不會改變你站在哪裡，所以現在
戰鬥結束只呼叫新的 `endCombat()`，**只收以「輪」計時的型態**；以「場景」計時的跟著
`combat.forms` 回到 `session.forms`，撐到玩家離開這個地點為止。

實作上刻意選了**「每次讀取前先對一次鑰匙」**（`formsForScene()`）而不是
「換地點時記得清一次」：後者要求每一個會改變地點的地方都記得呼叫，漏掉一個就是一個
永不過期的型態，而漏掉的那個地方**不會有任何症狀**——這正是這個專案一再踩到的那種洞。

### 二、三條接線，與一條刻意不接的

| 接線 | 之前 | 現在 |
| :--- | :--- | :--- |
| 啟動入口 | 不存在 | `functions/api/forms.js`（啟動／收功） |
| `/api/turn` 的檢定 | 沒傳 `extraSources` | 傳了，戰鬥外的變身真的加骰 |
| `combat/start` → `act` | 開戰一律空的型態狀態 | 帶進去、收兵帶回來 |
| `/api/check`、`/api/narrate` | — | **刻意不接**：它們是無存檔的示範端點（角色卡由 body 傳入），沒有 sessionId 可以查型態。前端只呼叫 `/api/turn`。理由寫在那兩個檔案裡 |

### 三、這一輪第一次真的在瀏覽器裡點過

先前兩輪的「已知未驗證」都寫著「商店頁沒有在真的瀏覽器裡點過（環境沒有 playwright）」。
這一次環境有 Chromium，所以整條路徑是實際跑過的，不是推論的：

1. `wrangler pages dev` 起本機 Pages（`[ai]` binding 需要 API token，驗證期間暫時停用，事後還原）
2. 用純函式先買好忍者＋寫輪眼，把角色卡存進 KV
3. `/api/forms` 回 `sceneKey: 恐怖片中:scenario.nostromo-01:n1`，列出洞察眼
4. 啟動 → 查克拉 6→5，**重讀存檔仍在進行中**（真的寫進 KV 了）
5. `/api/turn` 在變身後回 `持有能力加值：…帝國子民(檢定加骰+1)`，變身前沒有 —— **這就是那條缺了的線**
6. Chromium 裡按下商店頁的「啟動」，按鈕翻成「收功」，提示寫「持續到你離開這個地點」

### 四、順帶推翻一個上一輪的結論：線上 API 沒有壞，是網址打錯了

上一輪的「已知未驗證」寫著「Pages Functions 在 pages.dev 上似乎沒有被服務：`/api/shop`
與**既有的** `/api/session` 在 preview 與正式 host 都回 404（`/` 回 200）」，
並據此懷疑 Pages 專案設定有問題。

**那個結論是錯的。** 這個專案實際的 Pages 網域是 `ai-trpg-evd.pages.dev`，
而測試時打的是 `ai-trpg.pages.dev`——一個不屬於這個專案的網域。打對之後：

    /              → 200
    /api/session   → 200
    /api/shop      → 400 {"ok":false,"error":"需要 sessionId"}   ← 函式有在跑的證據
    /api/forms     → 400 同上（這一輪新加的端點也活著）

而且整條型態鏈在**線上**跑得完：建存檔 → 列出洞察眼 → 啟動扣 1 點查克拉（6→5）→
重讀線上 KV 仍在進行中。

**教訓跟上一輪的能量池是同一種**：一個查證結論的可信度，取決於你有沒有確認自己
量到的是正確的對象。`404` 有兩種讀法（函式沒部署／你打錯站），當時只考慮了第一種。
正確網址以每次部署後 PR 上 Cloudflare 機器人留言的 Preview URL 為準，
不要從專案名稱推測——這一條已經寫進 `DEPLOYMENT.md` 的步驟6。

### 五、順著同一把尺再掃一次，抓到兩個一模一樣的洞

補完 `session.forms` 之後，用「有哪些匯出函式在正式程式碼裡零呼叫端」掃了一遍全專案。
大部分是同檔內的輔助函式（假陽性），但有兩個是真的，而且**跟 `session.forms` 是同一個病**：

| 函式 | 後果 |
| :--- | :--- |
| `resolveFormActivation()` | **戰鬥中變不了身**——引擎做得出來，但 `/api/combat/act` 只認得 `weaponKey`，沒有任何 API 叫得動它 |
| `playerWeapons()` | **買到的武器在戰鬥裡按不到**——引擎其實吃得下（`resolvePlayerAttack()` 內部就查這張表），但戰鬥畫面的按鈕是 `index.html` 裡寫死的兩顆（徒手、手槍） |

第一個尤其要記：上一段剛寫的 `/api/forms` 會回一句「戰鬥中的變身要在戰鬥畫面裡做」，
而**那個畫面當時沒有那顆按鈕**——等於自己新增了一句指向不存在功能的訊息。

兩個一起補：新增 `combatOptions(combat, character)`，**由引擎算出「這一輪按得下去的東西」**
（武器表＋型態表，含「哪些型態已經在進行中」「哪些武器是型態給的」），
由 `/api/combat/start` 與 `/api/combat/act` 一起回傳，前端純畫。
`/api/combat/act` 多收一個 `action: "型態"`，且**不推進行動順位**——
書上這類啟動花的是動作，不是整個回合，玩家變完身本輪還可以出手。

瀏覽器實測：買了阿蘭斯與審判眼的角色開戰後，行動列出現四顆按鈕
（徒手／制式手槍／**加斯特沖擊波(龍骨炮)**／**帝國子民**），
按下帝國子民後它變成「（進行中）」，戰鬥日誌出現「帝國子民 啟動」。

### 六、兩個被關閉的問題

- **不良狀態軌：決定不做。** 使用者：「太複雜了，我想要全部刪除」，之後會自己重新平衡資源。
  `UNSUPPORTED_MECHANICS.不良狀態` 因此升格成**永久排除**，跟「專業系統：決定不做」同級。
  連帶：**寫輪眼的「內耗點數」永久留在丟棄堆**，而丟掉它讓那件商品比書上強——
  這是「之後自己看資源要怎麼平衡」時第一個該看的例子。
- **型錄轉換暫停。** 七類都有 3 件了，使用者決定停在這裡改做別的。
  優先順序裡剩下的第 8、9 條（可變量型態、維持成本型態）留著是因為它們是引擎機制，
  不是因為要衝貨架數量。

## 休息：把一個只出不進的資源接回來 —— 2026-08-17（第八輪）

**這一輪補的是自己上一輪造成的洞。** 把能量池接上消費端（型態啟動真的扣查克拉）之後，
情況變成「開一次洞察眼扣1點，那1點永遠回不來」——因為整個遊戲迴圈裡**沒有任何休息**：
`core/health.js` 的 `shortRest()` 與 `core/energyPools.js` 的 `recoverEnergy()`／
`longRestPools()` 全部是零呼叫端。這是用「有哪些匯出函式在正式程式碼裡零呼叫端」
掃出來的第三個洞，跟 `session.forms`、`resolveFormActivation()` 是同一種病。

### 一、代價由使用者決定，恢復量照書上抄

| | 什麼時候 | 代價 | 恢復多少 |
| :--- | :--- | :--- | :--- |
| 完全恢復 | 主神空間 | 無 | 生命／意志力／所有能量池全滿 |
| 打坐 | 副本中 | **3 回合時間預算** | 每個池子做一次關鍵屬性判定，**恢復等同成功數**；生命 1 點沖擊 |

左半邊（什麼時候、代價）是使用者的決定；右半邊是書上原文——每個能量池的頁面都寫著
同一句「自行恢復或者打坐，前者一天恢復完全，後者每次消耗起碼15分鐘，做一個關鍵屬性的判定，
恢復等同於成功數的點數」。兩種休息剛好對上那句話的兩半。

**打坐是一次真的擲骰**，走 `performCheck()`，骰池是那個池子的兩個關鍵屬性之和——
也就是決定這個池子上限的同一個數字。所以「打坐了一場、一點都沒恢復」是會發生的，
那是書上的規則不是 bug，測試裡用 `successThreshold: 11` 逼出這個情況鎖住它。

**意志力刻意不在副本中恢復**：書上的意志力恢復是「冥想」**專長**給的每日效果（第2700行起），
不是通則。沒有通則就不發明數字（第 0 條總則）。主神空間的完全恢復包含意志力，
因為那是使用者明確說的「完全恢復」。

### 二、順帶接上另一條零呼叫端：復活要清掉型態

`clearTemporaryEffectsOnRevival()` 也是零呼叫端，而且它收的
`{temporary, permanent}` 是一個**從來沒有實作出來的效果系統**的形狀。
書上的規則（復活後非永久 buff 全部移除）真正該清的東西是**型態**——
而型態在同一天才剛變成跨戰鬥、跨重整的持久狀態，不清的話玩家倒下復活之後還維持著變身。

執行點放在 `functions/api/revive.js`（用 `endScene()`），
並在那個舊函式的註解上寫明「**這不是那條規則目前的執行點**」，
免得下一個人以為呼叫它就等於執行了規則。

### 三、實測

本機 Pages + Chromium 跑完整條：開眼 6→5 → 按下休息 → 「打坐了3回合；查克拉 5→10/11」
出現在敘事列、時間預算變成 3/14（充裕）→ 連休 4 次後被擋下
（「打坐要花3回合，但只剩下2回合」）。無頁面錯誤。

## 型態容器的兩個缺口，與兩個躲在另一條路徑上的舊洞 —— 2026-08-17（第九輪）

這一輪做的是 `CONVERSION_RULES.md` 優先順序上僅剩的兩條引擎機制（第 8、9 條），
它們是**同一個容器的兩個缺口**，所以一起做：

| 缺口 | 書上長什麼樣 | 撿回來的特性 |
| :--- | :--- | :--- |
| 可變量支付 | 「支付最多不超過自身敏捷或感知取低/點劍氣，獲得等額加值」（第240996行） | 混元劍經·劍氣 |
| 維持成本 | 「每輪必須以一個自由動作支付1點內力維持此效果」（第207198行） | 葵花寶典·鬼魅身 |

### 一、兩個擴充都是「玩家在啟動當下的決定」，紅線沒有動

型態當初能存在，靠的是一個論證：「變身期間」這個條件的裁判**是引擎不是 AI**，
所以它不違反「條件式效果不准轉成數值」那條紅線。這一輪的兩個擴充沿用同一個論證，
而且更乾淨——**選擇點只存在啟動的那一瞬間**：付幾點、選攻還是選防，都在
`activateForm()` 裡當場乘開／選定，寫進進行中的紀錄。之後每一次查表看到的都是
一組普通的固定效果，**查表的時候已經沒有任何條件要判斷了**。

順帶做了第三件沒排進清單的事：`modes`（二選一）。理由是「劍氣」同一句話裡就同時卡著
可變量與二選一，只做一半就得把另一半丟進 `droppedTraits`，那等於這件商品要再排一輪。

兩個刻意的「不給預設值」：**沒報支付點數就不啟動**，不會偷偷用最小值——
那會讓一次手滑變成一次真的花錢；**上限規則（取低／取高）漏寫就擋下來**，
不猜一個，因為猜錯的方向剛好是「玩家可以多付幾點」。

### 二、維持成本是型態的第二種到期方式，收錢的地方只有一個

先前型態只有一種死法：時鐘走完。現在多了「某一輪付不出錢」。
**收錢的地方只有 `encounterState.js` 的 `advanceTurn()`**，也就是引擎裡唯一
會週期性回頭的地方——這跟第七輪 `formsForScene()` 的選擇是同一個原則：
與其要求每一條路徑都記得收錢，不如讓所有路徑都經過同一個收費站。

推論有兩個，都要看見：
1. **維持成本只能掛在「輪」上**（`validateEffect()` 擋）。掛在「場景」上就是一個
   永遠不會被收錢的維持成本，比沒有維持成本更糟，因為它看起來有。
2. **因此鬼魅身只在戰鬥中用得起來**，書上沒有這個限制。這是登記簿上**第一筆「比書上弱」**
   （`CONVERSION_RULES.md` 第 14 節）。登記簿變長是轉換率上升的副產品：
   一條還躺在 `droppedTraits` 裡的特性不會讓商品變強或變弱，它只是不在。

角色卡因此要沿著攻擊函式流回呼叫端（`resolvePlayerAttack()` 現在回傳 `character`），
跟 `resolveFormActivation()` 同一個約定。沒接住的話，維持成本會每輪都「扣了但沒扣」。

### 三、實測抓到兩個舊洞，兩個都不是這一輪寫壞的

本機 Pages + Chromium 跑完整條（開劍氣付3點 → 攻擊 → 劍氣到期 → 鬼魅身維持一輪 →
內力見底斷氣，訊息全部出現在戰鬥紀錄上）。過程中撞到兩個**上一輪就存在**的問題：

1. **`/api/forms` 擋下來的時候回的是 `ok:true`。** `...formsPayload(session)` 自己帶著
   `ok:true`，而它被展開在 `ok:false` **後面**，把它蓋掉了。前端的 `if (!res.ok)` 因此
   永遠不成立——玩家按一個按不動的按鈕，會看到一句綠色的「啟動成功」而什麼也沒發生。
   **這條線上一輪是用眼睛看過的，但看的是「能不能啟動」，沒有看「擋下來的那一條」。**
   教訓：驗收一條路徑時，**成功路徑與被擋下的路徑是兩條路徑**。
2. **重整頁面回到戰鬥中時，行動列退回寫死的兩顆按鈕。** 上一輪把行動列改成引擎算的
   `combatOptions()`，但只接了 `/api/combat/start` 與 `act` 兩條回應，**續戰那條路徑
   沒有人算它**（`currentCombatOptions = null`）。症狀跟上一輪修掉的完全一樣——
   買到的武器與身上的型態按不到——只是躲在另一條路徑上。現在 `/api/session` 的 GET
   在戰鬥進行中時一起回 `combatOptions`。

兩個都補了測試（`test/formsApi.test.js` 是新增的，它問的不是規則對不對，
而是**API 層有沒有把玩家的決定送進去、有沒有把引擎的收費送回來**）。

### 四、本機實測的跑法（下一個人會需要）

```bash
npm install -D wrangler playwright-core      # 兩個都不是專案依賴，測完可以丟掉
npx wrangler pages dev --port 8788
```

兩個會擋住你的地雷：
- `wrangler.toml` 的 `[ai]` binding 在非互動環境會要求 `CLOUDFLARE_API_TOKEN` 而啟動失敗。
  本機測試時把那兩行註解掉，**測完一定要還原**（`git checkout wrangler.toml`）。
- Chromium 在 `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`；
  頁面的 Tailwind 是 CDN 載入的，這個環境連不出去，所以畫面沒有樣式——
  **功能測得出來，版面測不出來**，不要用截圖判斷樣式有沒有壞。

## 敘事體驗與節奏(Phase 5.3, 2026-08-18)的決策記錄

這一輪全部集中在「同樣的數字，怎麼讀起來不一樣」。引擎沒有任何公式被改動——
下面五件事**沒有一件**參與骰池、DC、傷害或HP轉換的運算，這是這一輪的紅線。

### 一、敘事者人格面具：文筆層底下再拆一層

`STYLE_PROFILES` 回答的是「怎麼寫」（句法、修辭、黑名單），它沒有回答「**誰**在寫」，
於是換文筆設定檔之後，敘事仍然是同一個聲音。`NARRATOR_PERSONAS` 補的就是這一層：
冷酷裁判／溫柔神明／崩潰倖存者三個面具，可以跟任何一個文筆設定檔自由搭配。

分層沒有被打破：面具走的是跟文筆完全同一條組裝路線（`buildStylePrompt()` ->
`composeSystemInstruction()`），仍然排在規則契約之前，模型讀到的最後一句話仍然是
「以規則契約為準」。`test/narrativeStyle.test.js` 對**每一個面具**都跑一次這條檢查——
新增面具時那個迴圈會自動涵蓋它，不需要有人記得補測試。

順手把兩段本來放錯地方的文字搬回文筆層：
- 【定向要求】（玩家人在哪／為什麼在這裡／下一步往哪去）原本寫在 `buildOptionsSpec()` 裡，
  但它約束的是敘事，跟「要產幾個選項」無關。留在那裡的後果是換文筆時沒有人會想到檢查它。
- 【活場法／防全知】是這一輪新增的（來源：使用者提供的 SillyTavern 預設）。
  「防全知」看起來像規則、其實是文筆：判定結果是事實，但「玩家角色當下知不知道」是取鏡問題。

### 二、純敘事選項：不是每個動作都該擲骰

`requiresCheck: false` 的選項完全不進判定流程，`attribute/skill/difficulty/dc` 全部被
強制清成 `null`。「乾淨」是這個設計的重點：只要 `dc` 還留著一個數字，之後任何一個呼叫端
都可能拿它去擲骰，而按鈕上寫的是「這個行動沒有風險」——那就是引擎在騙玩家。
`optionToCheckParams()` 對純敘事選項直接丟錯，就是為了讓這種漏接當場炸掉而不是靜靜發生。

兩個防呆：欄位缺席時一律當成「要檢定」（猜錯的代價不對稱：猜成不檢定會讓有風險的行動
變成免費通行證）；一輪最多兩個純敘事選項，超過的丟掉並用保底檢定選項補滿，
否則模型可以把整頁選項都給成無風險行動，等於把骰子系統關掉。

**已知限制（沒有假裝防住）**：伺服器查驗的是前端送回來的那個選項物件，所以玩家竄改
`requiresCheck` 仍然改得動，跟「把難度分級從困難改成容易」是同一類問題，解法也一樣
（伺服器端保存這一回合發出去的選項，或對選項簽章）。單人遊戲裡這只是自己騙自己。

### 三、失敗轉折：把「處境不同」換成「不准待在原地」

舊的 `NO_RESET_CLAUSE` 只要求「下一回合的處境必須不同」，而那對模型太好交差了——
把燈光改成閃爍的、把玩家從門左邊挪到門右邊，形式上就「不同」了，玩家實際上還是站在
同一道門前面用同一個方法再試一次。新增的 `FAIL_FORWARD_CLAUSE` 改成**可以檢查的**要求：
位置、方法還能不能用、現場多了少了誰，三者至少要換掉一樣；並且把失敗限定成兩種形狀
（動作完成了但引發更糟後果／徹底失敗且處境更危險），明確禁掉第三種「暫停後重試」。

`OUTCOME_TIERS` 因此被導出：那幾段文字是這個模組唯一的產出，被誰改短一句，
遊戲就會靜靜退回舊行為而沒有任何測試會紅，所以測試直接逐級檢查禁令還在不在。

### 四、傷害嚴重度標籤：把數字翻譯成畫面強度

AI 收到「實質傷害 3 點」時，沒有依據判斷 3 點在這套規則裡是擦傷還是皮開肉綻，
於是每次受擊都寫得差不多。`damageSeverityTag()` 依「實質傷害 + 嚴重度(B/L/A)」查表給一句
視覺提示，在**扣完血之後**才算——順序本身就是「它不參與運算」的證明。
惡性傷(A)不論點數多少都算重創，因為 A 傷會直接吃掉完好格且不可回復。

標籤同時走三條路：戰鬥紀錄（玩家看得到）、事件日誌摘要（AI 的事實記憶）、
以及 `buildCombatNarrationPrompt()` 組出來的敘事指令（要求AI照標籤描寫視覺與聽覺細節，
但不可以把標籤文字或傷害數字寫進敘事裡）。

### 五、敵人意圖預告：讓戰鬥有得判斷

在這之前戰鬥畫面上只有血條與「輪到你了」，敵人要做什麼玩家一無所知，最佳策略永遠是
「按同一顆攻擊鍵到分出勝負」。`currentTelegraph` 在**每一輪開始、行動順位回到玩家之前**
抽好（`createEncounter()` 與 `advanceTurn()` 各一處），所以玩家永遠是先看到預告再做決定。

它是純敘事資料：不影響命中、傷害、先攻。要讓預告帶機制效果（「翹起尾巴的下一輪傷害+2」）
是另一件事，需要規則書依據，這一輪刻意不做——保持它完全無害，資料寫錯也只會讓畫面上
多一句怪話，不會弄壞戰鬥數學。抽取用注入的 `pickFn`（跟骰子的 `rollFn` 同一個約定），
測試才鎖得住。

### 六、順手接上的兩條線（都來自使用者提供的 SillyTavern 預設）

- **思維鏈 `st_thought`**：JSON schema 裡排在 `narration` **之前**，因為結構化輸出是按
  properties 順序生成的——只有真的排前面，「先盤算再下筆」才成立。它不給玩家看、
  不進存檔的 history，也不是真理來源（裡面寫了數字一律不採用）。
- **XML 標籤隔離**：`<Scene>`／`<Story_So_Far>`／`<Fact_Log>`／`<Player_Action>`／
  `<Engine_Result>`／`<Combat_Result>`。長 prompt 裡四段全是自然語言時，歷史敘事裡的
  一句話會被當成本回合的指令（實測踩過：前情提要裡的「你可以再試一次」）。
  標籤只是分隔符，【】小標題全部保留，內容一個字都沒改。

## 給下一個 AI 的具體建議

1. 先讀這份檔案，再讀 `TEST_PLAN.md`，兩份一起看就知道「什麼做完了、什麼是規則書的、什麼是我們設計的」。
2. 改動任何 `[規則書]` 模組前，回頭查 `rules2.35.txt` 對應行號(TEST_PLAN.md 有列)，不要憑印象改。
3. 新增任何數值系統，一律先寫測試(理論值/不變量/書內範例三選一)，再寫實作，這是這個專案從 Phase 1
   就建立的習慣，請延續。
4. 如果使用者又丟出一個「聽起來很酷但沒有明確數值」的新點子(這種事常發生)，先做「這個東西的分級表要
   怎麼設計成有限選項」的思考，不要讓 AI 在執行期間即興決定數值——這是整個專案最核心的設計哲學。

## 回合選項系統(Phase 5.2)的決策記錄

- **「AI幫每個選項挑屬性+技能組合」是規則書授權的，不是自創玩法**：rules-2.35.txt
  第686~686行明文寫「一次行動究竟關聯何種關鍵技能以及何種關鍵屬性…取決于ST眼中的常識，
  并且可能有多種屬性+技能的組合均合適，**由ST選擇最合適的一種**」。本專案的AI扮演的正是ST，
  所以讓它挑組合是照規則書辦事。分界線是：AI挑「哪一組」，引擎算「挑完之後怎麼算」，
  而且AI挑的東西**必須通過引擎查驗**才算數(見 content/turnOptions.js 的 validateOption)。
- **難度不讓AI講數字，只讓它從五級量表挑一級**：這是最高原則第4條的直接應用。
  規則書沒有通用的難度對照表(它是每個技能各給情境表)，所以五級量表本身是[設計]，
  但DC數值取自書中實際用過的分布(攀爬表DC1/2/3/4/5、平衡表DC1/2/3)。
  書中的DC10/DC20保留給「表面完全光滑」這種極端情境，**刻意不放進AI可選的量表**——
  那種難度應該由劇本資料指定，不是AI臨場決定。
- **查驗策略是「盡量修好」而不是「整批丟掉」**：丟掉一個選項，玩家可選項就少一個，
  體驗損失比「這個選項降級成純屬性檢定」大得多。但每次修正都會出現在回傳的warnings裡，
  前端也會顯示成 SYSTEM.RULE_CHECK 區塊——**不靜靜修掉**，否則你永遠不會發現
  你的AI供應商其實常常在亂挑技能。
- **數量不足時不自己補選項**：補出來的選項文字等於程式碼在編劇情，違反「敘事只能來自AI、
  數值只能來自引擎」的分工。數量不足就如實回報，前端照樣有第五種自訂行動，玩家不會卡住。
- **JSON解析失敗時降級為純敘事**：模型再怎麼交代還是會偶爾包markdown或加開場白，
  解析器容忍這些；但真的解析不出來時**不假裝成功**，而是把整段文字當敘事、選項留空，
  玩家仍可用自訂行動繼續玩。整個回合失敗比這糟得多。
- **chosenOption 從前端送回來時一定在伺服器端重新查驗**：那是前端的東西，玩家隨時可以改。
  查驗成本極低(純比對表格)。**dc欄位一律被忽略、依難度分級重算**，擋掉「把dc改成0讓判定必過」。
  **[已知限制]** 擋不掉「把難度從困難改成容易」——那是量表內的合法值，而伺服器目前沒有回合狀態，
  無從得知AI原本給的是哪一級。要完全防堵需要伺服器端保存回合狀態或對選項簽章，
  等存檔/session機制做出來才能做。以單人遊戲來說改難度只是自己騙自己，先接受，但不假裝防住了。

## P0(記憶／存檔／建卡)的決策記錄

- **AI失憶是先前最嚴重的問題，根因是沒有狀態**：`core/eventLog.js` 早就寫好了，
  但 `/api/narrate` 每次都傳 `recentEvents: []`，等於引擎有記憶體卻沒接上電。
  現在存檔裡有兩份記憶，餵給AI時都要帶上，兩者用途不同、缺一不可：
    - `history`：最近8輪的**敘事原文**。事件摘要只有事實(「判定：躲藏，成功」)，
      沒有語氣、場景細節與NPC說過的話，光靠它AI寫不出連貫劇情。
    - `log`：完整的**事件日誌摘要**（餵最近12筆）。標題明講「事實，不可改寫」，
      防止AI把先前失敗的判定重寫成成功。
  兩者生命週期不同：log只增不減(之後要做日誌回顧、算經驗值、判斷成就)，history只留最近N輪
  (每輪都塞會爆context也燒錢)。混在一起遲早要拆，所以一開始就分開。
- **[已知簡化] 沒有長期記憶**：history超過8輪就掉了，劇情拉長之後AI會忘記很早以前的事。
  正解是做滾動摘要或向量檢索。log仍然完整保留所以資料沒遺失，只是沒餵給AI。
- **存檔用 Cloudflare KV，沒有binding時退到記憶體版但明確標記 persistent:false**：
  記憶體版**只給本機開發用**。Workers每個isolate有自己的記憶體且隨時會被回收，
  正式環境依賴它的話存檔會莫名消失。API回傳 persistent 欄位，前端狀態列會顯示成
  橘色的「未持久化」——寧可讓玩家看到警告，也不能讓他以為有存檔然後某天發現全沒了。
  KV的最終一致性對單人回合制遊戲可以接受；之後若要做多人同場要換 Durable Objects。
- **角色卡一律以存檔裡的那份為準，不吃前端送來的**：否則玩家改一下 localStorage
  就能把屬性改成99。只有在沒有 sessionId 的相容模式下才會用body裡的character。
- **建卡的預算常數由 GET /api/character 提供，前端不自己抄一份**：前端抄常數是很常見的
  bug來源——規則改了但前端沒改，玩家看到的預算跟後端驗的不一樣。每次加點都打一次
  POST /api/character 拿即時回饋，加點合不合法一律由引擎判斷。
- **畫面切換用 style.display 而不是 Tailwind 的 .hidden**：那個class需要Tailwind CSS載入
  才有作用，而這份UI的CSS來自外部CDN。CDN慢或掛掉時，用class的話建卡表單會疊在遊戲畫面上。
  畫面切換是功能不是樣式，不該依賴外部資源。
- **`core/derivedStats.js` 是這次補上的規則書缺口**：先前 `emptyCharacter()` 有 derived.hp
  欄位但沒有人算它，測試都是手動寫 createHpState(20)。建卡要能產生合法角色卡就必須補。
  公式全部有書中出處(生命值=耐力+體積、意志值=決心+沉著+傳奇決心+傳奇沉著、先攻=敏捷+沉著、
  基礎防御=min(敏捷,感知)、敏感範圍=感知×10)。
  **書中提到的「陸行速度/飛行速度」刻意不做**——建卡頁那個欄位是空的，沒有給公式，
  不憑印象補一個上去。
- **[修正] 基礎防御取錯方向**：詳見 TEST_PLAN.md 的「已修正的bug」。簡述：實作成
  max(敏捷,感知)，但規則書衍生屬性段明文是「較低者」。這會讓每個角色防御偏高、
  所有攻擊命中率偏低，是實際影響戰鬥結果的bug。已加跨模組一致性測試，避免兩邊只改一邊。

## 靜默失敗排查(2026-08-16)的決策記錄

起因是一個玩家實際回報的症狀：**每一輪的四個行動選項文字逐字相同**。
追下去發現根本原因不是「AI呼叫失敗」，而是**呼叫成功、但回應內容不能用時被靜默吞掉**：
AI沒照JSON格式輸出 → `parseTurnResponse()` 失敗 → 選項整組退回 `FALLBACK_OPTIONS`
(一組固定文字) → 以 **HTTP 200 `ok:true`** 回傳，跟正常回合長得一模一樣。
這件事只在回應的 `warnings` 陣列裡留一句話，而前端從來沒有讀過 `warnings`，
伺服器端也完全沒有log。於是它可以連續發生幾十輪而沒有任何人發現。

以此為樣板掃過整個專案後，找到同一個模式的另外9處。這裡記錄的是**通則**，
免得之後又長出新的一份：

- **保底內容必須自我標示**。引擎可以墊選項(使用者明確要求版面永遠有四顆按鈕)，
  但墊出來的東西要標記來源(`option.source = "fallback"`)，回應要帶出數量
  (`degraded.aiOptionCount` / `fallbackOptionCount`)，畫面上要看得出來。
  「墊底」跟「AI真的產出」的差別，不可以只有寫程式的人知道。
- **`catch` 區塊裡至少要做一件事**。`catch {}` 與 `catch (err) { return }` 一律視為bug。
  最低標準是 `console.error/warn` 加一個可 grep 的前綴(本專案用
  `[LLM_FAILURE]` / `[LLM_DEGRADED]` / `[SCENARIO_SETTLEMENT_FAILED]` / `[RESUME_FAILURE]` 等)。
- **警告要分受眾**。給開發者的(進 console)與給玩家的(進畫面)是兩件事，
  全部丟進同一個 `warnings` 陣列的結果就是兩邊都沒人看。副本結算被擋下、
  最終戰沒結算成獎勵，這些是玩家要知道的，走 `scenario.warnings`。
- **規則層算出來的東西一定要有呼叫端在讀**。`core/health.js` 的 dead/unconscious 旗標
  與 `core/deathAndRevival.js` 的整套復活規則，寫得完整、測試也全綠，
  但在 functions/ 與 content/ 底下**一次都沒有被引用過**——玩家在戰鬥中被打死之後照樣
  可以繼續選選項、擲骰、推劇情。這是這次找到最嚴重的一項：規則層算對了，結果沒有生效，
  而且畫面看起來一切正常。修法是新增 `content/downState.js` 當接線層 + `/api/revive` 端點。
- **閘門一定要配一條出路**。加傷勢閘門擋住死亡角色的同時就要把復活流程接上，
  否則只是把「靜靜地用死角色繼續玩」換成「靜靜地卡死」，兩種都不能接受。
- **前端錯誤處理跟後端錯誤回應是同一件事的兩半**。後端回了漂亮的 502 + 錯誤訊息，
  前端不看 `res.ok` 的話等於沒回。`/api/turn` 的敘事失敗以前就是這樣：
  前端照常往下跑，玩家只看到「本回合無預設選項」，會以為那是遊戲設計。

`content/downState.js` 的復活費用有一個**誠實記錄的近似**：
`core/deathAndRevival.js` 的公式需要「建卡分數帳本」，而 `core/schema.js` 目前只有
`xp:{earned,spent}`。所以 `buildRevivalWallet()` 用經驗值帳本代打，並在檔案裡寫明
「這不是書中原意的那個數字，等點數帳本做出來只要改這一個函式」。
不假裝已經做完，也不因為缺一塊資料就整個不做。

## 成敗差異與開場體驗(2026-08-16)的決策記錄

起因是一份實際測玩回饋，三個症狀：

1. 「骰子成功和失敗造成的影響沒有什麼決定性差異，我成功了也會引來異形探查，
   失敗了AI就描述我可以再潛行一次，而沒有更強烈的戲劇張力」
2. 「要撰寫一個固定的故事開頭，涵蓋玩家初次出現在副本內的茫然和周圍環境的警惕」
3. 「按下按鈕也要有反饋，我一開始還以為是網頁當掉，結果只是回覆時間太久」

### 症狀1：成敗沒有差異 —— 這不是文筆問題，是狀態問題

在這之前，一次判定的成敗**唯一**的下場是 `core/narration.js` 給AI的一句語氣指令。
那句話沒有任何持續性：不會寫進存檔，下一回合的 prompt 也看不到它。於是第三回合的
處境跟第一回合完全一樣，玩家當然感覺不到差別——**把語氣指令寫得再兇也修不好，
因為問題不在AI怎麼寫，而在世界狀態根本沒變。**

修法分兩層，兩層缺一不可：

- **引擎層(新)**：`content/scenario/threat.js` 的迫近度軌。0~6 的整數，存在
  `session.scenario.progress.threat`，每回合依判定分級**查表**增減
  (大成功 -2 / 成功 -1 / 驚險成功 +1 / 些微失敗 +1 / 失敗 +2 / 慘烈失敗・大失敗 +3)。
  數值傾斜是刻意的：一次乾淨的成功還得起一次普通失敗，還不起一次嚴重失敗。
  四個階段(潛伏/追蹤/貼近/接觸)各自帶一段**完全不同的強制敘事指令**，階段一變，
  AI 收到的要求就整段換掉，場景寫不回原樣。撞到 6 就是「接觸」：前端開放戰鬥按鈕，
  而且 `/api/combat/start` 會改用副本自己的 `threatEncounter` 樣板開戰
  (不是通用佔位雜魚——否則整條軌道在兌現的那一刻穿幫)。分工照舊：**增減多少、
  現在是哪一階段全部由引擎查表，AI 一個數字都不碰**，AI 只負責把既定事實寫成畫面。
- **提示層**：`core/narration.js` 的六級指令從「語氣描述」改寫成「必須兌現的結果」，
  每一級明講得到什麼、失去什麼、以及**不准出現什麼收尾**。最關鍵的是那句禁令——
  「失敗之後禁止讓玩家用同樣的方法再試一次同一件事」。正面要求AI很容易只照做一半，
  禁令才擋得住它把場景寫回原點。

`test/threat.test.js` 鎖住的是「同一個起點，成功與失敗一定走向不同的迫近度」
與「每個判定分級都必須有對應的增減值」——後者是防止靜默失效：分級字串跟
`core/narration.js` 對不上時 `threatDeltaForOutcome()` 會回 0，那個分級的成敗
從此對世界沒有任何影響，而且不會報任何錯。

### 症狀2＋3：固定開頭 —— 一個改動同時解決兩件事

副本章節可以帶 `openingNarration`(固定開頭全文) 與 `openingOptions`(配套四選項)。
`/api/turn` 在「開場模式 + 這場遊戲還沒有任何歷史 + 章節有這兩個欄位」時直接短路回傳，
**完全不呼叫 LLM**。理由是兩個問題剛好同源：

- 品質：開場是玩家對副本的第一印象，不該每次進來都賭模型今天寫得好不好。
- 延遲：開場本來是玩家等最久的一次呼叫(建完卡 → 空白畫面 → 等三十秒)，現在是純本地回應。

玩家的第一個選擇之後就完全回到正常流程，AI 照樣接手，功能沒有被拿掉任何一塊。
固定選項一樣要過 `validateOption()`，不因為是人寫的就跳過規則查驗；它們標記為
`source: "scripted"`，跟保底選項的 `"fallback"` 分開——把作者寫的選項標成「保底」
等於對玩家說謊，也會讓真正的保底警告失去意義。

### 症狀3的另一半：等待狀態必須看得見

`public/app.js` 的 `runTurn()` 從按下按鈕到 fetch 回來為止，畫面上原本**沒有任何變化**：
選項還在(還可以再按)、沒有載入指示、d20 動畫要等回應回來才播。一個跑 20~40 秒的請求
配上完全靜止的畫面，看起來就是當掉了——玩家的判斷沒有錯，是介面沒告訴他真相。現在做三件事：
按下當下鎖住選項並標記按下的那一顆、在故事流插一個會動的「說書人書寫中」區塊(含計時)、
超過 15 秒補一句「較慢的模型需要 30 秒以上，畫面沒有當掉」。解鎖與收起指示一律放在
`finally`：任何一條失敗路徑漏掉，玩家就會卡在一個永遠轉不完的動畫前，比原本沒有指示更糟。

### 新手副本選《異形》(1979) 的理由

使用者把「系列哪一部」交給實作決定。第一部是整個系列裡結構最單純的一部——一艘封閉的船、
一隻怪物、一條逃生路線。新手副本要教會玩家的是「潛行會被發現、失敗會累積、時間會用完」
這三件事，場景越單純，這三件事越清楚。後面幾部(殖民地、實驗室)的複雜度會蓋掉教學意圖。

Boss 數值有一個值得記下來的坑：第一版照「牠在電影裡有多強」寫(敏捷3/感知3/護甲2)，
算出來防禦DC=5，新手角色的 6 顆骰要全中才打得到，而且打中還會被護甲吃光——
整場戰鬥在數學上是打不完的，是整合測試跑滿 300 回合都分不出勝負才抓到。
**寫敵人樣板前要先用 `core/combat/defense.js` 的公式算一次**，否則寫出來的是一個
看起來很帥、實際上無法通關的副本。現在的數值(感知2、護甲1)跟 echoInstitute 的最終戰同一級，
而且「感知低」有原作依據：牠沒有眼睛，靠熱與震動獵食。

## 單一檢定通關與定向(2026-08-16)的決策記錄

第二輪測玩回饋，兩個症狀（用 Gemini 3 Flash 實測）：

1. 「在AI的回覆中，我完全摸不清故事背景，也不理解現在具體要幹嘛？
   我就是看選項哪個數字高就按哪個」
2. 「只要玩家把單一屬性+單一技能點高，就能一直用同一個檢定通關，
   因為我的敏捷+潛行最高，我每次都按相關的選項」

### 這兩件事其實是同一件事

第2點是第1點的**後果**，不是獨立的平衡問題。玩家之所以退化成比較骰池大小，
是因為版面上只有數字是清楚的：選項只寫了一個動作（「搬移伺服器機櫃封鎖房門」），
沒寫**做這件事想得到什麼**；HUD 的「當前目標」寫的是節點標題（「母親的特別指令」），
那是給副本作者看的索引，對還沒玩到那裡的玩家就是一句謎語；敘事本身只寫當下這個房間，
玩家玩到第五回合早就忘記自己在哪艘船上。三件事加起來，故事在畫面上是缺席的，
唯一可比較的東西就只剩 DP 數字——玩家的行為完全理性，是介面逼他這樣玩的。

所以修法分兩路：**把故事放回畫面上**，以及**讓重複的最佳解有代價**。

### 一、把故事放回畫面上（全部是作者寫死的資料，不靠AI每回合重講）

- `pack.briefing`：副本簡介（船名／前提／目標／警告），常駐在畫面上可收合。
  世界觀骨幹交給AI每回合重講會愈講愈歪，而且很浪費篇幅。
- `node.playerGoal`：一句玩家看得懂的當前目標，取代HUD上的節點標題
  （標題退成 tooltip）。同一句話會餵進 prompt，並要求AI的敘事必須跟它對得上——
  只寫在HUD上不夠，敘事完全不提的話玩家還是接不起來。
- 選項多一個 `hint`（15字內，「做這件事想得到什麼」），寫進 `TURN_RESPONSE_SCHEMA` 的
  required，由供應商端保證它一定出現。版面同步重排：label 與 hint 是主體，
  檢定組合／DC／骰池退成最後一行的灰字。**最醒目的東西從數字換成了目的。**
- prompt 加一段「定向要求」：每段敘事都要讓一個中途才開始看的玩家搞清楚
  人在哪、在追求什麼、下一步往哪走；只有氣氛就算寫壞了。

### 二、讓重複的最佳解有代價（兩端一起改）

- **遊戲端**：`content/scenario/repetition.js` 的套路遞減。同一個「屬性＋技能」
  連續第3次 +1 DC、第4次 +2、第5次以後 +3（封頂），換一招立刻歸零。
  懲罰在擲骰前加進DC，並且**在玩家按下去之前**就標在選項上——按完才知道等於是在罰他。
  餵給AI的指令要求把它寫成「世界學會了這一招」（守在你慣用的路線上），
  而不是「玩家變累了」：前者是世界在回應你，後者是系統在說你不行。
  用「屬性＋技能」當指紋而不是只用技能，因為 敏捷+潛行 跟 意志+潛行 是兩種做法；
  純屬性檢定也有指紋，否則玩家可以靠「不帶技能」繞過整個機制。
- **建卡端**：配點成本從線性改成遞增（屬性 1/1/2/3、技能 1/1/2），預算同步調高
  （6→8、8→10）讓四個內建模板的配點維持不變。線性成本在骰池制底下不是一個平衡的選擇，
  是一個沒有理由不選的最佳解。改完之後把單一屬性點到5要花掉8點裡的7點，
  專精仍然可行也仍然強，但玩家會清楚感覺到那是拿其他五個屬性換來的。
  加點按鈕旁邊直接標出「下一點要幾點」，遞增曲線才是可以規劃的取捨而不是挫折來源。
- `buildOptionsSpec` 的選項守則也跟著改：**四個選項的技能必須全部不同**（原本只說
  「不要四個都用同一個技能」），而且有訓練的技能最多出現在兩個選項裡（原本是「至少兩個」
  ——那條規則本身就在保證玩家每回合都找得到自己的最強組合）。

### 一個提醒給之後改這裡的人

套路懲罰的預告（選項上的標籤）與實際擲骰時加的DC，必須來自**同一個函式**
（`retreadPenalty()`）。兩邊各算一次遲早會對不上，而「按下去之前看到DC3、按下去變成DC4」
是所有設計失誤裡最傷信任的一種。

## 建卡改成生平問答(2026-08-16)的決策記錄

使用者的要求（逐字）：

> 「我想把目前預設的背景故事拿掉，要透過一些更有代入感的方式讓玩家可以逐步建立一個他心目中的
> 角色，故事要豐富(且符合無限恐怖的設定，每個人都是現代普通人)，這樣屬性跟技能都改成後台，
> 透過建卡系統自動幫玩家分配好」

### 舊建卡錯在哪裡（不只是「不夠有代入感」）

四個身分模板（特戰隊員／武道極限／科技專家／戰地軍醫）＋二選一背景故事，那是**選職業**，
不是建角色。更關鍵的是它跟《無限恐怖》的前提直接衝突：主神空間抓進來的是**現代普通人**，
第一部片裡的人不是特種部隊，是上班族、學生、司機。「你原本是特戰隊員」把這個世界觀最重要的
那個落差先削掉了一半。所以模板資料是**刪掉**而不是留著不用——留著等於留一條隨時會被叫回來的舊路。

### 現在的做法

六題關於「被拉進來之前的日常」的問答（`content/chargen/lifePath.js`）：職業、成長環境、
一件沒說過的事、最接近死亡的一次、別人怎麼形容你、突發危險時的第一反應。一次只顯示一題——
六題攤在同一頁會變成問卷，玩家會用掃的；一次一題他才會真的讀完四個選項，而那四個選項就是這個角色。

每個選項帶三樣東西：一段**小傳片段**（會被接成角色背景）、一組**傾向權重**、
以及選填的**性格特質**（純敘事型，餵給AI）。權重由 `content/chargen/allocate.js` 用
「性價比貪婪法」換成實際配點：每一步買「權重 ÷ 這一級的成本」最高的那一級，
因為成本遞增，同一條線買到後面性價比自然下降，不需要另外寫「要平均分配」的規則。

寫題目的三條原則寫在 `lifePath.js` 檔頭，之後要加題目請照著寫，否則會慢慢退回「選職業」：
每個選項都必須是普通人的日常、每個選項都要能往兩個方向解讀（不能有最強解）、
story 片段要能直接接成通順的第二人稱小傳。

### 自動配點的三個硬性質（每一個都對應一次踩過的坑）

- **決定性**：同樣的答案一定得到同樣的卡。所有排序都有明確 tie-break，不依賴物件屬性列舉順序，
  更不可以擲骰——建卡結果會進存檔也會被測試鎖住，隨機化會讓「為什麼跟上次不一樣」變成查不出來的問題。
- **不極端**：屬性上限鎖 4（建卡規則本身是 5），而且**最多一個屬性到頂**。
  第一版沒有後面那條，權重最高的兩個屬性各吃掉 4 點，其餘四個全部停在 1，
  配出來是一張耐力1（生命值6）的玻璃大砲。玩家答的是六個關於人生的問題，不是「我要玩極端build」。
- **保證廣度**：配完至少 4 個技能 ≥1。第一版是「先貪婪、剩下的補廣度」，結果廣度保障幾乎永遠不生效
  ——貪婪法會把預算花光（潛行3+偵察3+格鬥2 剛好 10 點），補廣度那一步拿到的 remaining 是 0。
  保障要有效就必須**先付錢**：先把權重前四高的技能各買到 1 級，剩下的預算才交給貪婪法。
  這是順序問題，不是門檻問題。

測試用**全走訪**而不是抽樣（6×4×4×4×4×4 = 6144 種組合全跑）：組合數還在跑得完的範圍內，
而「某一組答案會配出壞卡」正是抽樣永遠抓不到、但玩家一定會踩到的那種問題。

### 順手接上的一條斷線：角色背景從來沒有送給AI過

玩家在建卡時決定的背景寫進 `character.concept.background`，然後就停在那裡——
整個 `functions/` 底下沒有任何地方讀它。玩家花時間拼出來的那個人，說書人從來不知道他是誰。
現在它進 `buildDmMemo()` 的主角信息表（來歷是**事實**，敘事不可與之矛盾；
「他容易對什麼有反應」那類傾向仍然走文筆層的 characterHints，兩者分層不變）。

## 專業(specialization)系統：決定不做(2026-08-16)

排查「規則簡化做到哪了」時查到的一條**會說謊的斷線**，記在這裡免得之後有人重新接上一半：

`content/turnOptions.js` 會查驗 AI 給的 `specialization`、把它塞進
`optionToCheckParams()`，一路傳進 `performCheck()`——但 `core/check.js`
**從頭到尾沒有讀過這個欄位**。也就是說選項帶不帶專業，判定結果完全一樣。
更糟的是 `validateOption()` 的警告文字寫著「引擎會依規則將技能等級減半」，
而那個減半規則在整個專案裡不存在。測試那邊也有三則 `test.skip` 等著有人來實作。

兩條路（接上／拆掉）選了**拆掉**：建卡已經改成後台自動配點，玩家沒有任何購買專業的入口，
接上等於為一個取得不到的資源實作一整套規則。所以現在 `validateOption()` 明確丟棄這個欄位，
schema 也不再宣告它，三則 skip 換成兩則**會執行**的測試，鎖住「帶不帶專業，骰池與 note 必須一模一樣」。
留著 skip 的測試等於留著一個「總有一天要做」的暗示，而那不是現在的決定。

規則書原文的專業機制仍記錄在 `RULES_DIGEST.md` 的「已知落差」，要做的時候從那裡接回來——
接的時候三層要一起做完：`INTENT_TABLE` 的表定專業、`performCheck()` 的減半、以及玩家取得專業的入口。
