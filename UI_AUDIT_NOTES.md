# AI-TRPG UI 版面稽核筆記

## 使用者提供的問題

使用者指出目前色彩配置大致順眼，但桌面寬螢幕下左右留白過大，中央故事文字區被壓縮到幾乎無法正常閱讀；同時各項 HUD、敘事流與行動選項的位置和尺寸仍未找到更好的配置。

## 目前前端結構

`public/index.html` 的遊戲畫面位於 `#app-viewport`，使用 `flex-1 flex overflow-hidden w-full max-w-7xl mx-auto`。子項依序是固定寬度的 `#char-hud`、中央 `main`、故事流 `#story-feed`、底部 `#story-action-panel`。目前 `#char-hud` 在桌面為 `w-72 md:w-80`，主畫面本身受 `max-w-7xl` 限制；故事流則設定 `padding-left/right: clamp(1rem, 5vw, 4.5rem)`，每則事件還限制 `max-width: 62rem`。

## 截圖與瀏覽器觀察

使用者截圖為約 1919×923 的寬螢幕畫面。視覺上最重要的問題是遊戲容器沒有充分利用寬度：左側角色欄、中央遊戲欄與外部空白的比例不平衡；故事流可閱讀寬度受到多層 max-width、欄位與 padding 同時限制。底部選項面板高度偏高，進一步壓縮可看見的故事內容。

本地瀏覽器實測 viewport 為 1280×1100；目前入口與建卡頁可正常渲染。靜態預覽無法呼叫 `/api/character`，因此建卡流程顯示後端 JSON 錯誤，但這不影響純 CSS/HTML 版面稽核。

## 初步設計方向

1. 遊戲畫面採用「內容優先」的三區結構：左欄改為較窄且可收合的狀態 rail；中央故事區分配主要寬度；底部決策區固定但降低視覺與垂直佔用。
2. 桌面主容器不應再使用過窄的固定 `max-w-7xl`；改成接近全寬並保留合理外距，例如 `width: min(100%, 1680px)` 或 `max-width: none` 搭配 24–32px 內距。
3. 故事正文要有舒適閱讀行長，但外層應先取得足夠寬度，再讓正文本身控制在約 68–78ch，而非讓整個中央欄變窄。敘事內容和事件型系統訊息可以使用不同 max-width。
4. 行動選項應維持兩欄，但卡片降低內距與底部資訊高度；自訂行動輸入列應與選項同一視覺層級，不再讓底部面板看起來像第二個主畫面。
5. 需要測試桌面寬螢幕與中等寬度，並保留手機抽屜行為。

## 第一版實作狀態

已在 `public/index.html` 的 `<style>` 末端加入桌面專用 `STORY-FIRST LAYOUT` 規則。它只在 `min-width: 768px` 生效，將 `#app-viewport` 的最大寬度從 Tailwind `max-w-7xl` 的 1280px 提高到 1760px，角色欄改為流動寬度，故事正文上限提高到 70rem，系統事件上限為 58rem，決策卡最小高度由 6.85rem 降到 5.35rem。

快取破除後確認瀏覽器已載入新版 CSS：`#app-viewport` 的 computed `max-width` 為 1760px，桌面 media query 命中；下一步需在同一測試狀態下重新建立故事與選項內容，確認新版實際高度與視覺比例。

## 第一次本地視覺驗證

在 1280×1100 的本地瀏覽器中，套用第一版 CSS 後，工作區寬度從 1280px（原本也是滿寬但受 1280px max-width 限制）維持可用寬度，角色欄由固定 320px 降為 256px，中央主欄由 960px 增為約 973px；故事流可視高度由約 583px 增為約 634px，底部操作面板由約 352px 降為約 301px。

進一步測試四欄決策卡後，操作面板高度降到約 210px、故事流增至約 725px；但 1280px 的中央欄每張卡只有約 224px，文字密度偏高。因此最後將四欄斷點從 1280px 提高至 1536px：真正的寬桌面（例如使用者提供的約 1920px 畫面）採四欄同列，中型桌面仍維持 2×2，兼顧閱讀與操作。

已執行 `git diff --check` 與完整 `npm test`，目前 772 個測試全部通過。

## 最終驗證

最後版本在 1280×1100 的本地 viewport 量測為：角色欄約 256px、中央主欄約 973px、故事流約 634px 高、底部操作面板約 301px 高，決策卡為 2×2（每張約 457px 寬）。在 1536px 以上的寬桌面，決策卡改為 4 欄同列；在使用者約 1920px 的畫面預期能大幅降低底部面板的垂直佔用。

這次實作維持既有手機規則：`max-width: 767px` 的角色欄抽屜、單欄故事流與手機選項配置不受桌面覆寫影響。

## 第二輪預覽載入備註

已將第二輪 HTML/CSS 與 `public/app.js` 修改完成，並通過 `node --check public/app.js`。本地瀏覽器以 `?round=2-layout` 載入時，預覽腳本可以執行部分 DOM 操作，但瀏覽器 console 顯示 `updateStoryFeedView is not defined`，表示目前頁面仍載入了舊版 `app.js`（或瀏覽器快取中的舊腳本），不是新檔案的語法錯誤。下一步會用版本化腳本引用或重新開啟頁面後再驗證。

## 第二輪功能載入驗證

已將 `public/index.html` 的 app.js 引用加上 `?v=20260822-r2` 版本參數，解決本地瀏覽器沿用舊版腳本的快取問題。重新載入後確認：`updateStoryFeedView` 與 `appendFeedEvent` 均成功載入，故事流工具列存在，三個篩選按鈕均已建立。

## 第二輪互動驗證

使用多回合測試資料驗證新的故事流功能：共建立 8 則事件與 2 個回合分隔，工具列正確顯示「8 則紀錄」，篩選按鈕初始為「全部」，故事流回到較上方時「最新」按鈕會顯示。

決策卡驗證為 4 張，主文字與 hint 分離，規則摘要改為「屬性＋技能 · 難度 DC」的主規則資訊，骰池與未受訓風險退為次要資訊；純敘事選項則顯示「純敘事行動／不擲骰 · 場景仍會推進」。1280px 測試 viewport 仍為 2×2 選項配置，操作面板約 301px 高，未破壞上一輪的桌面比例。

第二輪預覽截圖顯示：故事流已能以回合邊界閱讀，工具列固定在敘事區頂部，決策卡的主要標題、結果提示與規則摘要有明確層級。

## 敘事篩選驗證

點擊「敘事」後，故事流仍保留 8 則紀錄，但可見事件只剩 3 則 narration；divider、action、check、world 與 harm 均以 hidden 狀態保留在 DOM 中。工具列的「敘事」按鈕正確標記 active，紀錄總數仍顯示 8 則，避免玩家誤以為篩選會刪除事件。

## 完整事件流切換驗證

從「敘事」切回「全部」後，8 則事件與 2 個回合分隔均重新顯示，最新定位按鈕恢復可見；完整事件順序保持不變。預覽中可以看出玩家行動、判定、世界變化、傷害與說書人敘事已能沿著同一條時間線區分。

## 事件篩選驗證

點擊「事件」後，敘事事件 3 則被隱藏，回合分隔、玩家行動、判定、世界變化與傷害事件共 7 個元素保留可見；總紀錄仍為 8 則。這符合「事件」檢視用於快速確認規則結果與局勢變化的設計目的。

## 長內容驗證

以四個長選項與長提示測試後，1280px viewport 下決策卡沒有橫向溢出，選項網格與故事流也沒有水平溢出；長標題自然換行，兩張檢定卡高度約 121px，純敘事與一般卡約 103px，規則摘要仍維持在卡片底部。

## 最後腳本驗證

已確認瀏覽器目前載入 `app.js?v=20260822-r2b`，`updateStoryFeedLatestButton` 存在，故事流工具列可用；「最新」按鈕在跳轉時會依 `prefers-reduced-motion` 選擇平滑或立即捲動。

## 錯誤事件路徑驗證

在最新 `app.js?v=20260822-r2c` 中建立 narration 與回合失敗事件後，故事流計數正確為 2 則，fault 事件可見，重試按鈕仍存在，且沒有水平溢出。這條特殊錯誤路徑已改為直接使用 `buildFeedEvent` 建立的節點，讓篩選與計數規則一致。
