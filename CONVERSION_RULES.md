# 型錄轉換規則 —— 給接手的 AI 與人類

**先讀這一段。** 你要把 `rules-2.35.txt` 的資源型錄（500+ 頁）變成這個網站賣得出去的商品，
而這件事**不是搬字**。這個專案的規則已經被大幅簡化過，型錄條目寫的是完整版規則的語言，
兩邊的詞彙對不起來。這份文件規定了「對不起來的時候該怎麼辦」。

規則有兩種強度，請分清楚：

- **【硬性】** —— 不可違反。違反的話 `content/shop/catalog.js` 的 `validateGood()` 會擋下來，
  或 `test/shopPacks.test.js` 會紅。這些不是建議。
- **【判斷】** —— 需要你動腦，但必須把判斷過程寫進商品的 `conversionNote`。

相關檔案：轉換的守門員是 `content/shop/effects.js`（效果詞彙表）與
`content/shop/catalog.js`（商品驗證）；成果在 `content/packs/shop-starter-*.json`；
測試在 `test/shopPacks.test.js`。已經簡化好的規則本身列在本文件下半部的
**「第二部分：目前的簡化規則總表」**。

---

# 第一部分：轉換規則

## 0. 三條總則

1. **【硬性】不杜撰。** 商品的每一個數字都必須來自 `rules-2.35.txt` 的原文，或來自
   `content/templates.js` 的模板欄位。書上沒寫數字的東西，不准自己給一個。
2. **【硬性】每一件商品都要留得下回頭核對的路。** `sourceRef` 必填，而且要有頁名與行號。
3. **【硬性】轉換是有損的，損耗必須被記下來。** 丟掉的特性進 `droppedTraits` 並寫理由；
   放寬的條件寫進 `conversionNote`。**不允許默默丟掉，也不允許默默放寬。**

## 1. 每一個原文特性只有三種下場

拿到一條「特性：XXX」，它只能落到以下三格之一。沒有第四格。

| 下場 | 寫在哪 | 條件 |
| :--- | :--- | :--- |
| **轉成效果** | `effects[]` | 能落進 `EFFECT_KINDS` 的 15 種效果之一 |
| **降級成敘事** | `effects[]` 裡 `kind:"敘事"` | 有故事價值但沒有數值後果 |
| **丟棄** | `droppedTraits[]` | 引擎沒有這個機制，`reason` 必填 |

**【硬性】絕對不允許的第四種下場**：把它寫成一個新欄位塞進 JSON，看起來有效，
但沒有任何程式碼會讀它。這個專案已經為此付出過代價——見 `ARCHITECTURE.md` 的
「專業(specialization)系統：決定不做」，那是一整套寫好、測好、但從頭到尾沒有生效的規則。
**一個沒有消費端的欄位就是一個謊言，而且測試通過會讓謊言看起來像功能。**

## 2. 效果詞彙表：15 種，就這麼多

完整定義在 `content/shop/effects.js` 的 `EFFECT_KINDS`。每一種都寫明了它接到引擎的哪個欄位。

| 效果 | 必要欄位 | 接到哪 |
| :--- | :--- | :--- |
| `屬性` | attribute, amount | `character.attributes[attr]`，購買當下永久寫入 |
| `技能` | skill, amount | `character.skills[skill]`，購買當下永久寫入 |
| `生命上限` | amount | `derived.hp.max`（多出來的以「完好」入帳） |
| `意志上限` | amount | `derived.willpower.max` |
| `先攻` | amount | `combatProfile.initiativeBonus` → `turnOrder.rollInitiative()` |
| `防御` | amount | `combatProfile.equipmentDefense` → `defense.computeDefenseDC()` |
| `護甲` | amount | `combatProfile.armor` → `armor.applyArmor()` |
| `檢定加骰` | amount ＋匹配鍵 | `check.performCheck()` 的 `otherDpModifier`／`resolveCombatAction()` 的 `attackDpModifier` |
| `附加成功` | amount ＋匹配鍵 | `check.performCheck()` 的 `otherBonusSuccesses`／`attack.js` 的 `attackBonusSuccesses` |
| `武器` | label, attackType, weaponDamage | 背包 → `combat/attackTypes.js` |
| `治療` | severity, amount | `health.healDamage()`，使用當下才套用 |
| `好感度` | amount | `affection.applyAffectionDelta()` |
| `敘事` | text | 無數值，只送給 AI 當背景 |
| `型態` | label, activation, duration, grants | `content/shop/forms.js` 的進行中型態 → 期間內併進上面同一張表 |
| `能量池` | pool | `character.derived.energyPools` → `core/energyPools.js`（上限＝關鍵屬性之和） |

**【硬性】不可以自己加第 16 種。** 什麼時候可以擴充，見第 8 節。

### 2a. 型態（暫時性增益容器，2026-08-17 新增）

變身／爆發／開眼這類「付代價換一段時間的強化」。血統整類的價值幾乎都是這個形狀。

```json
{
  "kind": "型態",
  "label": "進化形態",
  "activation": { "action": "移動", "willpower": 1 },
  "duration": { "unit": "場景" },
  "grants": [{ "kind": "防御", "amount": 3 }]
}
```

四條【硬性】規定，全部由 `validateEffect()` 擋、由 `test/shopForms.test.js` 守：

1. **必須有啟動成本**（意志力、能量池、或一個動作等級，至少一樣）。沒有代價的型態沒有理由不永遠開著。
   啟動時 `forms.js` 會真的扣 `character.derived.willpower.current`——這是意志力的**第一個消費端**。
   吃能量池的寫成 `activation: { pool: { name: "劍氣", amount: 1 } }`（2026-08-17 新增）。
2. **必須有期限**，而且 `duration.unit` 只能是 `輪` 或 `場景`。輪數可以寫死（`rounds: 3`），
   也可以掛在一個屬性上（`roundsFromAttribute: "感知"`，對應書上大量的「持續你的感知值*1輪」）。
   這兩個單位是引擎裡僅有的兩個真的在前進的時鐘（`combat.round` 由 `encounterState.advanceTurn()`
   遞增；場景結束由呼叫端宣告）。
   書上的「10 分鐘」「每天一次」沒有東西可以倒數它們，寫進去就是一個永不過期的型態。
3. **`grants` 只能是查詢型效果**：`防御／護甲／先攻／檢定加骰／附加成功／武器／敘事`。
   `屬性／技能／生命上限／意志上限` 是購買當下寫進角色卡的，型態結束時收不回來；
   `治療／好感度` 是發生一次的事件，放進會反覆開關的容器裡會變成無限回血。
4. **型態不可以巢狀。**

**「每場景一次」不需要另外做次數限制**：一個持續整個場景的型態，本來就不可能在同一個
場景裡啟動第二次（`forms.js` 的「已在進行中」擋住）。已經這樣處理過：帝國子民。

## 3. 條件式效果的紅線（最重要的一條）

書上大量效果帶有情境條件：「位於酒館時」「對沒有盔甲的目標」「每天一次」「變身後」
「若目標體積小於你」。

**【硬性】這些一律不准轉成數值效果。**

理由是本專案最高原則第 4 條：引擎判斷不了這些條件，照收就等於讓 AI 在敘事時決定加值
有沒有生效——那就回到了「AI 自己編數字」，整個專案就是為了不要那樣才存在的。

因此 `檢定加骰` 與 `附加成功` **只接受 `attribute` / `skill` 當匹配鍵**，而且一律常態生效。
`validateEffect()` 會擋下沒有匹配鍵的檢定類效果。

### 3a. 匹配鍵的兩個維度（2026-08-17 擴充）

**複數鍵**：書上常寫「所有基於生理系屬性的檢定」。用 `attributes: ["力量","敏捷","耐力"]`
（技能同理用 `skills`）把那一組鍵一次寫齊。**這不是放寬匹配**——每一個鍵仍然必須是六維十技能之一，
仍然完全不需要臨場判斷。單數 `attribute` / `skill` 照舊可用。

**`scope`**：型錄裡「攻擊檢定 +X」跟「求生檢定 +X」是兩種不同的東西。

| scope | 生效範圍 | 查表函式 |
| :--- | :--- | :--- |
| `檢定` | 只有一般檢定 | `checkModifiersFor()` |
| `攻擊` | 只有戰鬥攻擊 | `attackModifiersFor()` |
| `全部`（預設） | 兩邊都算 | 兩個都查 |

攻擊用的匹配鍵來自 `core/combat/attackTypes.js` 的 `ATTACK_CHECK_KEYS`——也就是那種攻擊方式的
**公式裡本來就在用的那兩個欄位**（肉搏＝力量+格鬥、炮＝智力+射擊…），不是另外發明的對應關係。
所以第 5 節仍然完全成立：個別條目不可以改寫攻擊公式，只能在公式算完之後外加調整。

碰到條件式效果時，你的選擇只有三個：

- **【判斷】放寬成常態生效** —— 只有在「條件在單人遊戲裡幾乎永遠成立」或
  「條件對映到引擎根本沒有的欄位（例如性別）」時才可以，而且**必須在 `conversionNote`
  裡寫明這是放寬**。已經做過的例子：「處女座」的社交加值原本限女性佩戴。
- **【判斷】條件是「變身／開眼／爆發期間」時，包成型態** —— 見第 2a 節。
  這**不是把紅線放寬，是換了一個守門員**：紅線的理由是「不能讓 AI 決定加值何時生效」，
  而型態的開關是引擎自己持有、自己倒數、自己到期的狀態，裁判從 AI 換成了程式碼。
  代價是型態必須付出啟動成本並且會過期——這也是為什麼型態內部的 `檢定加骰` 可以省略匹配鍵
  （「變身期間所有檢定 +1」是書上原文，而引擎算得出來），型態外面的仍然不行。
- **丟棄** —— 進 `droppedTraits`。

## 4. 技能與屬性的對映（22技能 → 10技能，9維 → 6維）

**【硬性】** 商品裡只能出現六維與十技能的名字，寫原文的「白刃」「決心」會被 `validateEffect()` 擋下。

已經用過的對映，**新的轉換請沿用，不要每次重新發明**：

| 原文 | 本專案 | 備註 |
| :--- | :--- | :--- |
| 白刃、肉搏 | 格鬥 | |
| 槍械、弓 | 射擊 | |
| 運動 | 體魄 | |
| 潛行、盜竊 | 潛行 | |
| 求生、野外求生、追蹤、動物溝通 | 求生 | |
| 調查、感受、察言觀色 | 偵察 | **調查與感受都併到偵察**，於是有些專長的低階效果會重複，這是簡化的必然結果 |
| 手藝、電腦、爆破 | 技藝 | |
| 醫療 | 醫療 | |
| 學識、神秘學 | 秘識 | |
| 交際、說服、脅迫、掩飾、易容 | 交涉 | |

**九維裡的決心／風度／操控／沉著在六維裡不存在。** 效果掛在這四個屬性上的條目，
該效果一律丟棄（`reason` 寫清楚是哪一個屬性）。

**子項 → 整個技能是一次放寬。**【判斷】書上常寫「求生（追蹤）相關檢定 +2」，
簡化規則沒有子項，所以加值範圍會從一個子項擴大到整個技能。這種放寬**可以做**，
但要在 `conversionNote` 寫明。已經這樣做過：方向感、程序猿、饕餮盛宴。

## 5. 攻擊公式不可以被個別條目改寫

**【硬性】** `core/combat/attackTypes.js` 的七種攻擊方式（肉搏／白刃／投擲_輕／投擲_重／
弓箭／槍械／炮）各有固定的 DP 公式（關鍵屬性 + 技能 + 武器傷害）。

書上很多條目會寫「本武器可用敏捷代替力量」「攻擊檢定為敏捷+神秘學」。
**這些一律丟棄**，武器只轉「武器傷害」「射程」「攻擊方式分類」三樣。
否則七種攻擊方式的公式表就形同虛設，每件武器都變成一條特例。

已經這樣處理過：審判眼的龍骨炮（原文敏捷+神秘學，改走「炮」的智力+射擊）、
京都神鳴流（敏捷代替力量）、最蔡之人的籃球（風度代替敏捷）。

## 6. 價格：照抄書上的，不照模板改

**【硬性】書上寫多少就是多少。** `content/templates.js` 的模板價是**稽核基準**，不是覆蓋依據。
`auditGoodPricing()` 會回報落差，落差本身是要保留的事實。

已知的落差（都保留原數字，有測試鎖定）：

| 條目 | 書上 | 模板 |
| :--- | :--- | :--- |
| 改造類三個 D 級（假面騎士EVOL 等） | D+1000 | 500 |
| 太玄鑲華劍譜（技藝） | D+600 | 500 |
| 阿尼馬格斯（法術） | D+1000 | 500 |

**【判斷】書上完全沒寫價格時**，才用模板價填上，並在 `pricingNote` 寫明「這是用模板補書中留白，
不是覆蓋書上的數字」。已經這樣做過：阿蘭斯、寫輪眼、夜兔。

**價格標記法**由 `content/shop/wallet.js` 的 `parsePrice()` 負責，吃得下
`D+500`／`DD+4600`／`C+3000`／`200`／`3XP`／`免費`。看不懂的寫法會 throw，不會默默當成 0 元。

## 7. 上架狀態：上架／掛名／不收

2026-08-17 依使用者「轉不出來的先放名字就好」的決定，商品有三種下場：

| 狀態 | 意義 | 硬性要求 |
| :--- | :--- | :--- |
| `上架` | 效果完整轉出，可購買 | 至少要有一個非敘事的數值效果 |
| `掛名` | 只登錄名稱／價格／出處，**不可購買** | `pendingReason` 必填且要指名缺哪個機制；`effects` 必須是空的；`originalTraits` 必填 |
| 不收 | 連掛名都不做 | 寫進 pack 的 `examinedButRejected`，附理由與行號 |

**【硬性】掛名不是偷懶的擋箭牌。** `pendingReason` 的理由永遠是「引擎缺某個機制」，
不會是「還沒空轉」。看過但決定連掛名都不做的（例如整個專長三級全滅的「健步如飛」），
一定要留在 `examinedButRejected`——否則下一個人會重新花時間看一遍同樣的條目，再放棄一次。

**【硬性】掛名商品要保留 `originalTraits`**，之後補效果時不必回頭再翻一次規則書。

## 8. 什麼時候可以擴充效果詞彙表

**【判斷】** 條件有三個，要同時成立：

1. **引擎裡已經有消費端。** 不可以先加效果種類、再期待有人去接。
   反例：「傷勢免惡化」看起來很合理，但 `core/health.js` 的 `tickWorsening()`
   在整個遊戲迴圈裡**一次都沒有被呼叫過**，加了等於免疫一件不會發生的事。
2. **至少有兩、三個同型條目在等。** 只為一個條目擴充詞彙表不划算。
   目前在等的例子：「降龍十八掌」需要的「交叉屬性附加成功」（以力量攻擊時獲得傳奇敏捷的附加成功）。
3. **`EFFECT_KINDS` 裡要寫得出 `target`**，也就是指得出它接到引擎的哪個欄位，
   並且在 `test/shopCatalog.test.js` 補一則「它真的改變了某個引擎函式輸出」的測試。

**已經走完這三關的例子：`型態`（2026-08-17）。** 它的三關長這樣——消費端是 `effects.js`
既有的三個查表函式加上 `encounterState.js` 的戰鬥迴圈（同一輪順便把玩家的防御/武器
真的改成從商品重算，在那之前 `encounterState.js` 用的是寫死的 `emptyCombatProfile()`）；
在等的條目有 Orphnoch、帝國子民、紅馬尾戰士、寫輪眼四個；測試在 `test/shopForms.test.js`，
每一組都是「開啟後某個引擎函式的輸出真的變了，到期後真的變回來」。

**同一輪補掉的一個舊洞**：`checkModifiersFor()` 先前的唯一呼叫端是測試——
`functions/api/` 底下三個會呼叫 `performCheck()` 的地方沒有一個呼叫它，
於是整個「專長」貨架買下去在真實遊戲裡完全不生效。這正是第 1 節在講的
「看起來有效、實際上沒有程式碼會讀它」，只是漏掉的不是欄位，是最後一段接線。
**擴充詞彙表時請一路追到 API 進入點，不要只追到查表函式。**

## 9. 不可以用商品表達的東西

**【硬性】** 有些型錄效果不是「加值」，是「改寫核心規則」。這些**不管詞彙表怎麼擴充都不該用商品表達**：

- **改寫死亡判定**（夜兔的「殺戮機器」：惡性填滿也不死，但受到任意 1 點傷害立刻死亡）。
- **替換一次判定的屬性與技能**（暗示術：用施法判定代替操控+說服）。
- **替換角色的整組衍生數值**（阿尼馬格斯：變成動物後 AC/HP 變成該動物的）。

這類東西如果真的要做，該做成引擎的一個新模式（像 `core/combat/` 那樣獨立一層），
不是塞進 `effects[]`。

## 10. 這一輪的範圍限制（使用者指定，2026-08-17）

- **【硬性】只放到 D 級。** 商品的 `rank` 只能是 `D` 或無分級，有測試鎖定。
- **【硬性】一整套（D→C→B→A→S）的資源，更高的級數只登錄名字**，
  放在 `higherRanks: [{rank, name}]`，不帶效果也不帶價格。
- **【硬性】七個模板化分類（血統／改造／瞳術／稱號／流派／技藝／法術）各要有 3 件商品**，
  轉不出來的用掛名補足，有測試鎖定。

## 11. 尚未解決的結構問題（碰到請不要硬幹，回報）

- **分歧升級路線**：阿蘭斯在 D 級之後分成科技路線與士兵路線，選定不可更改。
  目前的 `lineageId` + `rank` 結構表達不了「同一條線底下有兩條互斥的升級鏈」。
- **不從 D 級起跳的資源**：瞳術有 5 個從 C 級開始、2 個從 B 級開始。
  `startsAtRank` 欄位有了，但那些條目的效果還沒轉。
- **技能等級上限**：書上寫「白刃技能提升 1 級（最高 5 級）」，
  但簡化規則的技能上限只在建卡期（3 級）檢查，遊戲中沒有上限檢查，商品加技能不會被擋。
- **天生武器 vs 裝備武器**：目前兩者都是 `武器` 效果、都進同一個背包。
  金剛狼的骨爪要不要跟手上的刀共用欄位，還沒定案。型態授予的天生武器（Orphnoch 的手足）
  也走同一條路，差別只在型態一結束它就從 `weaponsFrom()` 的結果裡消失。
- **⚠️ `session.forms` 是一個沒有消費端的欄位（2026-08-17 查證，這一節先前的敘述是錯的）**：
  上一輪把「型態狀態還沒進存檔」列在這裡，第 3 輪加了存檔欄位之後就當它解決了。
  **它沒有解決，而且現在的形狀比原本更危險**——`content/storage/sessionStore.js` 的
  version 2 確實建了 `session.forms`，但全專案沒有任何一行程式碼讀它或寫它：

  | 缺的接線 | 現況 |
  | :--- | :--- |
  | 戰鬥外啟動型態的入口 | 不存在（`activateForm()` 在 `functions/` 底下沒有任何呼叫端） |
  | `createEncounter(character, enemy, { forms })` | `combat/start.js` 沒有傳 `forms`，開戰時一律空的 |
  | 三個 `applyCheckModifiers()` 呼叫點（check／turn／narrate） | 都沒有傳 `extraSources`，戰鬥外的型態吃不到 |

  這正是第 1 節在講的「寫進去、看起來有效、沒有程式碼會讀它」，只是這次躺在**存檔格式**裡。
  補的時候有一個真正的設計問題要先回答：**戰鬥外的「場景」什麼時候結束？**
  戰鬥裡有 `finalizeIfOver()` 當那個時刻，戰鬥外沒有——答錯就會做出一個永不過期的型態，
  也就是 `forms.js` 整個設計在避開的那件事。合理的候選是副本節點推進
  （`content/scenario/progress.js`），但那要先確認節點推進真的等於換場。

## 12. 目前的轉換率（每次做完請更新這張表）

| 分類 | 看過 | 上架 | 掛名 | 卡住的機制 |
| :--- | ---: | ---: | ---: | :--- |
| 物品（武器） | 7 | 7 | 0 | 無 |
| 個人專長 | 11 | 8 | 0 | 專業／重擲／不良狀態／移動速度（3 個整個不收） |
| 血統 | 3 | 2 | 1 | （夜兔改寫死亡判定，不該用商品表達） |
| 改造 | 3 | 2 | 1 | 屬性力的關鍵屬性是決心+風度，**兩維都不存在＝永久不可能**（已結案） |
| 瞳術 | 3 | **2** | **1** | 維持成本（每輪付費）、對目標資源動手、不良狀態、信仰 |
| 稱號 | 3 | 2 | 1 | 格擋（舊四層防御）、移動速度 |
| 流派 | 3 | 2 | 1 | 破甲／高速、不良狀態 |
| 技藝 | 3 | 2 | 1 | 原文只有特性名、沒有數值（不是我們轉不出來） |
| 法術 | 3 | 1 | 2 | 施法者職能、對抗判定框架 |
| 能量池來源 | **3** | **3** | 0 | 無（劍氣／查克拉／內力三條鏈的起點） |
| **合計** | **42** | **31** | **8** | |

**這張表就是待辦清單**，而 2026-08-17 這一輪是它第一次被當成待辦清單用：上一版把
「暫時性增益容器」與「意志力消費端」列為第 1、2 順位，這輪把兩個一起做掉（`content/shop/forms.js`），
結果如預測——Orphnoch 從掛名轉正、阿蘭斯把原本丟掉的「帝國子民」撿了回來。
**卡住的真的是引擎機制，不是轉換人力**，這個假設現在有一次實測支持。

上一版把「能量池」排成下一輪第 1 順位，接著又把它改成「卡在資料不是卡在程式」。
**第二個判斷才是錯的，而且錯得有教育意義——已經做完了，經過寫在下面。**

### 能量池：一次錯誤判斷的完整經過

第一次查證時，看到規則書第 604 行寫「每種能量在獲得時，都會同時獲得它的運作機制
（包括使用何者作為關鍵屬性，如何恢復、補充…）」，加上手上三個掛名條目都沒寫自己池子的大小，
於是判斷「書上刻意沒有通用的上限公式，能量池要等資料不是等工」。

**那個判斷是錯的。通用公式一直都在，只是被本專案自己的規則書精簡輪刪掉了。**
原文在 `生命.htm`，2026-08-17 的 trim 把它移出了，理由寫的是「能量池整套不實作」：

> 「能量池一般都擁有自己的關鍵屬性。關鍵屬性決定了能量池的上限。
>   關鍵屬性一般為 2 個屬性，這 2 個屬性之和即為該能量池大小的基礎上限。」

**教訓有兩層：**

1. **不要因為「目前不實作」就移出一頁規則。**「目前不實作」正是「將來要實作」的同義詞，
   而實作它時需要的就是那一頁。判斷標準應該是「這一頁的規則**已經住進程式碼**了嗎」。
   完整檢討與取回的原文在 `RULES_TRIM.md`。
2. **查書查不到某條通則時，先確認它是不是被自己刪掉的。** `RULES_TRIM.md` 的移出清單
   就是為此存在的，但第一次查證時沒有去對它。

實作見 `core/energyPools.js`：上限＝關鍵屬性之和、重複開啟補償 +5/+3/+1/+1、消耗與恢復。
已登錄關鍵屬性的池子（每個都附行號）：劍氣＝敏捷+感知、查克拉＝耐力+感知、內力＝感知+耐力。
書上有些池子的關鍵屬性是風度/決心，那四維在這個專案不存在，那種池子算不出上限，不收。

### 池子的第二輪：通則寫好之後，追一條新的鏈只剩抄兩個屬性名（2026-08-17 同日）

第一條鏈（劍氣）證明的是「機制接得起來」。同一天的第二輪追了查克拉與內力兩條，
**兩條加起來沒有新增任何一行引擎程式碼**——查克拉鏈整條都是既有詞彙表拼出來的：

```
買忍者(稱號) → 開查克拉池(上限＝耐力+感知) → 買寫輪眼(同一個池子的第二個來源，上限+5)
             → 花標準動作＋1點查克拉開「洞察眼」 → 攻擊+2DP、防御+2，持續一個場景
買內力心法    → 開內力池(上限＝感知+耐力) → 買葵花寶典(第二個來源，上限+5)
```

三件事值得記下來：

1. **池子來源在原文裡是一個獨立欄位。** 忍者、寫輪眼、內力心法、混元劍經的原文都有
   單獨的一行「能量池：X」，不是藏在特性文字裡。追來源因此很快——
   `grep -n "能量池：" rules-2.35.txt` 就是下一輪的待辦清單。
2. **寫輪眼掛名的理由本身有一半是看漏。** 上一輪寫的 `pendingReason` 是「需要查克拉能量池」，
   但寫輪眼自己那一頁的第 117996 行就寫著「能量池：查克拉」——它自己就是來源。
   下次寫 `pendingReason` 說「缺某個資源」時，先確認那個資源不是這個條目自己給的。
3. **通則被一個獨立條目複述了。** 葵花寶典原文寫「若已擁有內力則使內力上限提高5點」，
   而那個 5 正是 `REOPEN_BONUSES[0]`。所以葵花寶典的商品資料裡**沒有任何地方寫 5**，
   通則自己算得出來。這是目前「上一輪的通則抄對了」最強的旁證。

### 查不到 vs. 查到了但不可能：兩種結論都要留下來

上一輪的教訓是「查不到某條通則時，先確認它是不是被自己刪掉的」。這一輪碰到的是相反的情況：
**屬性力的關鍵屬性查得到，答案是決心+風度——兩個都是六維裡不存在的維度。**

紅馬尾戰士先前的掛名理由寫的是「關鍵屬性還沒查到」，那句話會讓下一個人再去查一次。
現在它改成「查到了，而且除非六維把決心與風度加回來，否則這一件不會有上架的一天」，
查證結果本身登錄在 `core/energyPools.js` 的 **`REJECTED_POOLS`**（附行號、附它擋住哪些商品，
有測試守著「被否決的池子擋住的商品確實還是掛名」）。

**一個查證過的否定結論如果不寫下來，它跟「還沒查」在下一個人眼裡長得一模一樣。**

### 目前的優先順序

1. ~~暫時性增益容器 ＋ 意志力消費端~~ ✅ 2026-08-17（`content/shop/forms.js`）
2. ~~戰鬥攻擊路徑查效果表 ＋ 複數匹配鍵 ＋ scope~~ ✅ 2026-08-17
3. ~~存檔格式（錢包／型態）＋ 商店 API 與前端 ＋ 主神空間門禁~~ ✅ 2026-08-17
4. ~~能量池~~ ✅ 2026-08-17（`core/energyPools.js`，並追上第一個池子來源：混元劍經）
5. ~~繼續追池子來源~~ ✅ 2026-08-17（忍者→查克拉→寫輪眼上架；內力心法→內力→葵花寶典撿回
   葵花真氣；屬性力查到底＝決心+風度，結案進 `REJECTED_POOLS`）
6. **⚠️ 把 `session.forms` 接起來（或拆掉）** —— 見第 11 節。存檔裡有這個欄位、
   前端的商店頁也列得出「可啟動的型態」，但戰鬥外沒有任何東西啟動得了型態，
   也沒有任何檢定路徑讀得到它。**這是目前唯一一個已知的、活在存檔格式裡的謊言**，
   排在所有轉換工作前面。
7. **不良狀態軌** —— 噁心／魅惑／凍結／劇痛／內耗點數。卡住的條目數量
   最多（毀滅之眼、降龍十八掌、夜兔、最蔡之人的兩條，以及寫輪眼被丟掉的「內耗」），
   而且書上的點數規則是寫死的，不缺數字。
   一個具體的起點：寫輪眼的內耗點數是目前**唯一一條被丟掉的限制**（丟掉它讓那件商品比書上強，
   見該條目的 `conversionNote`），做這一輪時它應該第一個被補回來。
8. **可變量型態** —— 書上大量寫「支付最多不超過X點，獲得等額加值」。目前 activation 只吃固定成本，
   卡住的至少三條：混元劍經的「劍氣」、內力心法的「初級內力增強」、以及每一個池子的「基礎用法」
   （劍氣／查克拉／內力那三頁各有一組，見 `POOL_DEFS` 裡的行號）。
9. **維持成本型態** —— 「每輪支付X點否則立刻結束」。型態目前只在啟動時收一次錢，之後靠時鐘到期。
   卡住的：葵花寶典的鬼魅身、毀滅之眼的破法雙瞳、寫輪眼 C 級以上的多數能力。
   這一條跟第 8 條是同一個容器的兩個缺口，適合同一輪做。

剩下的（施法者職能、對抗判定）是更大的工程，不建議在同一輪處理。

---

# 第二部分：目前的簡化規則總表

轉換時你需要知道「現在的規則長什麼樣」才知道能不能接得上。這一節按類別整理。
**數值的完整版在 `RULES_DIGEST.md`，這裡只列轉換時最常撞到的部分。**

標記：🟢 規則書逐字　🟡 本專案簡化　🔵 本專案原創

## A. 角色數值

| 項目 | 簡化後 | 原規則書 | 對轉換的影響 |
| :--- | :--- | :--- | :--- |
| 屬性 🟡 | **六維**：力量／敏捷／耐力／智力／感知／意志 | 九維（多決心／風度／操控／沉著） | 效果掛在那四維上的一律丟棄 |
| 技能 🟡 | **十技能**：格鬥／射擊／體魄／潛行／求生／偵察／技藝／醫療／秘識／交涉 | 22 技能＋子項專業 | 見第 4 節對映表 |
| 專業 🟡 | **不存在**（已明確決定不做） | 每技能有多個專業，提供專業加值 | 「獲得專業X」一律丟棄 |
| 生命值 🟢 | 完好／沖擊B／嚴重L／惡性A 四段層疊，總量守恆 | 同 | 治療類效果接得上 |
| 意志值 🟡 | `意志×2`，**啟動型態時會被扣**（`content/shop/forms.js`，2026-08-17 起） | 可花費換 DP、換防御、驅動能力 | 「花1點意志力變身/開眼」轉得出來了（型態的 `activation.willpower`）；「花1點意志力換1個DP」這種即時兌換仍然沒有 |
| 能量池 🟡 | **不存在**（`energyPools` 永遠是空物件） | 魔力／查克拉／內力／劍氣等各自的機制 | 掛在能量池上的效果一律丟棄 |
| 衍生值 🟡 | 生命上限＝耐力+體積(5)、意志＝意志×2、先攻＝敏捷+感知、防御＝min(敏捷,感知) | 公式含決心／沉著 | 先攻／防御加值接得上 |

## B. 判定

| 項目 | 簡化後 | 對轉換的影響 |
| :--- | :--- | :--- |
| 骰池 🟢 | `DP = 屬性 + 技能 ± 調整`，8/9/10 算成功，10 加骰 | `檢定加骰` 效果接這裡 |
| 附加成功 🟢 | 只有原始成功數 > 0 時才生效 | `附加成功` 效果接這裡 |
| 技能 0 級懲罰 🟢 | 心智系自動失敗／戰鬥身手 -1 成功／社交 -2 成功 | 商品加技能會讓角色跨過這道門檻，是真實的價值 |
| 加值類型 🟡 | **不存在**。內在／修行／器械／士氣／招式／洞察加值一律直接相加 | 「本加值為XX加值」「與YY不疊加」一律丟棄 |
| 豁免 🟡 | **不存在**。強韌／反射／意志豁免全走一般檢定 | 「以強韌豁免」的效果一律丟棄 |
| 對抗判定 🟡 | **沒有資料流程** | 需要目標抵抗的效果一律丟棄 |
| 重擲 🟡 | **不存在**（加骰是骰面觸發的，不是玩家宣告的） | 「每天一次可以重投」一律丟棄 |

## C. 戰鬥

| 項目 | 簡化後 | 原規則書 | 對轉換的影響 |
| :--- | :--- | :--- | :--- |
| 防御 🟡 | **單一 DC** = `min(敏捷,感知) + 技能補正 + 裝備防御` | 基礎／閃避／洞察／格擋四層 | 四層的加值全部落到 `防御` 這一個效果，處理方式必須一致 |
| 傷害減免 🟡 | **單一 Armor 值**：`max(0, 基礎傷害 − 護甲)` | 免疫→忽略→硬度→吸收→轉化→抗力→抵消 八步驟 | 「破甲」「防彈」「硬度」一律丟棄 |
| 傷害類型 🟡 | **不分類型** | 物理／能量／精神／力場／毒素／墜落 | 「純能量傷害」「火焰傷害」轉成一般傷害，類型記在 droppedTraits |
| 命中 🟡 | 原始成功數 **嚴格大於** 防御DC 才命中 | 同左（本專案版） | |
| 基礎傷害 🟡 | 總成功數 − 防御DC，**無武器別上限** | 各攻擊方式有 damageCap | 「傷害上限為X」一律丟棄 |
| 攻擊方式 🟢 | 七種，公式表寫死（見第 5 節） | 同 | 個別條目不可改寫公式 |
| 先攻 🟢 | `1D10 + 先攻值`，不受加骰影響 | 同 | `先攻` 效果接得上 |
| 行動經濟 🟢 | 自由／迅捷／移動／標準／整輪／全回合／反射／專注 | 同 | 額度有實作，但「降低一級動作」這類改寫還沒接線 |
| 不良狀態 🟡 | **不存在**（暈眩／流血／劇痛／惡心／疲乏／燃燒／措手不及） | 完整狀態軌 | 相關效果與免疫一律丟棄 |
| 移動距離 🟡 | **不存在**（抽象回合制，沒有以公尺計的移動） | 有 | 「移動速度+5米」一律丟棄 |
| 體積 🟡 | 只在生命上限公式裡當常數 5 | 物品佔位、武器體積、目標體積比較 | 「目標體積小於你」一律丟棄 |
| 隊友 🟡 | **單人遊戲，沒有友方單位** | 有 | 「友方單位獲得X」一律丟棄 |
| AoE／進階動作 🟡 | **刻意排除**（全力一擊／衝鋒／擒抱／摔絆／範圍攻擊） | 有 | 相關效果一律丟棄 |

## D. 成長與經濟

| 項目 | 簡化後 | 對轉換的影響 |
| :--- | :--- | :--- |
| 建卡 🟡 | 六題生平問答自動配點；屬性 8 點（上限 5，成本遞增）、技能 10 點（上限 3） | 商品不參與建卡，是進遊戲之後的成長路徑 |
| 屬性 XP 🟢 | 目前值 × 4 XP；0→1 與 1→2 同價 | |
| 技能 XP 🟢 | 0→1 固定 3XP；其餘 `(等級-1)×2`，**最低 1** | |
| 專長 🟢 | 每個專長點數 3XP（輪回隊 6XP），必須逐級按順序學 | 專長商品用 XP 定價，中間級不可缺號 |
| 貨幣 🟢 | 支線 DCBAS（3 進位無損拆組）／獎勵點數／XP | 見第 6 節 |
| 模板能力排他 🟢 | 血統／改造／瞳術／修真／魔導書五類，各只能有一個 | 商品的 `exclusiveGroup` 只能填這五個 |
| 依序購買 🟢 | 預設 D→C→B→A→S | |
| 死亡復活 🟢🟡 | 一輩子 2 次，費用＝未花＋已花分數總和（拿掉了書上「第二次需劇情道具」的門檻） | |
| 技能樹 🟡 | **不存在** | 「開啟X級技能樹」一律丟棄 |
| 施法者職能 🟡 | **不存在** | 法術類幾乎整批卡在這裡 |

## E. 本專案原創的系統 🔵

這些規則書裡沒有，是為了單人 AI 敘事新增的。轉換時**不要**把型錄效果硬塞進這些系統。

| 系統 | 內容 | 模組 |
| :--- | :--- | :--- |
| 劇情扭轉度 | 5 級，獎勵倍率 1.0~2.5、難度加值 0~6 | `content/scenario/divergence.js` |
| 時間預算 | 主線推進與 NPC 好感度共用一筆倒數 | `content/scenario/timeBudget.js` |
| NPC 好感度 | 6 級，判定加值 -2~+3（數值草案） | `content/affection.js` |
| 迫近度軌 | 0~6，判定成敗換算成世界狀態 | `content/scenario/threat.js` |
| 套路遞減 | 同一組屬性+技能連用會加 DC | `content/scenario/repetition.js` |
| 回合難度分級 | 5 級對應 DC 1~5 | `content/turnOptions.js` |
| 敘事結果分級 | margin → 大成功/成功/驚險/失敗/慘烈失敗 | `core/narration.js` |

---

## 附：一次轉換的標準流程

1. 用頁名或行號在 `rules-2.35.txt` 定位條目，**讀完整段**，不要只看特性名稱。
2. 抄下原文的價格、前提、每一條特性的完整文字。
3. 逐條特性走第 1 節的三格分流，同時記下 `droppedTraits` 的理由。
4. 前提用第 4 節的對映表翻成 `prerequisites`，原文保留在 `originalPrerequisite`。
5. 用 `auditGoodPricing()` 對一次模板價，落差寫進 `pricingNote`，**不要改數字**。
6. 決定 `status`：有數值效果就 `上架`，沒有就 `掛名`（並填 `pendingReason` 與 `originalTraits`）。
7. 一整套的資源，把更高級數的名字填進 `higherRanks`。
8. 跑 `node --test test/shopPacks.test.js`，紅的就回頭修。
9. 更新第 12 節的轉換率表。
