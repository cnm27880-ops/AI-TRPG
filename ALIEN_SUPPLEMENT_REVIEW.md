# 《異形補充.md》可用性與接入審查

**審查對象：** `/home/ubuntu/upload/異形補充.md`
**對照內容：** 原《異形(一).md》至《異形(四).md》、AI-TRPG 目前副本引擎與先前專案審查結果
**審查目的：** 判斷補充稿是否有效解決重大場景偏短的問題，並確認新增的事件、自由行動、狀態旗標、結局與跨副本影響是否可以直接使用。

---

## 一、總結結論

這份補充稿的品質比上一版明顯提升，尤其成功補足了你最在意的「重大場景敘述太短」問題。它已經不只是幾段結果文字，而是把場景 A 至 I 重新整理成完整的重大場景資料：每個場景都有用途、主持真相、玩家知識邊界、進入文字、推進節拍、預設處理方法、自由行動裁定、機器可讀狀態草稿與離場規則。

補充稿本身約 **1,908 行、3,145 個詞元／文字單位、146,633 位元組**，包含 **9 個重大場景、86 個完整結果文字 ID、27 個自由行動案例與 9 個狀態效果草稿**。從內容設計的角度來看，它已經足以支撐一場具有明確節奏與多種玩家處理方式的《異形》TRPG 副本。

不過，這份文件仍然不能直接宣稱「完全符合目前 AI-TRPG 專案架構規範」。它的末尾雖然列出「全部通過」，但那個結論只代表文件作者依照文字自我檢查，並不代表目前程式真的已經支援這些新欄位。實際上，現有引擎仍然沒有正式載入 `availableApproaches`、`worldFlags`、NPC 狀態、位置狀態、物品欄、感染狀態、事件 `effects` 或跨副本 `worldDelta`。

最準確的評價是：

> **補充稿已經達到「可以作為正式副本劇本與 AI GM 內容來源」的程度，但仍需要一次資料正規化與程式接入，才能成為真正可執行的副本包。**

| 評估項目 | 原稿 | 補充稿 | 判斷 |
|---|---:|---:|---|
| 重大場景敘事厚度 | 5.5/10 | 8.5/10 | 已達到可玩程度，核心場景有明確過程與情緒轉折 |
| 場景結構完整度 | 7/10 | 9/10 | A 至 I 均有統一格式 |
| 預設解法多樣性 | 7/10 | 8/10 | 自由行動範例明顯增加 |
| 結果文字完整度 | 3/10 | 7.5/10 | 重大場景大幅補齊，但不是全副本所有事件都完整 |
| 機器可讀性 | 4/10 | 6/10 | 有狀態草稿，但 effects 粒度仍不足 |
| 目前引擎直接相容性 | 3/10 | 3.5/10 | 仍需轉接層，補充稿沒有改變目前程式限制 |
| 跨副本承接價值 | 4/10 | 7/10 | 已有世界後果與旗標雛形，但缺少正式 outcome 格式 |
| 是否值得保留 | — | **是** | 不建議推倒重做 |

---

## 二、它確實解決了「劇情敘述偏短」的問題

這次補充不是單純把每段文字加長，而是增加了場景的戲劇結構。A 場景負責入界、規則說明與第一個威脅信號；B 場景讓 Ash 從普通科學官逐步露出非人破綻；C 場景把 937 指令設計成階梯式真相揭露；D 場景讓 Ash 的背叛成為前面異常的回收；E 場景將探索節奏切換成倒數逃亡；F 至 H 場景形成連續的追擊與終局戰；I 場景則提供戰後空白、感染檢查與結算前的最後選擇。

這樣的結構比上一版「setup 加幾句結果」更適合 AI GM。AI 不只知道要寫什麼場景，也知道場景應該如何開始、怎樣升高壓力，以及在什麼條件下離開。

目前最成功的幾段是：

| 場景 | 補強效果 |
|---|---|
| A 休眠室開場 | 玩家第一次看到主神手錶、陸遠、酸液與通風管異常，入界感完整 |
| B Ash 接觸 | 交涉、觀察與偷窺分開，Ash 的真相可以逐步被玩家推理出來 |
| C 937 揭露 | 有表面指令、殘缺片段、完整條文、警報與 Ash 收到通知的資訊階梯 |
| D Ash 突襲 | 由對峙、辯護、衝刺到戰鬥，已經接近完整高潮場景 |
| E 自毀倒數 | 明確把副本分成「還能探索」與「只能逃跑」兩個節奏階段 |
| F 通風管追擊 | 已有射擊、奔跑、冷卻劑與起重機、火牆、犧牲掩護等替代方案 |
| G 水仙號甦醒 | 成功建立「以為逃生，實際上威脅跟上船」的第二次恐怖轉折 |
| H 最終決戰 | 兩階段絕殺與安全繩、氣閘、魚叉、推進器的因果關係更清楚 |
| I 休眠結尾 | 不再是直接通關，而是把感染、樣本、遺言、救援 NPC 與結局串起來 |

因此，如果問題是「重大場景現在還會不會太簡短」，我的答案是：**已經不會。** 普通事件的文字可以維持較短，讓 AI GM 根據玩家行動延伸；A 至 I 這些重大場景已經有足夠的固定素材。

---

## 三、最值得保留的設計

### 3.1 玩家知識與主持真相分開

每個場景都列出「玩家在場景開始時已知」「玩家在場景結束時應知道」「玩家仍然不應知道」。這是非常重要的資料，因為它直接告訴 AI GM 哪些內容可以說、哪些內容只能透過觀察或事件逐步揭露。

例如場景 B 不直接把 Ash 是生化人的真相放到玩家開場文字，而是讓玩家透過無脈搏、恆定瞳孔、沒有呼吸起伏與異常關節活動逐步推理。這比讓旁白直接說「Ash 是生化人」更適合 TRPG。

### 3.2 自由行動不再只是口號

原稿雖然也有多種處理方法，但補充稿真正示範了玩家可以提出什麼樣的非預設行動。像是把 Ash 夾在氣密門、切斷主機通信、把 937 列印成實體證據、用起重機重物壓制異形、叫瀕死的陸遠掩護撤退、切斷水仙號照明、提前拔掉氣閘保險插銷等，已經能讓作者與 AI GM 理解「自由行動應該落在哪個範圍內」。

這些範例最有價值的地方，不是它們本身一定要成為遊戲選項，而是它們建立了合法性判斷的示範：玩家有沒有位置、工具、時間，以及這個行動會改變什麼。

### 3.3 Ash 的固定目標與可變反應分得更清楚

補充稿明確寫出 Ash 的 937 指令是核心鎖定，普通交涉不能讓他放棄任務；但交涉可以改變他提供的資訊、戒心與攻擊時機。這是正確的 NPC 設計：

> **NPC 的核心目標固定，NPC 的手段、情緒表現與對玩家的信任可以改變。**

這個原則未來可以直接複製到其他副本的反派、任務 NPC 與中立角色。

### 3.4 結局已經開始具備跨副本價值

補充稿不只寫「玩家成功或死亡」，也開始描述諾斯托羅莫號事件在後續世界中的影響，例如公司官方掩蓋、樣本促成生化研究、倖存者帶回證詞、玩家遺留物成為深空殘骸與感染基因進入主神資料庫。

這些內容很適合轉成跨副本的 `worldDelta`、NPC memory seed 與 unresolved thread。但目前仍需要把它們從結局文字中抽出成獨立資料，不能只依賴 AI 閱讀段落後自行推測。

---

## 四、必須修正的資料與程式問題

### 4.1 最大問題：狀態效果是「場景級」，不是「結果級」

每個場景末尾都有一個機器可讀狀態效果草稿，但它通常代表「場景成功完成後的總效果」，不是每個處理方法和每個骰子結果的效果。

例如場景 A 的草稿固定寫入：

```json
{
  "itemsAdd": ["item_flashlight"],
  "cluesAdd": ["clue_alien_trace"],
  "npcTrustDelta": {"npc_luyuan": 1},
  "timeCost": 1,
  "worldFlagsAdd": ["flag_cryo_cleared"]
}
```

但玩家如果在場景 A 的 `console.failure` 中斷操作，或在 `recon.critical_failure` 中提高威脅，這些效果不應該全部同時發生。玩家也不一定會拿到手電筒、取得線索或提高陸遠信任。

Ash 場景的問題更明顯：狀態草稿固定寫入 `npc_ash: destroyed`、`npc_luyuan: injured`、取得權限卡與解鎖自毀，但玩家可能在 Ash 戰鬥中失敗後逃跑，或者只是讓 Ash 暫時癱瘓，不能直接套用「Ash destroyed」。

應該改成每一個結果都有自己的 effects：

```json
{
  "resultId": "evt_ash_ambush.shoot.success",
  "effects": {
    "itemsAdd": ["item_access_card"],
    "npcStatusChanges": {
      "npc_ash": "destroyed"
    },
    "npcInjuries": {
      "npc_luyuan": ["wrist_injury"]
    },
    "worldFlagsAdd": ["flag_ash_destroyed"],
    "unlockEvents": ["evt_engine_coolant_prep", "evt_trigger_overload"]
  }
}
```

場景級 effects 可以保留，但只能作為「場景完成後的預設摘要」，不能作為正式裁定來源。

### 4.2 補充稿新增了多個目前尚未註冊的 ID

以下 ID 在補充稿中使用，但必須確認是否已加入正式副本資料表與程式 schema：

| 類型 | 新增或需確認的 ID | 問題 |
|---|---|---|
| 線索 | `clue_ship_map` | 原本線索表主要有 `clue_alien_trace`、`clue_crew_status`、`clue_order_937`、`clue_ash_synthetic`、`clue_vent_network`、`clue_narcissus_prep`，需要補登或改用既有 ID |
| 物品 | `item_order_937_doc` | 原物品表沒有這個實體文件，需要新增正式物品資料 |
| 狀態旗標 | `flag_cryo_cleared`、`flag_met_ash`、`flag_order_937_revealed`、`flag_ash_destroyed` 等 | 目前程式沒有正式 world flag registry |
| NPC 狀態 | `suspicious`、`destroyed`、`injured`、`survived` | 需要定義合法狀態集合，不能只在文字中自由增加 |
| 結局旗標 | `npc_luyuan_survived`、`npc_ripley_survived` | 命名看起來像 NPC ID，不像 `flag_`；需要統一命名規則 |
| 感染狀態 | `clean_or_infected`、`preserved_or_destroyed` | 這是兩個可能值的聯集，不是實際狀態，不能直接寫入 state |

補充稿宣稱所有物品、NPC 與線索 ID 都存在，但實際上至少有 `clue_ship_map` 與 `item_order_937_doc` 需要人工確認。這是內容生成模型很常見的自我檢查錯誤：格式看起來正確，不代表它真的讀過目前的完整 registry。

### 4.3 `skill: null` 仍然出現一次

補充稿的正文大多正確使用 JSON 的 `null`，但它的自我檢查結果不能直接信任。實際掃描補充稿時仍能找到一次 `skill` 與 `null` 的匹配，建議正式接入前再跑 JSON parser 與 schema validator，不要只用文字搜尋判斷。

最終格式必須是：

```json
{"attribute":"智力","skill":null,"difficulty":"困難"}
```

而不是：

```json
{"attribute":"智力","skill":"null","difficulty":"困難"}
```

### 4.4 `sampleStatus` 與 `infectionStatus` 使用了不合法的聯集字串

場景 I 的狀態草稿寫成：

```json
{
  "sampleStatus": "preserved_or_destroyed",
  "infectionStatus": "clean_or_infected"
}
```

這種寫法適合文件說明「可能是 A 或 B」，不適合當作正式狀態。程式需要一個確定值，例如：

```text
sampleStatus = none | tissue | intact | destroyed
infectionStatus = unknown | suspected | infected | cleared
```

在場景 I 的結果中，應該根據實際判定只寫入其中一個值。若尚未判定，使用 `unknown`，不要使用 `clean_or_infected`。

### 4.5 同一個場景的玩家結果可能互相矛盾

場景 H 把 `evt_narcissus_flush_airlock` 與 `evt_narcissus_final_purge` 合併成「水仙號終局絕殺」，敘事上沒有問題，但程式上仍然是兩個不同事件。前一個事件的目標是把異形逼到氣閘，後一個事件才是排氣與點火。

如果引擎把 H 當成一次事件處理，就會失去兩段戰術決策；如果引擎仍然把它當成兩個事件，文件就必須明確分別保存：

```text
phase_1 = alien_positioned_at_airlock
phase_2 = airlock_opened_and_thruster_ignited
```

同樣地，場景 G 的 `harpoon.success` 只代表玩家取得魚叉，不代表玩家已穿上宇航服；但場景 H 的固定真相又規定沒有宇航服與安全繩就一定死亡。這個條件應該在 `app_purge_tether_shoot` 的 required items／required states 中正式寫出，而不是只放在主持真相裡。

---

## 五、劇情與遊戲性上需要調整的地方

### 5.1 目前仍然太多「一次成功就保證解決」

補充稿大量使用「百分之百安全」「完全無傷」「絕對無法回頭」「全員安全」「唯一生路」等表述。這些句子在電影式劇本中很有力，但在 TRPG 系統裡容易造成一個問題：玩家只要通過一次檢定，後續風險就被文字提前消除。

建議保留少數真正的硬規則，例如「沒有安全繩開啟氣閘會死亡」，但把其他結果改成風險改變：

| 現在寫法 | 建議寫法 |
|---|---|
| 全員百分之百安全進入水仙號 | 成功取得安全進入窗口，但後續仍可能因其他狀態受傷 |
| 異形被徹底壓制 | 異形被壓制一至兩個行動輪，或威脅暫時降低 |
| 絕對無法回頭 | 主要路線關閉；若玩家付出重大代價，仍可能存在極端旁路 |
| 異形完全無法從此處追擊 | 該路線在短時間內無法直接追擊，但異形可能改走其他路線 |
| 無傷完成 | 不承受該方法的直接傷害，但仍可能增加噪音、耗材或後續風險 |

這樣才能讓成功變成「取得優勢」，而不是「永久取消挑戰」。

### 5.2 部分自由行動的成功後果過強

有三類案例需要特別調整：

第一，場景 F 中讓瀕死的陸遠自我犧牲，成功後寫成玩家「百分之百安全」並直接解鎖改裝沙漠之鷹。這會把一個重大的角色犧牲變成低難度意志／交涉檢定，且玩家可以藉此繞過追擊風險。建議把它改成：成功取得掩護窗口、陸遠死亡或永久離場、玩家仍要進行一次逃生檢定，武器是否保留則依陸遠先前狀態判定。

第二，場景 E 中請 Parker 協同拉閥，成功後宣稱全員無傷且沒有疲勞。NPC 協助可以降低難度或減少時間，但不應完全消除工程區的環境風險。

第三，場景 H 中用電池短路或魚叉刺入內顎等極端方法，成功後直接造成異形立即死亡。可以保留這些高風險解法，但難度、前置條件與後果應該比普通方法更嚴格，而且要保留酸血、爆炸、失壓或武器損壞等代價。

### 5.3 失敗後果有時仍沒有真正落到現有傷勢系統

補充稿使用了「生命值大幅下降」「右臂神經完全切斷」「三度燒傷」「行動力大幅受損」「瀕死警告」等表達，但目前專案有自己的生命值、傷勢與死亡邏輯。文字不能代替程式寫入狀態。

建議將結果效果改成既有或待新增的 injury ID，例如：

```text
burn_minor
burn_major
fracture_leg
fracture_arm
acid_burn_major
suffocation_major
bleeding_major
unconscious
```

並在每個結果中列出：

```json
{
  "injuriesAdd": ["acid_burn_major"],
  "damage": {
    "type": "acid",
    "severity": "major"
  },
  "deathCheck": false
}
```

若現有程式尚未支援這些 injury ID，就標記為「待程式新增」，不要讓 AI GM 假裝它們已經會自動生效。

### 5.4 時間單位混用了「十四小時、十四回合、最後十分鐘」

補充稿同時使用十四小時、十四回合與最後十分鐘。現有專案的時間預算主要是回合數，不是即時分鐘倒數。因此目前可以有兩種處理方式：

- 若想保留 TRPG 的簡潔性，就統一寫成「十四個行動回合」，自毀後改成「剩餘若干回合」或「倒數階段」。
- 若一定要使用「最後十分鐘」，就需要新增獨立的 `countdownPhase` 與每個行動的時間消耗，不要只在文字中宣稱時間已經變成十分鐘。

我會建議第一版先把它定義成：

```text
timeBudget = 14 turns
countdownPhase = none | overload_started | final_escape
```

等基本流程穩定後，再做分鐘級顯示。

### 5.5 結尾的感染規則需要更公平

場景 I 的感染結局很有戲劇性，但現在的文字容易造成玩家覺得「只要曾經受過傷就可能被感染」。應該明確區分：

- 普通酸液灼傷不等於寄生。
- 異形爪擊不一定等於感染。
- 只有特定事件或特定暴露方式會加入 `infectionStatus: suspected`。
- 最終醫療排查是確認或解除疑慮，不應憑空創造感染。
- 若玩家從未遇到幼體、抱臉體或明確污染源，不應因普通失敗突然感染。

這樣感染結局才會被玩家視為「自己曾經忽略的風險」，而不是系統在結尾抽獎。

---

## 六、跨副本承接的價值與不足

補充稿已經開始寫「可供後續同世界觀副本讀取的歷史影響」，這是很好的方向，但現在主要仍是結局旁的自然語言。要真正支援未來副本，應該在每個結局後增加一個獨立的 `scenarioOutcome`。

建議格式如下：

```json
{
  "scenarioId": "alien_biochemical_depths",
  "outcomeId": "end_heroic_rescue",
  "worldId": "alien_universe",
  "timelineId": "worldline_0001",
  "playerOutcome": {
    "alive": true,
    "infectionStatus": "cleared",
    "inventoryCarryOver": ["item_desert_eagle"],
    "injuriesCarryOver": ["burn_minor"]
  },
  "worldDelta": [
    {
      "key": "nostromo.status",
      "value": "destroyed",
      "persistence": "world",
      "provenance": "evt_trigger_overload"
    },
    {
      "key": "special_order_937.status",
      "value": "partially_exposed",
      "persistence": "world",
      "provenance": "evt_order_937_reveal"
    }
  ],
  "npcDeltas": [
    {
      "npcId": "npc_luyuan",
      "status": "alive",
      "relationship": "trusted_comrade",
      "memorySeeds": ["player_saved_luyuan_during_escape"]
    }
  ],
  "unresolvedThreads": [
    "company_coverup_active",
    "survivor_testimony_may_be_suppressed"
  ]
}
```

目前補充稿的跨副本資料缺少三項：

1. **知情範圍。** Ripley 親眼看見的事情，與公司從黑盒子記錄推測的事情不應相同。
2. **記憶來源。** 後續 NPC 需要知道某件事是親眼看見、聽人轉述、讀取公司檔案，還是只是一則傳聞。
3. **未解線索。** 後續副本需要知道哪些事情仍然可以承接，例如公司回收 937 資料、樣本是否流出、倖存者是否遭到追捕。

因此，補充稿很適合作為跨副本歷史的素材來源，但還沒有成為正式的跨副本資料格式。

---

## 七、建議的實際接入順序

### 第 1 步：先把補充稿當成 AI GM 劇本，不急著全部程式化

目前可以先把 A 至 I 的內容交給 AI GM prompt 使用，讓 AI 根據主持真相、自由行動表與結果文字進行敘事。這樣能先測試玩家是否真的會做出預期以外的行動，以及各場景是否過於線性。

### 第 2 步：統一所有 ID 與狀態值

先處理以下項目：

- `clue_ship_map` 是否新增或改用既有線索 ID。
- `item_order_937_doc` 是否正式加入物品表。
- 所有 `flag_` 是否建立 registry。
- 所有 NPC status 是否建立 enum。
- `sampleStatus`、`infectionStatus` 改成單一實際值。
- `npc_luyuan_survived` 等名稱改成一致的 flag 命名。
- 所有文字鍵使用完整事件前綴。
- 所有純屬性檢定使用真正的 JSON `null`。

### 第 3 步：將 effects 從場景級下沉到結果級

先不用一次處理所有結果。可以先處理重大場景的核心結果：

- `evt_order_937_reveal.query.success`
- `evt_ash_ambush.shoot.success`
- `evt_trigger_overload.pull.success`
- `evt_vent_ambush_escape.sprint.success`
- `evt_narcissus_final_purge.classic.success`
- `evt_hypersleep.clean.success`

只要這六個結果能正確寫入狀態，就能開始驗證「規則裁定、AI 敘事、狀態持久化」是否接通。

### 第 4 步：先讓結局輸出 scenarioOutcome

在 `settlement` 結算完成時保存：

```text
outcomeId
worldDelta
npcDeltas
memorySeeds
unresolvedThreads
playerCarryOver
```

下一個副本先只讀五至十個固定 world facts，不要一開始讀取全部 event log。

### 第 5 步：最後才加入完整 NPC 記憶與分支開場

當世界變更能穩定保存後，再讓 Ripley、陸遠、公司人員或後續作品的新 NPC 依照歷史改變態度、提供不同資訊與解鎖不同事件。

---

## 八、最終可用性分級

| 內容 | 可用性 | 判斷 |
|---|---|---|
| 場景 A 至 I 的重大場景敘事 | **A：可直接作為劇本素材** | 敘事厚度已足夠，建議只做校稿與邏輯修正 |
| 結果文字 | **A-：可直接作為 AI 演出素材** | 文字大多可用，但 effects 要獨立抽出 |
| 自由行動裁定範例 | **A-：可作為 AI GM 參考** | 很有價值，但部分成功後果過強 |
| 預設處理方法 | **B：可作為設計稿** | 需要統一前置條件、物品與事件完成規則 |
| 機器可讀狀態草稿 | **C：只能作為轉換中間稿** | 目前是場景級摘要，尚不足以直接執行 |
| 結局旗標 | **C：需要正規化** | 有概念但缺少正式 registry 與狀態來源 |
| 跨副本歷史影響 | **B：概念成立** | 已有素材，但要轉成 `worldDelta`、`npcDeltas` 與 memory seeds |
| 直接匯入目前引擎 | **D：目前不可行** | 現有 runtime 尚未支援事件卡層與持久世界狀態 |

---

## 九、最後結論

這份補充稿值得保留，而且它已經成功達成了本次最重要的目標：**重大場景不再只是幾句簡短的結果描述，而是已經有完整的進入、升壓、玩家介入、判定後果與離場轉折。**

我不建議你再要求 Gemini 立刻把這些場景寫得更長。現在更重要的工作不是繼續增加形容詞，而是做一次「資料正規化」：把文字中提到的物品、線索、NPC 狀態、威脅變化、傷勢、感染、樣本與結局條件逐一抽成程式可驗證的結果效果。

最需要優先修改的三件事是：

1. **把場景級 `stateEffects` 改成結果級 `effects`。**
2. **統一 ID、旗標與狀態值，修正 `clue_ship_map`、`item_order_937_doc` 與聯集狀態字串。**
3. **把「十四小時、十四回合、最後十分鐘」統一成引擎真正能計算的時間模型。**

完成這三項後，這份內容就不只是「Gemini 寫得很好的劇本」，而會變成可以真正驗證你 AI GM 理念的第一個完整副本。
