{
  "name": "wxh-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "無限恐怖TRPG 2.35 核心運算引擎——純規則運算，不含AI/LLM呼叫。",
  "scripts": {
    "test": "node --test",
    "deploy": "wrangler pages deploy public"
  }
}

